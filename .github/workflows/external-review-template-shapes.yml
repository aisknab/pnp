name: external-review-template-shapes-verification

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: external-review-template-shapes-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
          show-progress: false
          persist-credentials: false
          set-safe-directory: false

      - uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Verify external-review template hashes
        run: sha256sum -c external-review/templates/SHA256SUMS

      - name: Check external-review template-shape syntax
        run: node --check pcc-external-review-template-shape0.mjs

      - name: Run external-review template-shape tests
        run: node --test test/pcc-external-review-template-shape0.test.mjs
