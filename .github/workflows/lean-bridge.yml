name: lean-bridge

on:
  pull_request:
    paths:
      - 'lean-toolchain'
      - 'lakefile.lean'
      - 'lake-manifest.json'
      - 'lean/**'
      - 'lean/PNP/Concrete/LockedNANDSourceParser*.lean'
      - 'lean/PNP/Concrete/LockedNANDTargetEmitter*.lean'
      - 'lean/PNP/Concrete/LockedNANDPolynomialReduction.lean'
      - 'lean/PNP/Concrete/CNFToNAND.lean'
      - 'lean-audit/**'
      - 'lean-audit/PNPConcreteLockedNANDSourceParserAxiomAudit.lean'
      - 'lean-audit/PNPConcreteLockedNANDTargetEmitterAxiomAudit.lean'
      - 'lean-audit/PNPConcreteLockedNANDPolynomialReductionAxiomAudit.lean'
      - 'lean-audit/PNPConcreteCNFToNANDAxiomAudit.lean'
      - 'lean-regression/**'
      - 'lean-regression/PNPConcreteLockedNANDSourceParser.lean'
      - 'lean-regression/PNPConcreteLockedNANDTargetEmitter.lean'
      - 'lean-regression/PNPConcreteLockedNANDPolynomialReduction.lean'
      - 'lean-regression/PNPConcreteCNFToNAND.lean'
      - 'formal-publication0.mjs'
      - 'pcc-formal-reconstruction-status0.mjs'
      - 'pcc-formal-public-surface0.mjs'
      - 'package.json'
      - 'scripts/export-lean-theorem-inventory.mjs'
      - 'scripts/generate-formal-publication.mjs'
      - 'scripts/build-canonical-report.mjs'
      - 'scripts/pnp-verify-all.mjs'
      - 'publication/**'
      - 'status/LEAN_THEOREM_INVENTORY.json'
      - 'status/FORMAL_RECONSTRUCTION_STATUS.json'
      - 'public/pnp-theorem-inventory.json'
      - 'public/pnp-status.json'
      - 'canonical_proof_report.tex'
      - 'canonical_proof_report.pdf'
      - 'audits/lean-theorem-inventory0.test.mjs'
      - 'audits/formal-publication0.test.mjs'
      - 'audits/lean-root-target0.test.mjs'
      - 'audits/lean-concrete-machine0.test.mjs'
      - 'audits/lean-concrete-tape-handoff0.test.mjs'
      - 'audits/lean-concrete-tape-blank-equivalence0.test.mjs'
      - 'audits/lean-concrete-pipeline-tape-geometry0.test.mjs'
      - 'audits/lean-concrete-pipeline-input-framer0.test.mjs'
      - 'audits/lean-concrete-pipeline-output-handoff0.test.mjs'
      - 'audits/lean-concrete-pipeline-state-namespace0.test.mjs'
      - 'audits/lean-concrete-pipeline-sequential-state-namespace0.test.mjs'
      - 'audits/lean-concrete-pipeline-sequential-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-stage-bridges0.test.mjs'
      - 'audits/lean-concrete-terminal-output-packer0.test.mjs'
      - 'audits/lean-concrete-pipeline-terminal-bridge0.test.mjs'
      - 'audits/lean-concrete-pipeline-paired-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-machine-simulation0.test.mjs'
      - 'audits/lean-concrete-complexity0.test.mjs'
      - 'audits/lean-concrete-pipeline-refinement0.test.mjs'
      - 'audits/lean-concrete-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-layout0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau0.test.mjs'
      - 'audits/lean-concrete-cook-levin-verifier-tableau0.test.mjs'
      - 'audits/lean-concrete-cook-levin-local-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau-cnf-semantics0.test.mjs'
      - 'audits/lean-concrete-cook-levin-raw-tape-bridge0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-size0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-schedule0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-cursor0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-input-length0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-input-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-token-appender0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-token-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-complete-header0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-body-start-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-dynamic-token-cursor-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fifth-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-constraint-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-sign-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-first-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-second-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-third-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-terminator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-successor-token-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-second-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-third-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-fourth-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-fifth-padding-or-terminator-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-sixth-padding-or-opening-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-seventh-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-nand-semantics0.test.mjs'
      - 'audits/lean-nand-enumerator0.test.mjs'
      - 'audits/lean-nand-reference-minimum0.test.mjs'
      - 'audits/lean-locked-nand-baseline0.test.mjs'
      - 'audits/lean-locked-nand-threshold-boundary0.test.mjs'
      - 'audits/lean-locked-nand-carrier-trace0.test.mjs'
      - 'audits/lean-locked-nand-global-candidates0.test.mjs'
      - 'audits/lean-locked-nand-global-baseline-distinct0.test.mjs'
      - 'audits/lean-locked-nand-global-unsatisfiable-final-zero0.test.mjs'
      - 'audits/lean-locked-nand-global-semantic-threshold0.test.mjs'
      - 'audits/lean-concrete-locked-nand-semantic-reduction0.test.mjs'
      - 'audits/lean-concrete-locked-nand-source-parser0.test.mjs'
      - 'audits/lean-concrete-locked-nand-target-emitter0.test.mjs'
      - 'audits/lean-concrete-locked-nand-polynomial-reduction0.test.mjs'
      - 'audits/lean-concrete-cnf-to-nand0.test.mjs'
      - 'audits/lean-concrete-cnf-to-nand-polynomial-reduction0.test.mjs'
      - 'audits/lean-residual-routes0.test.mjs'
      - 'audits/lean-residual-gain-chain0.test.mjs'
      - 'audits/lean-residual-gain-stopping0.test.mjs'
      - 'audits/lean-residual-terminal-full-bridge0.test.mjs'
      - 'audits/lean-residual-terminal-mode-firewall0.test.mjs'
      - 'audits/lean-residual-terminal-projection-minimum0.test.mjs'
      - 'audits/lean-residual-terminal-projection-transfer0.test.mjs'
      - 'audits/lean-residual-terminal-saturation0.test.mjs'
      - 'audits/lean-residual-terminal-physical-support-completion0.test.mjs'
      - 'audits/lean-residual-terminal-support-extraction0.test.mjs'
      - 'audits/lean-residual-terminal-proper-support0.test.mjs'
      - 'audits/lean-residual-terminal-support-square-closure0.test.mjs'
      - 'audits/lean-residual-terminal-governed-support-completion0.test.mjs'
      - 'audits/lean-residual-terminal-frontier-pushout0.test.mjs'
      - 'audits/lean-residual-terminal-projection-square0.test.mjs'
      - 'docs/lean_residual_gain_chain.md'
      - 'docs/lean_residual_gain_stopping.md'
      - 'docs/lean_residual_terminal_full_bridge.md'
      - 'docs/lean_residual_terminal_mode_firewall.md'
      - 'docs/lean_residual_terminal_projection_minimum.md'
      - 'docs/lean_residual_terminal_projection_transfer.md'
      - 'docs/lean_residual_terminal_saturation.md'
      - 'docs/lean_residual_terminal_physical_support_completion.md'
      - 'docs/lean_residual_terminal_support_extraction.md'
      - 'docs/lean_residual_terminal_proper_support.md'
      - 'docs/lean_residual_terminal_support_square_closure.md'
      - 'docs/lean_residual_terminal_governed_support_completion.md'
      - 'docs/lean_residual_terminal_frontier_pushout.md'
      - 'docs/lean_residual_terminal_projection_square.md'
      - 'docs/lean_bridge.md'
      - 'docs/lean_concrete_locked_nand_polynomial_reduction.md'
      - 'docs/lean_concrete_cnf_to_nand_semantic_compiler.md'
      - 'docs/lean_concrete_cnf_to_nand_polynomial_reduction.md'
      - 'docs/lean_concrete_machine.md'
      - 'docs/lean_tape_handoff.md'
      - 'docs/lean_tape_blank_equivalence.md'
      - 'docs/lean_pipeline_tape_geometry.md'
      - 'docs/lean_pipeline_input_framer.md'
      - 'docs/lean_pipeline_output_handoff.md'
      - 'docs/lean_pipeline_state_namespace.md'
      - 'docs/lean_pipeline_sequential_state_namespace.md'
      - 'docs/lean_pipeline_sequential_compiler.md'
      - 'docs/lean_pipeline_stage_bridges.md'
      - 'docs/lean_terminal_output_packer.md'
      - 'docs/lean_pipeline_terminal_bridge.md'
      - 'docs/lean_pipeline_paired_compiler.md'
      - 'docs/lean_pipeline_compiler.md'
      - 'docs/lean_pipeline_machine_simulation.md'
      - 'docs/lean_concrete_complexity.md'
      - 'docs/lean_cook_levin_layout.md'
      - 'docs/lean_cook_levin_tableau.md'
      - 'docs/lean_cook_levin_verifier_tableau.md'
      - 'docs/lean_cook_levin_local_cnf.md'
      - 'docs/lean_cook_levin_tableau_cnf.md'
      - 'docs/lean_cook_levin_tableau_cnf_semantics.md'
      - 'docs/lean_cook_levin_raw_tape_bridge.md'
      - 'docs/lean_cook_levin_formula_size.md'
      - 'docs/lean_cook_levin_formula_schedule.md'
      - 'docs/lean_cook_levin_formula_cursor.md'
      - 'docs/lean_cook_levin_builder_input_length.md'
      - 'docs/lean_cook_levin_builder_input_prefix.md'
      - 'docs/lean_cook_levin_builder_token_appender.md'
      - 'docs/lean_cook_levin_builder_first_token_prefix.md'
      - 'docs/lean_cook_levin_builder_complete_header.md'
      - 'docs/lean_cook_levin_builder_body_start_prefix.md'
      - 'docs/lean_cook_levin_builder_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_first_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_separator_step.md'
      - 'docs/lean_cook_levin_builder_second_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_third_clause_separator_step.md'
      - 'docs/lean_cook_levin_builder_third_clause_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_fifth_clause_padding_run.md'
      - 'docs/lean_nand_semantics.md'
      - 'docs/lean_nand_enumerator.md'
      - 'docs/lean_nand_reference_minimum.md'
      - 'docs/lean_locked_nand_macros.md'
      - 'docs/lean_locked_nand_prefix.md'
      - 'docs/lean_locked_nand_baseline.md'
      - 'docs/lean_locked_nand_threshold_boundary.md'
      - 'docs/lean_locked_nand_carrier_trace.md'
      - 'docs/lean_locked_nand_global_candidates.md'
      - 'docs/lean_locked_nand_global_baseline_distinct.md'
      - 'docs/lean_locked_nand_global_unsatisfiable_final_zero.md'
      - 'docs/lean_locked_nand_global_semantic_threshold.md'
      - 'docs/lean_concrete_locked_nand_semantic_reduction.md'
      - 'docs/lean_concrete_locked_nand_source_parser.md'
      - 'docs/lean_concrete_locked_nand_target_emitter.md'
      - 'docs/lean_residual_routes.md'
      - 'docs/lean_theorem_inventory.md'
      - 'docs/proof_pipeline.md'
      - 'docs/FORMAL_RECONSTRUCTION.md'
      - 'docs/github_actions_audit.md'
      - 'README.md'
      - '.github/workflows/lean-bridge.yml'
  push:
    branches:
      - main
    paths:
      - 'lean-toolchain'
      - 'lakefile.lean'
      - 'lake-manifest.json'
      - 'lean/**'
      - 'lean/PNP/Concrete/LockedNANDSourceParser*.lean'
      - 'lean/PNP/Concrete/LockedNANDTargetEmitter*.lean'
      - 'lean/PNP/Concrete/LockedNANDPolynomialReduction.lean'
      - 'lean/PNP/Concrete/CNFToNAND.lean'
      - 'lean-audit/**'
      - 'lean-audit/PNPConcreteLockedNANDSourceParserAxiomAudit.lean'
      - 'lean-audit/PNPConcreteLockedNANDTargetEmitterAxiomAudit.lean'
      - 'lean-audit/PNPConcreteLockedNANDPolynomialReductionAxiomAudit.lean'
      - 'lean-audit/PNPConcreteCNFToNANDAxiomAudit.lean'
      - 'lean-regression/**'
      - 'lean-regression/PNPConcreteLockedNANDSourceParser.lean'
      - 'lean-regression/PNPConcreteLockedNANDTargetEmitter.lean'
      - 'lean-regression/PNPConcreteLockedNANDPolynomialReduction.lean'
      - 'lean-regression/PNPConcreteCNFToNAND.lean'
      - 'formal-publication0.mjs'
      - 'pcc-formal-reconstruction-status0.mjs'
      - 'pcc-formal-public-surface0.mjs'
      - 'package.json'
      - 'scripts/export-lean-theorem-inventory.mjs'
      - 'scripts/generate-formal-publication.mjs'
      - 'scripts/build-canonical-report.mjs'
      - 'scripts/pnp-verify-all.mjs'
      - 'publication/**'
      - 'status/LEAN_THEOREM_INVENTORY.json'
      - 'status/FORMAL_RECONSTRUCTION_STATUS.json'
      - 'public/pnp-theorem-inventory.json'
      - 'public/pnp-status.json'
      - 'canonical_proof_report.tex'
      - 'canonical_proof_report.pdf'
      - 'audits/lean-theorem-inventory0.test.mjs'
      - 'audits/formal-publication0.test.mjs'
      - 'audits/lean-root-target0.test.mjs'
      - 'audits/lean-concrete-machine0.test.mjs'
      - 'audits/lean-concrete-tape-handoff0.test.mjs'
      - 'audits/lean-concrete-tape-blank-equivalence0.test.mjs'
      - 'audits/lean-concrete-pipeline-tape-geometry0.test.mjs'
      - 'audits/lean-concrete-pipeline-input-framer0.test.mjs'
      - 'audits/lean-concrete-pipeline-output-handoff0.test.mjs'
      - 'audits/lean-concrete-pipeline-state-namespace0.test.mjs'
      - 'audits/lean-concrete-pipeline-sequential-state-namespace0.test.mjs'
      - 'audits/lean-concrete-pipeline-sequential-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-stage-bridges0.test.mjs'
      - 'audits/lean-concrete-terminal-output-packer0.test.mjs'
      - 'audits/lean-concrete-pipeline-terminal-bridge0.test.mjs'
      - 'audits/lean-concrete-pipeline-paired-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-compiler0.test.mjs'
      - 'audits/lean-concrete-pipeline-machine-simulation0.test.mjs'
      - 'audits/lean-concrete-complexity0.test.mjs'
      - 'audits/lean-concrete-pipeline-refinement0.test.mjs'
      - 'audits/lean-concrete-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-layout0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau0.test.mjs'
      - 'audits/lean-concrete-cook-levin-verifier-tableau0.test.mjs'
      - 'audits/lean-concrete-cook-levin-local-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau-cnf0.test.mjs'
      - 'audits/lean-concrete-cook-levin-tableau-cnf-semantics0.test.mjs'
      - 'audits/lean-concrete-cook-levin-raw-tape-bridge0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-size0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-schedule0.test.mjs'
      - 'audits/lean-concrete-cook-levin-formula-cursor0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-input-length0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-input-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-token-appender0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-token-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-complete-header0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-body-start-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-dynamic-token-cursor-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-third-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-first-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-second-literal-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-prefix0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fourth-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-fifth-clause-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-first-constraint-padding-run0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-separator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-sign-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-first-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-second-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-third-unary-unit-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-terminator-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-successor-token-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-second-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-third-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-fourth-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-fifth-padding-or-terminator-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-sixth-padding-or-opening-unary-opportunity-step0.test.mjs'
      - 'audits/lean-concrete-cook-levin-builder-second-constraint-seventh-padding-or-unary-opportunity-step0.test.mjs'
      - 'audits/lean-nand-semantics0.test.mjs'
      - 'audits/lean-nand-enumerator0.test.mjs'
      - 'audits/lean-nand-reference-minimum0.test.mjs'
      - 'audits/lean-locked-nand-baseline0.test.mjs'
      - 'audits/lean-locked-nand-threshold-boundary0.test.mjs'
      - 'audits/lean-locked-nand-carrier-trace0.test.mjs'
      - 'audits/lean-locked-nand-global-candidates0.test.mjs'
      - 'audits/lean-locked-nand-global-baseline-distinct0.test.mjs'
      - 'audits/lean-locked-nand-global-unsatisfiable-final-zero0.test.mjs'
      - 'audits/lean-locked-nand-global-semantic-threshold0.test.mjs'
      - 'audits/lean-concrete-locked-nand-semantic-reduction0.test.mjs'
      - 'audits/lean-concrete-locked-nand-source-parser0.test.mjs'
      - 'audits/lean-concrete-locked-nand-target-emitter0.test.mjs'
      - 'audits/lean-concrete-locked-nand-polynomial-reduction0.test.mjs'
      - 'audits/lean-concrete-cnf-to-nand0.test.mjs'
      - 'audits/lean-concrete-cnf-to-nand-polynomial-reduction0.test.mjs'
      - 'audits/lean-residual-routes0.test.mjs'
      - 'audits/lean-residual-gain-chain0.test.mjs'
      - 'audits/lean-residual-gain-stopping0.test.mjs'
      - 'audits/lean-residual-terminal-full-bridge0.test.mjs'
      - 'audits/lean-residual-terminal-mode-firewall0.test.mjs'
      - 'audits/lean-residual-terminal-projection-minimum0.test.mjs'
      - 'audits/lean-residual-terminal-projection-transfer0.test.mjs'
      - 'audits/lean-residual-terminal-saturation0.test.mjs'
      - 'audits/lean-residual-terminal-physical-support-completion0.test.mjs'
      - 'audits/lean-residual-terminal-support-extraction0.test.mjs'
      - 'audits/lean-residual-terminal-proper-support0.test.mjs'
      - 'audits/lean-residual-terminal-support-square-closure0.test.mjs'
      - 'audits/lean-residual-terminal-governed-support-completion0.test.mjs'
      - 'audits/lean-residual-terminal-frontier-pushout0.test.mjs'
      - 'audits/lean-residual-terminal-projection-square0.test.mjs'
      - 'docs/lean_residual_gain_chain.md'
      - 'docs/lean_residual_gain_stopping.md'
      - 'docs/lean_residual_terminal_full_bridge.md'
      - 'docs/lean_residual_terminal_mode_firewall.md'
      - 'docs/lean_residual_terminal_projection_minimum.md'
      - 'docs/lean_residual_terminal_projection_transfer.md'
      - 'docs/lean_residual_terminal_saturation.md'
      - 'docs/lean_residual_terminal_physical_support_completion.md'
      - 'docs/lean_residual_terminal_support_extraction.md'
      - 'docs/lean_residual_terminal_proper_support.md'
      - 'docs/lean_residual_terminal_support_square_closure.md'
      - 'docs/lean_residual_terminal_governed_support_completion.md'
      - 'docs/lean_residual_terminal_frontier_pushout.md'
      - 'docs/lean_residual_terminal_projection_square.md'
      - 'docs/lean_bridge.md'
      - 'docs/lean_concrete_locked_nand_polynomial_reduction.md'
      - 'docs/lean_concrete_cnf_to_nand_semantic_compiler.md'
      - 'docs/lean_concrete_cnf_to_nand_polynomial_reduction.md'
      - 'docs/lean_concrete_machine.md'
      - 'docs/lean_tape_handoff.md'
      - 'docs/lean_tape_blank_equivalence.md'
      - 'docs/lean_pipeline_tape_geometry.md'
      - 'docs/lean_pipeline_input_framer.md'
      - 'docs/lean_pipeline_output_handoff.md'
      - 'docs/lean_pipeline_state_namespace.md'
      - 'docs/lean_pipeline_sequential_state_namespace.md'
      - 'docs/lean_pipeline_sequential_compiler.md'
      - 'docs/lean_pipeline_stage_bridges.md'
      - 'docs/lean_terminal_output_packer.md'
      - 'docs/lean_pipeline_terminal_bridge.md'
      - 'docs/lean_pipeline_paired_compiler.md'
      - 'docs/lean_pipeline_compiler.md'
      - 'docs/lean_pipeline_machine_simulation.md'
      - 'docs/lean_concrete_complexity.md'
      - 'docs/lean_cook_levin_layout.md'
      - 'docs/lean_cook_levin_tableau.md'
      - 'docs/lean_cook_levin_verifier_tableau.md'
      - 'docs/lean_cook_levin_local_cnf.md'
      - 'docs/lean_cook_levin_tableau_cnf.md'
      - 'docs/lean_cook_levin_tableau_cnf_semantics.md'
      - 'docs/lean_cook_levin_raw_tape_bridge.md'
      - 'docs/lean_cook_levin_formula_size.md'
      - 'docs/lean_cook_levin_formula_schedule.md'
      - 'docs/lean_cook_levin_formula_cursor.md'
      - 'docs/lean_cook_levin_builder_input_length.md'
      - 'docs/lean_cook_levin_builder_input_prefix.md'
      - 'docs/lean_cook_levin_builder_token_appender.md'
      - 'docs/lean_cook_levin_builder_first_token_prefix.md'
      - 'docs/lean_cook_levin_builder_complete_header.md'
      - 'docs/lean_cook_levin_builder_body_start_prefix.md'
      - 'docs/lean_cook_levin_builder_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_first_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_separator_step.md'
      - 'docs/lean_cook_levin_builder_second_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_second_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_third_clause_separator_step.md'
      - 'docs/lean_cook_levin_builder_third_clause_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_third_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_first_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_second_literal_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_prefix.md'
      - 'docs/lean_cook_levin_builder_fourth_clause_padding_run.md'
      - 'docs/lean_cook_levin_builder_fifth_clause_padding_run.md'
      - 'docs/lean_nand_semantics.md'
      - 'docs/lean_nand_enumerator.md'
      - 'docs/lean_nand_reference_minimum.md'
      - 'docs/lean_locked_nand_macros.md'
      - 'docs/lean_locked_nand_prefix.md'
      - 'docs/lean_locked_nand_baseline.md'
      - 'docs/lean_locked_nand_threshold_boundary.md'
      - 'docs/lean_locked_nand_carrier_trace.md'
      - 'docs/lean_locked_nand_global_candidates.md'
      - 'docs/lean_locked_nand_global_baseline_distinct.md'
      - 'docs/lean_locked_nand_global_unsatisfiable_final_zero.md'
      - 'docs/lean_locked_nand_global_semantic_threshold.md'
      - 'docs/lean_concrete_locked_nand_semantic_reduction.md'
      - 'docs/lean_concrete_locked_nand_source_parser.md'
      - 'docs/lean_concrete_locked_nand_target_emitter.md'
      - 'docs/lean_residual_routes.md'
      - 'docs/lean_theorem_inventory.md'
      - 'docs/proof_pipeline.md'
      - 'docs/FORMAL_RECONSTRUCTION.md'
      - 'docs/github_actions_audit.md'
      - 'README.md'
      - '.github/workflows/lean-bridge.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lean-bridge-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lean-bridge:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          show-progress: false
          persist-credentials: false
          set-safe-directory: false

      - name: Install hosted-runner report dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            latexmk \
            lmodern \
            poppler-utils \
            texlive-latex-base \
            texlive-latex-recommended

      - name: Install pinned Elan and Lean
        env:
          ELAN_ARCHIVE_SHA256: df0b2b3a439961ffcbb3985214365ffe40f49bc871df04dff268c7d8e21ca8b2
        run: |
          archive="$RUNNER_TEMP/elan-v4.2.3.tar.gz"
          curl --proto '=https' --tlsv1.2 --fail --location --silent --show-error \
            --output "$archive" \
            https://github.com/leanprover/elan/releases/download/v4.2.3/elan-x86_64-unknown-linux-gnu.tar.gz
          echo "$ELAN_ARCHIVE_SHA256  $archive" | sha256sum -c -
          tar --no-same-owner -xzf "$archive" -C "$RUNNER_TEMP" elan-init
          "$RUNNER_TEMP/elan-init" -y --no-modify-path --default-toolchain "$(cat lean-toolchain)"
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Verify exact tool versions
        run: |
          elan --version | tee /tmp/elan-version.txt
          lean --version | tee /tmp/lean-version.txt
          lake --version | tee /tmp/lake-version.txt
          grep -F 'elan 4.2.3 (b6cec7e10' /tmp/elan-version.txt
          grep -F 'Lean (version 4.31.0' /tmp/lean-version.txt
          grep -F '68218e876d2a38b1985b8590fff244a83c321783' /tmp/lean-version.txt
          grep -F 'Lake version 5.0.0-src+68218e8 (Lean version 4.31.0)' /tmp/lake-version.txt

      - name: Audit explicit root and assumption inventory
        run: node --test audits/lean-root-target0.test.mjs audits/lean-concrete-machine0.test.mjs audits/lean-concrete-tape-handoff0.test.mjs audits/lean-concrete-tape-blank-equivalence0.test.mjs audits/lean-concrete-pipeline-tape-geometry0.test.mjs audits/lean-concrete-pipeline-input-framer0.test.mjs audits/lean-concrete-pipeline-output-handoff0.test.mjs audits/lean-concrete-pipeline-state-namespace0.test.mjs audits/lean-concrete-pipeline-sequential-state-namespace0.test.mjs audits/lean-concrete-pipeline-sequential-compiler0.test.mjs audits/lean-concrete-pipeline-stage-bridges0.test.mjs audits/lean-concrete-terminal-output-packer0.test.mjs audits/lean-concrete-pipeline-terminal-bridge0.test.mjs audits/lean-concrete-pipeline-paired-compiler0.test.mjs audits/lean-concrete-pipeline-compiler0.test.mjs audits/lean-concrete-pipeline-machine-simulation0.test.mjs audits/lean-concrete-complexity0.test.mjs audits/lean-concrete-pipeline-refinement0.test.mjs audits/lean-concrete-cnf0.test.mjs audits/lean-concrete-cook-levin-layout0.test.mjs audits/lean-concrete-cook-levin-tableau0.test.mjs audits/lean-nand-semantics0.test.mjs audits/lean-nand-enumerator0.test.mjs audits/lean-nand-reference-minimum0.test.mjs audits/lean-locked-nand-baseline0.test.mjs audits/lean-locked-nand-threshold-boundary0.test.mjs audits/lean-locked-nand-carrier-trace0.test.mjs audits/lean-residual-routes0.test.mjs audits/lean-residual-gain-chain0.test.mjs audits/lean-residual-gain-stopping0.test.mjs audits/lean-residual-terminal-full-bridge0.test.mjs audits/lean-residual-terminal-mode-firewall0.test.mjs audits/lean-residual-terminal-projection-minimum0.test.mjs audits/lean-residual-terminal-projection-transfer0.test.mjs audits/lean-residual-terminal-saturation0.test.mjs audits/lean-residual-terminal-physical-support-completion0.test.mjs audits/lean-residual-terminal-support-extraction0.test.mjs audits/lean-residual-terminal-proper-support0.test.mjs audits/lean-residual-terminal-support-square-closure0.test.mjs audits/lean-residual-terminal-governed-support-completion0.test.mjs audits/lean-residual-terminal-frontier-pushout0.test.mjs audits/lean-theorem-inventory0.test.mjs audits/formal-publication0.test.mjs

      - name: Audit uniform bounded verifier-tableau source
        run: node --test audits/lean-concrete-cook-levin-verifier-tableau0.test.mjs

      - name: Audit finite local Cook-Levin CNF compiler source
        run: node --test audits/lean-concrete-cook-levin-local-cnf0.test.mjs

      - name: Audit finite whole-tableau Cook-Levin CNF source
        run: node --test audits/lean-concrete-cook-levin-tableau-cnf0.test.mjs

      - name: Audit finite whole-tableau Cook-Levin CNF semantics
        run: node --test audits/lean-concrete-cook-levin-tableau-cnf-semantics0.test.mjs

      - name: Audit finite Cook-Levin raw-tape bridge
        run: node --test audits/lean-concrete-cook-levin-raw-tape-bridge0.test.mjs

      - name: Audit external Cook-Levin formula-size bound
        run: node --test audits/lean-concrete-cook-levin-formula-size0.test.mjs

      - name: Audit rectangular Cook-Levin formula schedule
        run: node --test audits/lean-concrete-cook-levin-formula-schedule0.test.mjs

      - name: Audit direct Cook-Levin formula cursor
        run: node --test audits/lean-concrete-cook-levin-formula-cursor0.test.mjs

      - name: Audit literal Cook-Levin builder input-length tally
        run: node --test audits/lean-concrete-cook-levin-builder-input-length0.test.mjs

      - name: Audit executable Cook-Levin builder input prefix
        run: node --test audits/lean-concrete-cook-levin-builder-input-prefix0.test.mjs

      - name: Audit standalone Cook-Levin builder token appender
        run: node --test audits/lean-concrete-cook-levin-builder-token-appender0.test.mjs

      - name: Audit composed Cook-Levin builder first-token prefix
        run: node --test audits/lean-concrete-cook-levin-builder-first-token-prefix0.test.mjs

      - name: Audit composed Cook-Levin complete width header
        run: node --test audits/lean-concrete-cook-levin-builder-complete-header0.test.mjs

      - name: Audit composed Cook-Levin body-start prefix
        run: node --test audits/lean-concrete-cook-levin-builder-body-start-prefix0.test.mjs

      - name: Audit composed Cook-Levin first-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-first-literal-prefix0.test.mjs

      - name: Audit composed Cook-Levin first-clause prefix
        run: node --test audits/lean-concrete-cook-levin-builder-first-clause-prefix0.test.mjs

      - name: Audit Cook-Levin dynamic token-cursor step
        run: node --test audits/lean-concrete-cook-levin-builder-dynamic-token-cursor-step0.test.mjs

      - name: Audit Cook-Levin first-clause padding run
        run: node --test audits/lean-concrete-cook-levin-builder-first-clause-padding-run0.test.mjs

      - name: Audit Cook-Levin second-clause separator step
        run: node --test audits/lean-concrete-cook-levin-builder-second-clause-separator-step0.test.mjs

      - name: Audit Cook-Levin second-clause first-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-second-clause-first-literal-prefix0.test.mjs

      - name: Audit Cook-Levin second-clause second-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-second-clause-second-literal-prefix0.test.mjs

      - name: Audit Cook-Levin complete second-clause prefix
        run: node --test audits/lean-concrete-cook-levin-builder-second-clause-prefix0.test.mjs

      - name: Audit Cook-Levin second-clause padding run
        run: node --test audits/lean-concrete-cook-levin-builder-second-clause-padding-run0.test.mjs

      - name: Audit Cook-Levin third-clause separator step
        run: node --test audits/lean-concrete-cook-levin-builder-third-clause-separator-step0.test.mjs

      - name: Audit Cook-Levin third-clause first-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-third-clause-first-literal-prefix0.test.mjs

      - name: Audit Cook-Levin third-clause second-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-third-clause-second-literal-prefix0.test.mjs

      - name: Audit Cook-Levin complete third-clause prefix
        run: node --test audits/lean-concrete-cook-levin-builder-third-clause-prefix0.test.mjs

      - name: Audit Cook-Levin third-clause padding run
        run: node --test audits/lean-concrete-cook-levin-builder-third-clause-padding-run0.test.mjs

      - name: Audit Cook-Levin fourth-clause separator step
        run: node --test audits/lean-concrete-cook-levin-builder-fourth-clause-separator-step0.test.mjs

      - name: Audit Cook-Levin fourth-clause first-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-fourth-clause-first-literal-prefix0.test.mjs

      - name: Audit Cook-Levin fourth-clause second-literal prefix
        run: node --test audits/lean-concrete-cook-levin-builder-fourth-clause-second-literal-prefix0.test.mjs

      - name: Audit Cook-Levin complete fourth-clause prefix
        run: node --test audits/lean-concrete-cook-levin-builder-fourth-clause-prefix0.test.mjs

      - name: Audit Cook-Levin fourth-clause padding run
        run: node --test audits/lean-concrete-cook-levin-builder-fourth-clause-padding-run0.test.mjs

      - name: Audit Cook-Levin fifth-clause padding run
        run: node --test audits/lean-concrete-cook-levin-builder-fifth-clause-padding-run0.test.mjs

      - name: Audit Cook-Levin first-constraint padding run
        run: node --test audits/lean-concrete-cook-levin-builder-first-constraint-padding-run0.test.mjs

      - name: Audit Cook-Levin second-constraint separator step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-separator-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal sign step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-sign-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal first unary-unit step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-first-unary-unit-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal second unary-unit step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-second-unary-unit-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal third unary-unit step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-third-unary-unit-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal terminator step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-terminator-step0.test.mjs

      - name: Audit Cook-Levin second-constraint first-literal successor-token step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-successor-token-step0.test.mjs

      - name: Audit Cook-Levin second-constraint padding-or-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-padding-or-unary-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint second padding-or-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-second-padding-or-unary-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint third padding-or-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-third-padding-or-unary-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint fourth padding-or-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-fourth-padding-or-unary-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint fifth padding-or-terminator opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-fifth-padding-or-terminator-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint sixth padding-or-opening-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-sixth-padding-or-opening-unary-opportunity-step0.test.mjs

      - name: Audit Cook-Levin second-constraint seventh padding-or-unary opportunity step
        run: node --test audits/lean-concrete-cook-levin-builder-second-constraint-seventh-padding-or-unary-opportunity-step0.test.mjs

      - name: Audit locked-NAND global carrier and trace equivalence
        run: node --test audits/lean-locked-nand-carrier-trace0.test.mjs

      - name: Build explicit PNP root
        run: lake build PNP

      - name: Re-export compiled theorem inventory
        run: npm run formal:inventory:check

      - name: Verify generated publication status and report source
        run: npm run formal:publication:check

      - name: Rebuild and verify canonical report PDF
        run: npm run report:check

      - name: Print root and conditional-bridge axioms
        run: lake env lean -DwarningAsError=true lean-audit/PNPBridgeAxiomAudit.lean

      - name: Print concrete bitstring and polynomial-kernel axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteBitStringAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete bitstring and polynomial kernel unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 41

      - name: Print concrete finite-rule machine axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteMachineAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete finite-rule machine kernel unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 38

      - name: Print concrete complexity axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteComplexityAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete complexity layer unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 48

      - name: Print blank-delimited output and pure handoff axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteTapeHandoffAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Blank-delimited output or pure handoff target unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 14

      - name: Print blank-materialization equivalence axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteTapeBlankEquivalenceAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Blank-materialization equivalence unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 21
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteTapeBlankEquivalence.lean

      - name: Print boundary-marked pipeline tape geometry axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineTapeGeometryAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Boundary-marked pipeline tape geometry unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 20

      - name: Print executable all-input framer axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineInputFramerAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Executable all-input framer unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 70

      - name: Print executable internal output-handoff axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineOutputHandoffAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Executable internal output handoff unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 23

      - name: Print collision-free pipeline state-namespace axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineStateNamespaceAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Collision-free pipeline state namespace unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 39

      - name: Print sequential pipeline state-namespace axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineSequentialStateNamespaceAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Sequential pipeline state namespace unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 26

      - name: Print uniform two-machine sequential compiler axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineSequentialCompilerAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Uniform two-machine sequential compiler unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcretePipelineSequentialCompiler.lean

      - name: Print executable pipeline stage-bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineStageBridgesAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Executable pipeline stage bridges unexpectedly depend on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 56

      - name: Print executable terminal raw-output packer axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteTerminalOutputPackerAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Executable terminal raw-output packer unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 69

      - name: Print executable handoff-to-terminal bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineTerminalBridgeAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Executable handoff-to-terminal bridge unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 59

      - name: Print uniform canonical-pair pipeline compiler axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelinePairedCompilerAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Uniform canonical-pair pipeline compiler unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 28
          lake env lean -DwarningAsError=true lean-regression/PNPConcretePipelinePairedCompiler.lean

      - name: Print uniform all-input pipeline compiler axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineCompilerAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Uniform all-input pipeline compiler unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 29
          lake env lean -DwarningAsError=true lean-regression/PNPConcretePipelineCompiler.lean

      - name: Print framed raw-machine simulation axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineMachineSimulationAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Framed raw-machine simulation unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 96

      - name: Print recursive raw-pipeline refinement axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcretePipelineRefinementAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Recursive raw-pipeline refinement unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 16
          lake env lean -DwarningAsError=true lean-regression/PNPConcretePipelineRefinementRecursive.lean

      - name: Print concrete publication-target axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteTargetAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete publication target unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 2

      - name: Print concrete CNF semantics and codec axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete CNF semantics or codec unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 80

      - name: Print concrete Cook-Levin layout axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinLayoutAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete Cook-Levin layout unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 86

      - name: Print concrete fixed Cook-Levin tableau axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinTableauAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete fixed Cook-Levin tableau unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 30

      - name: Print uniform bounded verifier-tableau axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinVerifierTableauAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Uniform bounded verifier tableau unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 41

      - name: Print finite local Cook-Levin CNF compiler axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinLocalCNFAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Finite local Cook-Levin CNF compiler unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 75

      - name: Print finite whole-tableau Cook-Levin CNF axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinTableauCNFAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Finite whole-tableau Cook-Levin CNF unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 81

      - name: Print finite whole-tableau Cook-Levin CNF semantic axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinTableauCNFSemanticsAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.")" -eq 161
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected tableau-CNF semantic axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Tableau-CNF semantics reached a forbidden project or choice axiom.' >&2
            exit 1
          fi

      - name: Print finite Cook-Levin raw-tape bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinRawTapeBridgeAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.(Tape\.|CookLevin\.)")" -eq 54
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(Classical.choice|Quot.sound|propext)(, (Classical.choice|Quot.sound|propext))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin raw-tape bridge axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin raw-tape bridge reached a forbidden project axiom.' >&2
            exit 1
          fi

      - name: Print external Cook-Levin formula-size axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinFormulaSizeAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.")" -eq 110
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin formula-size axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin formula-size proof reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinFormulaSize.lean

      - name: Print rectangular Cook-Levin formula-schedule axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinFormulaScheduleAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.")" -eq 79
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin formula-schedule axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin formula schedule reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinFormulaSchedule.lean

      - name: Print direct Cook-Levin formula-cursor axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinFormulaCursorAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.")" -eq 136
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin formula-cursor axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin formula cursor reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinFormulaCursor.lean

      - name: Print literal Cook-Levin builder input-length axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderInputLengthAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderInputLength\.")" -eq 39
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin builder input-length axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin builder input-length tally reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 32
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 1
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 6
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderInputLength.lean

      - name: Print executable Cook-Levin builder input-prefix axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderInputPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderInputPrefix\.")" -eq 40
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin builder input-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin builder input prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 29
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 1
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 10
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderInputPrefix.lean

      - name: Print Cook-Levin builder token-appender axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderTokenAppenderAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderTokenAppender\.")" -eq 68
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin builder token-appender axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin builder token appender reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 42
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 13
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 13
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderTokenAppender.lean

      - name: Print Cook-Levin builder first-token-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFirstTokenPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstTokenPrefix\.")" -eq 37
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin builder first-token-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin builder first-token prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 21
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 3
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 13
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFirstTokenPrefix.lean

      - name: Print Cook-Levin unary polynomial evaluator axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderUnaryPolynomialAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderUnaryPolynomial\.")" -eq 74
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin unary polynomial evaluator axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin unary polynomial evaluator reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 50
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 5
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 19

      - name: Print Cook-Levin complete-header builder axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderCompleteHeaderAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderCompleteHeader\.")" -eq 84
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin complete-header builder axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin complete-header builder reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 43
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 6
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 35
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderCompleteHeader.lean

      - name: Print Cook-Levin body-start-prefix builder axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderBodyStartPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderBodyStartPrefix\.")" -eq 60
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin body-start-prefix builder axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin body-start-prefix builder reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 30
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 5
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 25
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderBodyStartPrefix.lean

      - name: Print Cook-Levin first-literal-prefix builder axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFirstLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstLiteralPrefix\.")" -eq 74
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin first-literal-prefix builder axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin first-literal-prefix builder reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 21
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 3
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 50
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFirstLiteralPrefix.lean

      - name: Print Cook-Levin first-clause-prefix builder axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFirstClausePrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.(BuilderFirstClausePrefix|BuilderFirstLiteralPrefix\.rule_source_ne_acceptState)")" -eq 80
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin first-clause-prefix builder axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin first-clause-prefix builder reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 38
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 13
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 29
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFirstClausePrefix.lean

      - name: Print Cook-Levin dynamic-token-cursor-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderDynamicTokenCursorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.")" -eq 47
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin dynamic-token-cursor-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin dynamic token cursor reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 7
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 26
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderDynamicTokenCursorStep.lean

      - name: Print Cook-Levin first-clause-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFirstClausePaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.")" -eq 83
          test "$(printf '%s\n' "$audit_output" | grep -Fc "'PNP.Concrete.CookLevin.BuilderCompleteHeader.HeaderController.workRunExact_of_unit_or_separator'")" -eq 1
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin first-clause-padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin first-clause padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 36
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFirstClausePaddingRun.lean

      - name: Print Cook-Levin second-clause-separator-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondClauseSeparatorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSeparatorStep\.")" -eq 54
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-clause-separator-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-clause separator step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 15
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 30
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondClauseSeparatorStep.lean

      - name: Print Cook-Levin second-clause-first-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondClauseFirstLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseFirstLiteralPrefix\.")" -eq 85
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-clause first-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-clause first-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 25
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 18
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 44
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondClauseFirstLiteralPrefix.lean

      - name: Print Cook-Levin second-clause-second-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondClauseSecondLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.")" -eq 113
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-clause second-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-clause second-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 34
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 25
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 56
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondClauseSecondLiteralPrefix.lean

      - name: Print Cook-Levin complete second-clause-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondClausePrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClausePrefix\.")" -eq 55
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin complete second-clause-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin complete second-clause prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 15
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 10
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 32
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondClausePrefix.lean

      - name: Print Cook-Levin second-clause-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondClausePaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClausePaddingRun\.")" -eq 65
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.PaddingCountdown\.(machine|loop_workRunExact|loopSteps_le)'")" -eq 3
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-clause padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-clause padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 26
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 9
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondClausePaddingRun.lean

      - name: Print Cook-Levin third-clause-separator-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderThirdClauseSeparatorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClauseSeparatorStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSeparatorStep\.SeparatorCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin third-clause separator-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin third-clause separator step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderThirdClauseSeparatorStep.lean

      - name: Print Cook-Levin third-clause-first-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderThirdClauseFirstLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClauseFirstLiteralPrefix\.")" -eq 74
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseFirstLiteralPrefix\.FalseTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseFirstLiteralPrefix\.FirstLiteralSuffix\.(rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState|machine)'")" -eq 5
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin third-clause first-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin third-clause first-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 24
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 18
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 45
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderThirdClauseFirstLiteralPrefix.lean

      - name: Print Cook-Levin third-clause-second-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderThirdClauseSecondLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClauseSecondLiteralPrefix\.")" -eq 145
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin third-clause second-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin third-clause second-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 46
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 32
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 67
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderThirdClauseSecondLiteralPrefix.lean

      - name: Print Cook-Levin complete third-clause-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderThirdClausePrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClausePrefix\.")" -eq 55
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin complete third-clause-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin complete third-clause prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 10
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderThirdClausePrefix.lean

      - name: Print Cook-Levin third-clause-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderThirdClausePaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClausePaddingRun\.")" -eq 65
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.PaddingCountdown\.(machine|loop_workRunExact|loopSteps_le)'")" -eq 3
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin third-clause padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin third-clause padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 26
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 9
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderThirdClausePaddingRun.lean

      - name: Print Cook-Levin fourth-clause-separator-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFourthClauseSeparatorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFourthClauseSeparatorStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSeparatorStep\.SeparatorCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin fourth-clause separator-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin fourth-clause separator step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFourthClauseSeparatorStep.lean

      - name: Print Cook-Levin fourth-clause-first-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFourthClauseFirstLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFourthClauseFirstLiteralPrefix\.")" -eq 97
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.(TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)|TrueFalseSuffix\.(machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)|SecondLiteralSuffix\.(machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState))'")" -eq 16
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin fourth-clause first-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin fourth-clause first-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 33
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 25
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 57
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFourthClauseFirstLiteralPrefix.lean

      - name: Print Cook-Levin fourth-clause-second-literal-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFourthClauseSecondLiteralPrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFourthClauseSecondLiteralPrefix\.")" -eq 124
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderThirdClauseSecondLiteralPrefix\.(TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)|TrueFalseSuffix\.(machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)|TrueTrueFalseSuffix\.(machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)|SecondLiteralSuffix\.(machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState))'")" -eq 21
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin fourth-clause second-literal-prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin fourth-clause second-literal prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 46
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 32
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 69
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFourthClauseSecondLiteralPrefix.lean

      - name: Print Cook-Levin complete-fourth-clause-prefix axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFourthClausePrefixAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFourthClausePrefix\.")" -eq 55
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin complete fourth-clause prefix axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin complete fourth-clause prefix reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 10
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFourthClausePrefix.lean

      - name: Print Cook-Levin fourth-clause-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFourthClausePaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFourthClausePaddingRun\.")" -eq 65
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.PaddingCountdown\.(machine|loop_workRunExact|loopSteps_le)'")" -eq 3
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin fourth-clause padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin fourth-clause padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 26
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 9
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFourthClausePaddingRun.lean

      - name: Print Cook-Levin fifth-clause-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFifthClausePaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFifthClausePaddingRun\.")" -eq 65
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.PaddingCountdown\.(machine|loop_workRunExact|loopSteps_le)'")" -eq 3
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin fifth-clause padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin fifth-clause padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 28
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 9
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFifthClausePaddingRun.lean

      - name: Print Cook-Levin first-constraint-padding-run axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderFirstConstraintPaddingRunAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstConstraintPaddingRun\.")" -eq 65
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderFirstClausePaddingRun\.PaddingCountdown\.(machine|loop_workRunExact|loopSteps_le)'")" -eq 3
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin first-constraint padding-run axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin first-constraint padding run reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 26
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 9
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderFirstConstraintPaddingRun.lean

      - name: Print Cook-Levin second-constraint-separator-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSeparatorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintSeparatorStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSeparatorStep\.SeparatorCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint separator-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint separator step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSeparatorStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-sign-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSignStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralSignStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal sign-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal sign step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSignStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-first-unary-unit-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralFirstUnaryUnitStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralFirstUnaryUnitStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal first-unary-unit-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal first unary-unit step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralFirstUnaryUnitStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-second-unary-unit-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSecondUnaryUnitStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralSecondUnaryUnitStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal second-unary-unit-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal second unary-unit step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSecondUnaryUnitStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-third-unary-unit-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralThirdUnaryUnitStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralThirdUnaryUnitStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseSecondLiteralPrefix\.TrueTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal third-unary-unit-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal third unary-unit step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralThirdUnaryUnitStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-terminator-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralTerminatorStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.")" -eq 48
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondClauseFirstLiteralPrefix\.FalseTokenCursor\.(appender|machine|rules_length|rules_pairwise_query_distinct|machine_acceptState_ne_rejectState|rule_source_ne_acceptState)'")" -eq 6
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderDynamicTokenCursorStep\.CursorAdvance\.(malformedScratch_enters_dead|deadState_workStep)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal terminator-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal terminator step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 14
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 11
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 31
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralTerminatorStep.lean

      - name: Print Cook-Levin second-constraint-first-literal-successor-token-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSuccessorTokenStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralSuccessorTokenStep\.")" -eq 80
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor|followingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint first-literal successor-token-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint first-literal successor-token step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSuccessorTokenStep.lean

      - name: Print Cook-Levin second-constraint-padding-or-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintPaddingOrUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.")" -eq 80
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_optional_unary|secondFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint padding-or-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint padding-or-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintPaddingOrUnaryOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-second-padding-or-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSecondPaddingOrUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintSecondPaddingOrUnaryOpportunityStep\.")" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.WidthOptionalAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_two_optional_unary|thirdFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint second padding-or-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint second padding-or-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSecondPaddingOrUnaryOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-third-padding-or-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintThirdPaddingOrUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintThirdPaddingOrUnaryOpportunityStep\.")" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.WidthOptionalAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_three_optional_unary|fourthFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint third padding-or-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint third padding-or-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintThirdPaddingOrUnaryOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-fourth-padding-or-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFourthPaddingOrUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFourthPaddingOrUnaryOpportunityStep\.")" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.WidthOptionalAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_four_optional_unary|fifthFollowingTokenSlot_direct_eq_padding_or_f)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint fourth padding-or-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint fourth padding-or-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFourthPaddingOrUnaryOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-fifth-padding-or-terminator-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          outer_count="$(printf '%s\n' "$audit_output" | grep -E "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStep\." | grep -Ev "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStep\.WidthOptionalTerminatorAppender\." | wc -l)"
          test "$outer_count" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStep\.WidthOptionalTerminatorAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_four_optional_unary_and_optional_terminator|sixthFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint fifth padding-or-terminator opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint fifth padding-or-terminator opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-sixth-padding-or-opening-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSixthPaddingOrOpeningUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintSixthPaddingOrOpeningUnaryOpportunityStep\.")" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.WidthOptionalAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_four_optional_unary_and_optional_terminator_and_optional_opening_unary|seventhFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint sixth padding-or-opening-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint sixth padding-or-opening-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSixthPaddingOrOpeningUnaryOpportunityStep.lean

      - name: Print Cook-Levin second-constraint-seventh-padding-or-unary-opportunity-step axiom closure
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSeventhPaddingOrUnaryOpportunityStepAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintSeventhPaddingOrUnaryOpportunityStep\.")" -eq 66
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintPaddingOrUnaryOpportunityStep\.WidthOptionalAppender\.")" -eq 14
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.CookLevin\.BuilderSecondConstraintFirstLiteralTerminatorStep\.(encodeCNFTokens_eq_terminator_then_successor_and_four_optional_unary_and_optional_terminator_and_optional_opening_unary_and_optional_first_unary|eighthFollowingTokenSlot_direct_eq_padding_or_t)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot.sound|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected Cook-Levin second-constraint seventh padding-or-unary opportunity-step axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Cook-Levin second-constraint seventh padding-or-unary opportunity step reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 12
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 33
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSeventhPaddingOrUnaryOpportunityStep.lean

      - name: Print concrete CNF work-input bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFWorkInputAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete CNF work-input bridge unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 20

      - name: Print direct concrete CNF verifier bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFVerifierAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Direct concrete CNF verifier bridge unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 8

      - name: Print complete concrete CNF work-correctness axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFWorkAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete CNF work correctness unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 766

      - name: Print direct-wire NAND semantics axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDSemanticsAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Direct-wire NAND semantics unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 57

      - name: Print exact-width NAND enumerator axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDEnumeratorAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Exact-width NAND enumerator unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 41

      - name: Print NAND truth-table equivalence axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDTruthTableAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'NAND truth-table equivalence unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 29

      - name: Print exhaustive NAND reference-minimum axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDMinimumAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Exhaustive NAND reference minimum unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 27

      - name: Print concrete framed-composition axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDCompositionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete framed composition unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 50

      - name: Print concrete framed-slack axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPNANDSlackAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Concrete framed slack unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 15

      - name: Print typed local locked-NAND candidate axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDDirectAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Typed local locked-NAND candidates unexpectedly depend on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 48

      - name: Print direct-wire output-lower-bound axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPDirectWireBaselineAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Direct-wire output lower bound unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 25

      - name: Print source-derived locked-NAND accounting axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDBaselineAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Source-derived locked-NAND accounting unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 23

      - name: Print finite local locked-NAND baseline axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDLocalBaselineAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Finite local locked-NAND baselines unexpectedly depend on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 30

      - name: Print conditional locked-NAND threshold-boundary axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDThresholdBoundaryAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Conditional locked-NAND threshold boundary unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 32

      - name: Print locked-NAND carrier and trace-equivalence axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDCarrierTraceAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.DirectWire\.LockedNANDTrace\.")" -eq 71
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked-NAND carrier/trace axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked-NAND carrier/trace reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 18
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 13
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 40
          lake env lean -DwarningAsError=true lean-regression/PNPLockedNANDCarrierTrace.lean

      - name: Print locked-NAND global candidate assembly axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDGlobalCandidatesAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.DirectWire\.LockedNANDGlobalCandidates\.")" -eq 71
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked-NAND global-candidate axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked-NAND global candidates reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 6
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 2
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 63
          lake env lean -DwarningAsError=true lean-regression/PNPLockedNANDGlobalCandidates.lean

      - name: Print locked-NAND global BaselineDistinct axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDGlobalBaselineDistinctAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.DirectWire\.LockedNANDGlobalCandidates\.")" -eq 5
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[propext, Quot.sound\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked-NAND BaselineDistinct axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked-NAND BaselineDistinct reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 5
          lake env lean -DwarningAsError=true lean-regression/PNPLockedNANDGlobalBaselineDistinct.lean

      - name: Print locked-NAND global unsatisfiable final-zero axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDGlobalUnsatisfiableFinalZeroAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.DirectWire\.LockedNANDGlobalCandidates\.(fullCandidate_final_eq_false_of_unsatisfiable|fullCandidate_referenceMinimum_eq_baseline_of_unsatisfiable)'")" -eq 2
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[propext, Quot.sound\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked-NAND unsatisfiable final-zero axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked-NAND unsatisfiable final-zero reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 2
          lake env lean -DwarningAsError=true lean-regression/PNPLockedNANDGlobalUnsatisfiableFinalZero.lean
          node --test audits/lean-locked-nand-global-unsatisfiable-final-zero0.test.mjs

      - name: Print locked-NAND global semantic-threshold axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDGlobalSemanticThresholdAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.DirectWire\.LockedNANDGlobalCandidates\.")" -eq 8
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[propext, Quot.sound\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked-NAND semantic-threshold axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked-NAND semantic threshold reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 8
          lake env lean -DwarningAsError=true lean-regression/PNPLockedNANDGlobalSemanticThreshold.lean
          node --test audits/lean-locked-nand-global-semantic-threshold0.test.mjs

      - name: Print encoded locked-NAND semantic-boundary axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteLockedNANDSemanticReductionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.LockedNAND\.")" -eq 48
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected encoded locked-NAND semantic-boundary axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Encoded locked-NAND semantic boundary reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 4
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 37
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 7
          lake env lean -DwarningAsError=true lean-regression/PNPConcreteLockedNANDSemanticReduction.lean
          node --test audits/lean-concrete-locked-nand-semantic-reduction0.test.mjs

      - name: Print strict-v0 locked-NAND source-parser axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteLockedNANDSourceParserAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.Concrete\.LockedNAND\.")" -eq 380
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected strict-v0 locked-NAND source-parser axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Strict-v0 locked-NAND source parser reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 247
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 58
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 75

      - name: Run strict-v0 locked-NAND source-parser regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPConcreteLockedNANDSourceParser.lean

      - name: Run strict-v0 locked-NAND source-parser hostile audit
        run: node --test audits/lean-concrete-locked-nand-source-parser0.test.mjs

      - name: Print strict-v0 locked-NAND target-emitter axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteLockedNANDTargetEmitterAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.")" -eq 3295
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected strict-v0 locked-NAND target-emitter axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Strict-v0 locked-NAND target emitter reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'does not depend on any axioms')" -eq 2224
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext]')" -eq 429
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms: [propext, Quot.sound]')" -eq 642

      - name: Run strict-v0 locked-NAND target-emitter regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPConcreteLockedNANDTargetEmitter.lean

      - name: Run strict-v0 locked-NAND target-emitter hostile audit
        run: node --test audits/lean-concrete-locked-nand-target-emitter0.test.mjs

      - name: Print strict-v0 locked-NAND polynomial-reduction axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteLockedNANDPolynomialReductionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.")" -eq 16
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected strict-v0 locked-NAND polynomial-reduction axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Strict-v0 locked-NAND polynomial reduction reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms:')" -eq 16

      - name: Run strict-v0 locked-NAND polynomial-reduction regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPConcreteLockedNANDPolynomialReduction.lean

      - name: Run strict-v0 locked-NAND polynomial-reduction hostile audit
        run: node --test audits/lean-concrete-locked-nand-polynomial-reduction0.test.mjs

      - name: Print CNF-to-NAND semantic-compiler axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFToNANDAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          expected_count="$(node -p "require('./status/FORMAL_RECONSTRUCTION_STATUS.json').leanConcreteCNFToNANDSemanticCompilerAuditedDeclarationCount")"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.")" -eq "$expected_count"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected CNF-to-NAND semantic-compiler axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'CNF-to-NAND semantic compiler reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms:')" -eq "$expected_count"

      - name: Run CNF-to-NAND semantic-compiler regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPConcreteCNFToNAND.lean

      - name: Run CNF-to-NAND semantic-compiler hostile audit
        run: node --test audits/lean-concrete-cnf-to-nand0.test.mjs

      - name: Print all-input CNF-to-NAND polynomial-reduction axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPConcreteCNFToNANDPolynomialReductionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          expected_count="$(node -p "require('./status/FORMAL_RECONSTRUCTION_STATUS.json').leanConcreteCNFToNANDPolynomialReductionAuditedDeclarationCount")"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.")" -eq "$expected_count"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound|propext, Quot\.sound)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected all-input CNF-to-NAND polynomial-reduction axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'All-input CNF-to-NAND polynomial reduction reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms:')" -eq "$expected_count"

      - name: Run all-input CNF-to-NAND polynomial-reduction regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPConcreteCNFToNANDPolynomialReduction.lean

      - name: Run all-input CNF-to-NAND polynomial-reduction hostile audit
        run: node --test audits/lean-concrete-cnf-to-nand-polynomial-reduction0.test.mjs

      - name: Print explicit-list residual-route axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualRoutesAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Explicit-list residual routes unexpectedly depend on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 30

      - name: Print universal residual-gain-chain axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualGainChainAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Universal residual-gain chain unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 12

      - name: Print locked residual-gain-chain axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPLockedNANDResidualGainBoundAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          test "$(printf '%s\n' "$audit_output" | grep -Ec "^'PNP\.")" -eq 4
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext, Quot\.sound|Quot\.sound, propext)\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected locked residual-gain-chain axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Locked residual-gain chain reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Fc 'depends on axioms:')" -eq 4

      - name: Run residual-gain-chain regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualGainChain.lean

      - name: Run residual-gain-chain hostile audit
        run: node --test audits/lean-residual-gain-chain0.test.mjs

      - name: Print global strict-gain stopping axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualGainStoppingAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Global strict-gain stopping unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 12

      - name: Run global strict-gain stopping regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualGainStopping.lean

      - name: Run global strict-gain stopping hostile audit
        run: node --test audits/lean-residual-gain-stopping0.test.mjs

      - name: Print terminal full-carrier residual bridge axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalFullBridgeAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Terminal full-carrier residual bridge unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 22

      - name: Run terminal full-carrier residual bridge regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalFullBridge.lean

      - name: Run terminal full-carrier residual bridge hostile audit
        run: node --test audits/lean-residual-terminal-full-bridge0.test.mjs

      - name: Print terminal quotient mode firewall axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalModeFirewallAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          if printf '%s\n' "$audit_output" | grep -F 'depends on axioms:'; then
            echo 'Terminal quotient mode firewall unexpectedly depends on axioms.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$audit_output" | grep -Fc 'does not depend on any axioms')" -eq 29

      - name: Run terminal quotient mode firewall regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalModeFirewall.lean

      - name: Run terminal quotient mode firewall hostile audit
        run: node --test audits/lean-residual-terminal-mode-firewall0.test.mjs

      - name: Print terminal projection-minimum axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalProjectionMinimumAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.terminal")" -eq 27
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[propext\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal projection-minimum axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal projection minimum reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[propext\]')" -eq 27

      - name: Run terminal projection-minimum regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalProjectionMinimum.lean

      - name: Run terminal projection-minimum hostile audit
        run: node --test audits/lean-residual-terminal-projection-minimum0.test.mjs

      - name: Print terminal projection-transfer axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalProjectionTransferAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.(TerminalProjectionFourCorners|terminalProjectionDefect_int)")" -eq 8
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal projection-transfer axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal projection transfer reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 8

      - name: Run terminal projection-transfer regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalProjectionTransfer.lean

      - name: Run terminal projection-transfer hostile audit
        run: node --test audits/lean-residual-terminal-projection-transfer0.test.mjs

      - name: Print terminal saturation axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalSaturationAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 18
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal saturation axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal saturation reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 18

      - name: Run terminal saturation regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalSaturation.lean

      - name: Run terminal saturation hostile audit
        run: node --test audits/lean-residual-terminal-saturation0.test.mjs

      - name: Print terminal physical-support completion axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalPhysicalSupportCompletionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 40
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal physical-support completion axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal physical-support completion reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 40

      - name: Run terminal physical-support completion regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalPhysicalSupportCompletion.lean

      - name: Run terminal physical-support completion hostile audit
        run: node --test audits/lean-residual-terminal-physical-support-completion0.test.mjs

      - name: Print terminal support extraction axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalSupportExtractionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 34
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal support extraction axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal support extraction reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 34

      - name: Run terminal support extraction regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalSupportExtraction.lean

      - name: Run terminal support extraction hostile audit
        run: node --test audits/lean-residual-terminal-support-extraction0.test.mjs

      - name: Print terminal proper-positive support axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalProperSupportAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 37
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal proper-positive support axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal proper-positive support reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 37

      - name: Run terminal proper-positive support regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalProperSupport.lean

      - name: Run terminal proper-positive support hostile audit
        run: node --test audits/lean-residual-terminal-proper-support0.test.mjs

      - name: Print terminal saturated support-square axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalSupportSquareClosureAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 40
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal saturated support-square axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal saturated support square reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 40

      - name: Run terminal saturated support-square regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalSupportSquareClosure.lean

      - name: Run terminal saturated support-square hostile audit
        run: node --test audits/lean-residual-terminal-support-square-closure0.test.mjs

      - name: Print terminal governed support-completion axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalGovernedSupportCompletionAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 42
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal governed support-completion axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal governed support completion reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 42

      - name: Run terminal governed support-completion regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalGovernedSupportCompletion.lean

      - name: Run terminal governed support-completion hostile audit
        run: node --test audits/lean-residual-terminal-governed-support-completion0.test.mjs

      - name: Print terminal governed frontier-pushout axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalFrontierPushoutAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 39
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal governed frontier-pushout axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal governed frontier pushout reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 39

      - name: Run terminal governed frontier-pushout regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalFrontierPushout.lean

      - name: Run terminal governed frontier-pushout hostile audit
        run: node --test audits/lean-residual-terminal-frontier-pushout0.test.mjs

      - name: Print terminal governed projection-square axioms
        run: |
          audit_output="$(lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalProjectionSquareAxiomAudit.lean 2>&1)"
          printf '%s\n' "$audit_output"
          normalized="$(printf '%s\n' "$audit_output" | sed ':a;N;$!ba;s/\n / /g')"
          test "$(printf '%s\n' "$normalized" | grep -Ec "^'PNP\.DirectWire\.")" -eq 33
          unexpected="$(printf '%s\n' "$normalized" | grep -F 'depends on axioms:' | grep -Ev 'depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]$' || true)"
          if [ -n "$unexpected" ]; then
            printf '%s\n' "$unexpected" >&2
            echo 'Unexpected terminal governed projection-square axiom closure.' >&2
            exit 1
          fi
          if printf '%s\n' "$normalized" | grep -E 'Classical\.choice|sorryAx|PNP\.(CheckPCCPackexp|GeneratePCCPack|LockedNANDThreshold|ResidualBandExactMinimization)'; then
            echo 'Terminal governed projection square reached a forbidden project or choice axiom.' >&2
            exit 1
          fi
          test "$(printf '%s\n' "$normalized" | grep -Ec 'does not depend on any axioms|depends on axioms: \[(propext|Quot\.sound)(, (propext|Quot\.sound))*\]')" -eq 33

      - name: Run terminal governed projection-square regression
        run: lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalProjectionSquare.lean

      - name: Run terminal governed projection-square hostile audit
        run: node --test audits/lean-residual-terminal-projection-square0.test.mjs
