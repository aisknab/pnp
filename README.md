# pnp

**Public source and checker repository for a claimed proof that `P = NP`.**

> [!IMPORTANT]
> **Formal reconstruction is in progress. The repository does not currently establish `P = NP`,
> and public theorem emission is disabled.** The previous activated checker status has been
> withdrawn as proof authority because assertion-bearing records and trust objects do not replace
> derivations of their named mathematical propositions. See the
> [formal reconstruction notice](./docs/FORMAL_RECONSTRUCTION.md) and the active
> [machine-readable status](./status/FORMAL_RECONSTRUCTION_STATUS.json).

This repository contains the current Lean reconstruction, compiled theorem inventory,
fail-closed publication gate, generated nonclaiming canonical report, historical JavaScript checker
and replay records, tests, and reviewer documentation for a proposed
SAT-to-exact-NAND-minimization route. The claim is extraordinary and has not received independent
mathematical validation.

## Read this first

| Question | Current answer |
| --- | --- |
| **What is this repository?** | Source code, finite certificate records, checker and replay machinery, tests, release artefacts, and audit documentation for the author's claimed `P = NP` result. |
| **What extraordinary claim was proposed?** | The historical report claimed a deterministic polynomial-time SAT algorithm by reducing SAT to exact minimization of specially locked multi-output NAND words with residual slack at most four, then applying a claimed polynomial exact minimizer for that residual band. |
| **What is the current verification status?** | Formal reconstruction is in progress. Lean proves `PNP.Concrete.FinalUniversalDesign.cnfSATInNP : InNP CNFSAT` using a literal finite raw-machine verifier with an explicit polynomial bound. It recursively compiles every finite charged function/decision program into one literal raw machine, discharging `Formal.ConcreteComplexityMachineLink`. The concrete Cook–Levin formula is semantically equivalent to raw verifier execution; its canonical encoding has an explicit polynomial size bound and exact answer-independent schedule/cursor. The builder emits the complete first four populated clauses, traverses the remainder of the first scheduled constraint, emits the second constraint's separator and first positive literal through its width-selected successor, and consumes the next seven schedule opportunities: it emits nothing for width-one padding or the first four unary `T` tokens, the second literal's terminating `F`, and the following literal's opening and first unary-index `T` at wider widths. A fixed 351-rule literal bridge connects both exact post-header router terminal tapes to a shielded exact divider input and preserves arbitrary exterior builder workspace. A subsequent fixed 180-rule literal bridge copies the exact clause count from a restored sidecar, preserves the divider exterior and remainder ledger, and runs the quotient through a shielded comparator that agrees with every in-range body or Finish route. M215 then derives each canonical padding, body-token, or Finish entry without a supplied route or token and runs the existing fixed appender for every populated coordinate to the exact next emitted prefix. M216 recursively composes every verifier-derived post-header opportunity, proves every bounded output is the canonical prefix, reaches the exact complete encoded CNF token stream, and bounds aggregate staged work by one source-size polynomial. M217 then replaces the value-level selector-to-appender handoff with one fixed 64-rule physical optional-token dispatcher for arbitrary input, exterior workspace, output prefix, and all five tape-resident requests. M218 composes that fixed dispatcher across every canonical post-header coordinate, proves the bounded outputs reach the exact encoded CNF formula, and bounds total compiled dispatch work by one source-size polynomial. M219 closes the first literal classifier-to-request branch: for the unique Finish coordinate, it preserves the builder workspace as a protected comparator suffix, turns the equal terminal into an accepting classifier, writes M217's physical Finish request, and runs the fixed dispatcher in one collision-free 137-rule machine under an exact polynomial bound. M220 then composes the complete suffix-preserving classifier pipeline for every canonical post-header coordinate: one fixed collision-free 711-rule machine joins the router-to-divider bridge, exact divider, divider-to-comparator bridge and comparator through three exact tape handoffs over arbitrary protected workspace, agrees with typed body-or-Finish semantics, and retains exact compiled and polynomial bounds. M221 then derives the unique canonical Finish coordinate, reinterprets the M220 terminal verdict names without changing transition semantics, and chains the complete classifier to a one-cell writer in one collision-free 721-rule machine. The final tape changes exactly the focused end marker into the physical Finish request while preserving arbitrary workspace, with exact compiled and polynomial bounds. M222 inserts one blank sentinel before the canonical builder workspace, proves the complete classifier prefix is blank-free, and chains M221 to a fixed ten-rule left scanner. The collision-free 740-rule composition crosses exactly that prefix and halts at the spatial mirror of M217's canonical Finish request entry, with exact compiled and polynomial bounds. M223 then defines generic spatial reflection for literal work machines, proves exact execution commutes with reflection, reflects M217's fixed 64-rule dispatcher, and chains it after M222. The resulting collision-free 813-rule machine executes the unique full-classifier Finish path to a reflected appender endpoint containing the complete canonical CNF token stream, with exact compiled and polynomial bounds. Body-token and padding request generation, all-route dispatcher connection, and one literal repeated loop remain open. M224 then derives the first post-header coordinate and its separator request, executes the complete classifier's populated-body terminal, a fixed two-rule request writer, a fixed ten-rule workspace orienter, and M223's reflected dispatcher. The resulting collision-free 814-rule machine reaches the exact next canonical prefix ending in the first clause separator, with exact compiled and polynomial bounds. Arbitrary body-token and padding request generation, all-route dispatcher connection, and one literal repeated loop remain open. Separately, the global locked-NAND construction now has an exact typed semantic threshold, strict external bytes, and proved all-bitstring semantic transformations through concrete CNF-to-NAND and locked-NAND reductions. The report-facing SAT and locked-NAND endpoints now alias those exact concrete languages, reuse the concrete NP verifier and reduction, and require no duplicate locked-NAND project axiom or caller trust field. The residual layer proves finite semantic stopping and explicit PkgC restoration, ambient-ledger, V54, V53, and grouped BN6 boundaries. One conditional theorem consumes an explicit gain-coverage certificate asserting that every strict equivalent gain occurs as an original payload in a canonical Packet source cell; under precisely that premise, exhaustive scan silence yields a proof-bearing semantic minimum. The finite charge-surplus kernel derives strict replacement weight and a genuine gain from exact occurrence accounting, gate accounting, and semantics. The unit-charge bridge validates data-only replacement blueprints: a constructive exact-occurrence checker derives canonical ledgers from the two gate counts, checks semantic equivalence, constructs the charge-surplus realization, and scans every canonical handle in one supplied explicit family. The typed-realizer contract checks arbitrary finite faithful-selector tables and accepts only a genuine blueprint gain, an active same-or-lower-rank HN bot, an active same-or-lower-rank budget bot, or a faithful strictly lower-rank seed bot; its grouped-BN6 specialization covers every canonical handle. Its 20-declaration axiom closure excludes `Classical.choice`. The HB blocker-graph acyclicity contract checks that every supplied dependency edge strictly descends the exact ten-coordinate residual rank. The total-table contract removes the independent edge-list input: every finite HN/BUD node has one data-only row, every listed dependency is materialized mechanically as an edge, and acceptance yields exact representation coverage, well-founded rank induction, and exclusion of every nonempty directed cycle. Its 21-declaration axiom closure also excludes `Classical.choice`. The table, rank mapping, blocker semantics, semantic dependency completeness, and local invariant premise remain explicit or open, so this is not the full HB negative closure, global selector silence, or ZeroSlack. The checked HB active-dependency closure exhaustively requires every active HN/BUD node to name an active dependency in its total row; combined with exact-rank descent, well-founded induction proves every supplied activity bit false and removes HN/budget bot branches from the typed-realizer result, leaving a genuine gain or faithful lower seed. Its 13-declaration axiom closure excludes `Classical.choice`. The conditional selector-silence rank closure consumes explicit global semantic gain exclusion and uses strong induction on the supplied finite selector rank to prove every canonical handle in the accepted table nonfaithful. A specialization derives that premise from the existing explicit gain-coverage certificate and exact source-cell no-gain evidence. Its four theorem declarations use only `propext` and `Quot.sound`. The new executable selector-silence induction removes the global semantic no-gain premise: an exhaustive checker requires every canonical realizer claim to be a typed bottom, checked HB closure eliminates HN/BUD bottoms, and strong rank induction eliminates faithful lower seeds. Its 9 public declarations have the same restricted standard axiom closure. The family, rank and faithfulness tables, realizer claims, blocker sidecars, and dependency rows remain terminal-relative data inputs; the theorem does not construct them from terminal data or establish selector faithfulness or compatibility, unconditional HB negative closure, ZeroSlack, or polynomial runtime. Subsequent Packet routing proves exact earliest-field failures, computes residual descent from RankWF, copies the table-owned handle rank, reflects the canonical internal source route and positive source charge, and now computes canonical grouped-footprint colour eligibility plus typed frontier-signature equality; a frontier result carries exact signature inequality, colour, charge, rank, and exact-route results are impossible, and four semantic routes still lack external terminal semantics. The unary codes still have no encoded-circuit-size bound, and Lean still does not construct the coverage certificate, blueprints, rank assignment, faithfulness predicate, or blocker tables from terminal data, prove blocker semantics or semantic dependency completeness, establish selector faithfulness or compatibility, or prove polynomial selector generation/runtime. The surrounding finite results also do not derive all PkgC and BN6 inputs from a terminal candidate or prove complete route silence. M224 now executes the first populated separator route through one fixed 814-rule classifier, request-writer, orienter and reflected-dispatcher composition and reaches the exact next canonical emitted prefix with exact compiled and polynomial bounds, but arbitrary body-token and padding requests are not generated, all classifier outcomes are not connected to dispatch, and successive configurations are not one literal raw-machine loop; Lean still does **not** finish that formula builder, prove `CNFSAT ∈ P`, put the exact locked-NAND target in deterministic polynomial time, prove the remaining NP-hardness/NP-completeness links, or prove `P = NP`. `PNP.Main.p_eq_np` remains absent and the publication gate stays false. |
| **What can a hash check establish?** | That retrieved bytes match a published checksum ledger, subject to the hash implementation and collision assumptions. It does **not** establish theorem correctness, checker soundness, or correct generation. |
| **What can the checker establish?** | That the supplied records satisfy the predicates implemented by the named checker and its linkage rules. Checker acceptance does **not** independently establish that those predicates are mathematically sufficient or correctly implemented. |
| **What remains formally?** | The Lean toolchain/root, concrete machine kernel, all executable pipeline stages, recursive function/decision `RawRefinement`, the finite charged-pipeline P/NP/reduction interface, direct proof that `CNFSAT ∈ NP`, the current Cook–Levin schedule/builder prefix, the all-coordinate semantic post-header decoder, the fixed raw quotient/remainder divider, and their source-size-bounded literal tape bridge with arbitrary workspace preservation, post-divider body-versus-Finish raw classifier, all-coordinate canonical selected-token launch, and complete semantic schedule iteration, both concrete CNF-to-NAND and strict locked-NAND polynomial reductions, their exact report-facing compatibility layer, the locked-NAND baseline through its total polynomial machines, and the current finite residual kernels through explicit BN5, PkgC restoration and ambient reduction, V54, V53, grouped BN6 packets, payload-backed Packet selector seeds, exact grouped-footprint membership, canonical handles, a total unary handle codec, exact source-payload materialization, a source-cell candidate-gain scan, an exhaustive scan over every selector in the supplied finite family, its conditional gain-or-ZeroSlack upgrade under an explicit global gain-coverage certificate, the arbitrary finite occurrence-ledger charge-surplus kernel, the constructive supplied-family unit-charge blueprint validator/realizer, the arbitrary-finite data-only typed gain/HN/budget/lower-seed contract, supplied exact-rank HN/BUD blocker-graph acyclicity, total-table HB representation closure with well-founded induction, checked HB active-dependency closure with all supplied activity false, conditional supplied-table selector-silence rank closure under explicit global semantic gain exclusion, and executable selector-silence induction without that global no-gain premise are formalized and axiom-audited. Exact first-route semantics, RankWF descent reflection, canonical handle-rank reflection, internal source-route reflection, positive-source-charge reflection, canonical grouped-footprint colour reflection, and typed frontier-signature reflection additionally exclude colour, charge, rank, and exact-route results while preserving frontier inequality and exact failure evidence; the four remaining routes are still supplied-field boundaries. Remaining work includes implementing one literal raw-machine selector and physical stage-to-stage loop, proving complete builder `RawRefinement`, and packaging the Cook–Levin reduction; using the now-concrete locked-NAND endpoint to derive a deterministic SAT theorem; proving the remaining NP-hardness/NP-completeness transport and a deterministic `CNFSAT ∈ P` decider; deriving the explicit residual inputs, grouped family, replacement blueprints, gain-coverage certificate, faithful rank table, dependency table, and blocker-activity tables from terminal candidates; completing PkgC route integration and silence; deriving blocker semantics, semantic dependency completeness, and the checked local active-dependency premise from terminal data; establishing selector faithfulness or compatibility, unconditional HB negative closure, and complete route silence; bounding and generating the selector universe in encoded circuit size; proving polynomial runtime; formalizing unconditional global `ZeroSlack` and polynomial PCCMin; eliminating assumptions; and auditing the root theorem. `PNP.Main.ConcretePEqualsNP` is only an inactive definition; `PNP.Main.p_eq_np` is absent. |
| **What is the current canonical report?** | The root TeX/PDF is a generated, concise formal-reconstruction report with theorem emission disabled. The historical 56-page claim manuscript is available only at the pinned legacy coordinate recorded under `archive/legacy-v0/`. |
| **How is progress measured?** | Two separate metrics are used. [Formal artefact coverage](./docs/proof_progress.md) is 200 of 202 current scoped publication rows earned; it is not proof completion and its denominator can grow. The fixed-checkpoint risk-weighted proof completion estimate is 35 percent, with a 20 to 40 percent uncertainty range. Zero of five global gates are closed, the compiled project-specific axiom inventory is empty, `PNP.Main.p_eq_np` is absent, and the publication gate is false. |
| **How do I run the current verification?** | Run `npm ci --ignore-scripts` and `npm run pnp:verify -- --no-write`. This checks the non-claiming formal status, current package surface, pinned archive identity, and the small current-authority test suite; it is not a proof verification. |
| **Where should reviewers start?** | Start with the current-authority [compiled Lean theorem inventory](./docs/lean_theorem_inventory.md) and [formal reconstruction notice](./docs/FORMAL_RECONSTRUCTION.md). The reviewer guide, proof pipeline, terminology crosswalk, trust model, and audit questions are historical checker-route review aids whose numbered report citations target the pinned 56-page manuscript. |

The preceding residual milestone exhaustively checks the exact numerical
activation-coherence condition left open by the finite BCEL-ready/Packet
carrier binding. It compares the computed M183 terminal defect with the
declared Packet cut value and with the activation weight of every canonical
nonempty proper cut of the exact M184 family. Hypothetical acceptance recovers
the mapped-cut projection equation, but the already checked same-family Packet
exclusion forces deterministic rejection with either a cut-value mismatch or
one proof-bearing proper-cut mismatch. This diagnoses the missing bridge; it
does not prove activation coherence. The terminal problem, positive premise,
family, map, activation data, payloads, tables, and ranks remain supplied, and
unconditional ZeroSlack, polynomial PCCMin, SAT in P, and `P = NP` remain open.

M187 makes `ResidualBandExactMinimization` the concrete
fail-closed encoded direct-wire candidate/threshold predicate already used by
the locked-NAND endpoint. It proves exact reference-minimum semantics for every
intrinsically typed finite candidate and compiles the locked-to-residual edge as
the identity polynomial reduction, removing both the residual-band language
axiom and its caller-supplied trust field. The compiled inventory therefore
contains two project-specific axioms at that coordinate. Exhaustive `referenceMinimum` remains a
semantic specification, not an executable polynomial minimizer: this does not
prove PCCMin, ZeroSlack, deterministic SAT in P, the eligible root theorem, or
`P = NP`, and all five global gates remain open.

M188 replaces those final two opaque PCCPack declarations
with transparent typed definitions. `GeneratePCCPack` preserves an explicitly
supplied `PCCMinLoopCertificate`; `CheckPCCPackexp` checks the canonical package
identifier; compiled theorems prove generated-package acceptance, exact
certificate reflection, and mismatched-identifier rejection. The compiled
project-specific axiom inventory is therefore empty. This does not construct
the loop certificate, prove its semantic adequacy or polynomial runtime, close
ZeroSlack/PCCMin, establish deterministic SAT in P, or close any global gate.

M189 adds a general `PCCMinTotalOracle` control-flow
kernel. Every gain response carries a strict equivalent-gain proof and strict
residual-slack descent; terminal responses carry exact-minimum or ZeroSlack
evidence. Transparent well-founded recursion preserves semantics, returns an
exact minimum with zero slack, and bounds strict-gain iterations by the starting
slack. M189 does not construct or supply the oracle, and its regression fixture
uses exhaustive reference minimization rather than a polynomial algorithm. It
therefore does not earn a fixed proof-completion checkpoint or close a global
gate. See
[`lean_pccmin_total_oracle_loop.md`](./docs/lean_pccmin_total_oracle_loop.md).

M190 composes the manuscript's two proof-bearing PCCMin
stages. `PCCMinTotalNormalizer` returns either a strict equivalent gain or a
semantically equivalent normal form whose gate count cannot increase. A
subsequent oracle gain is therefore a strict gain from the pre-normalized
implementation, while exact-minimum and ZeroSlack endpoints transport back
through normalization. The composition reuses the checked M189 recursion and
retains its exactness and gain-iteration bound. M190 does not construct or
supply the normalizer or oracle, and its regression fixtures use exhaustive
reference minimization rather than polynomial algorithms. It therefore does
not earn a fixed proof-completion checkpoint or close a global gate. See
[`lean_pccmin_normalize_oracle_composition.md`](./docs/lean_pccmin_normalize_oracle_composition.md).

M191 opens the opaque M190 oracle into the manuscript's
rank-ordered stages. `PCCMinRankOrderedOracleBuilder` runs proof-bearing
HResolve first, BudgetResolve only after NoHereditary, and then every selector
in every row of the canonical finite rank list. A selector gain returns
immediately; ZeroSlack can be returned only after a typed blocker equation has
been retained for every selector in every rank row. The resulting oracle
reuses the M190 normalization composition and M189 well-founded exact loop.
M191 does not construct the resolver algorithms, selector rows, realizer,
blocker semantics, or ZeroSlack closure and proves no polynomial runtime. It
therefore does not earn a fixed proof-completion checkpoint or close a global
gate. See
[`lean_pccmin_rank_ordered_oracle.md`](./docs/lean_pccmin_rank_ordered_oracle.md).

M192 replaces M191's arbitrary selector rows and
proof-bearing selector realizer with checked data-only Packet tables. Every
canonical handle claim must pass as a valid unit-charge gain or typed HN,
budget, or lower-seed blocker. Each selector row is then derived by filtering
the exhaustive handle list with the table-owned rank map, so arbitrary caller
rows cannot omit a handle. The resulting checked plan reuses M191's complete
rank scan and the existing exact loop. M192 still supplies the grouped family,
rank map, claims, resolver algorithms, blocker semantics, and final ZeroSlack
closure, and proves no polynomial construction. It therefore earns no fixed
proof-completion point and closes no global gate. See
[`lean_pccmin_checked_packet_ranked_selector.md`](./docs/lean_pccmin_checked_packet_ranked_selector.md).

M193 removes M192's opaque
silence-to-ZeroSlack callback.
Complete checked rank-row silence is reflected into the executable selector
silence checker, and checked HB no-outcome closure proves that every canonical
handle is nonfaithful. An explicit positive-residual-slack-to-faithful-selector
premise then yields a contradiction and conditional ZeroSlack before the
existing exact loop runs. That positive-slack bridge, terminal data, resolver
algorithms, normalizer, and polynomial construction remain supplied or open.
M193 therefore earns no fixed proof-completion point and closes no global gate.
See
[`lean_pccmin_checked_packet_hb_zeroslack_bridge.md`](./docs/lean_pccmin_checked_packet_hb_zeroslack_bridge.md).

The current M194 BN6 computed-faithfulness milestone removes M193's arbitrary
positive-slack-to-faithful-selector callback. Positive residual slack now
supplies only the earlier constant-activation boundary; the general BN6 theorem
constructs a Packet, and the route-clear payload checker constructs a faithful
selector in the canonicalized table before the checked HB contradiction yields
conditional ZeroSlack. Constant activation, terminal data, route-clear payload
evidence, resolver algorithms, normalizer, and polynomial bounds remain
supplied or open. M194 therefore earns no fixed proof-completion point and
closes no global gate. See
[`lean_pccmin_checked_packet_bn6_hb_zeroslack_bridge.md`](./docs/lean_pccmin_checked_packet_bn6_hb_zeroslack_bridge.md).

M195's BCEL activation-route milestone removes M194's opaque
positive-slack-to-constant-activation callback from the new endpoint. A total
classifier binds the supplied Packet carrier to the same-candidate checked
BCEL nucleus and compares its cut value and every canonical nonempty proper-cut
activation weight. A mismatch remains an exact typed route; coherence derives
constant activation, identifies it with projection excess, and reuses M194 for
conditional ZeroSlack. The terminal problem, positive premise, checked ready
certificate, family, tables, ranks, route-clear and HB data, resolvers,
normalizer, and polynomial bounds remain supplied or open. M195 therefore earns
no fixed proof-completion point and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_activation_route.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_activation_route.md).

M196's BCEL-derived-family milestone removes M195's independently
supplied Packet carrier and cut value. A grouped-cell ledger now constructs its
BN6 family carrier and positive cut value directly from the same checked finite
BCEL nucleus. The carrier and cut-value mismatch branches are impossible by
construction; the new endpoint preserves only an exact proper-cut activation
mismatch or M195's conditional ZeroSlack result. The grouped cells, payloads,
tables, ranks, route-clear and HB data, resolvers, normalizer, and polynomial
bounds remain supplied or open. M196 therefore earns no fixed proof-completion
point and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_derived_family.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_derived_family.md).

The M197 canonical-grouping milestone removes M196's supplied V54
consumer systems, singletonization certificates, exact group-footprint-size
proofs, and duplicate-free grouping proof. Each supplied raw support is
normalized inside the checked BCEL carrier, its singleton consumer system is
constructed, duplicate footprints are coalesced, and every positive payload
atom is preserved before the resulting canonical grouped family reuses M196.
The raw supports and payload atoms, tables, ranks, route-clear and HB data,
resolvers, normalizer, and polynomial bounds remain supplied or open. M197
therefore earns no fixed proof-completion point and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_canonical_grouping.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_canonical_grouping.md).

The M198 canonical cut-ledger milestone proves that this duplicate-
footprint coalescing preserves the direct raw positive-cell crossing-mass sum
for every finite cut. The checked endpoint therefore states the remaining
proper-cut activation mismatch directly against the raw positive-cell cut
ledger rather than an opaque grouped-family total. The raw cells and payloads,
terminal construction,
constant-activation equation, tables, ranks, route-clear and HB data,
resolvers, normalizer, and polynomial bounds remain supplied or open. The
mismatch is not yet a strict gain or globally decreasing route. M198 therefore
earns no fixed proof-completion point and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_canonical_cut_ledger.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_canonical_cut_ledger.md).

The M199 canonical constant-cut-basis milestone replaces the new
endpoint's inherited all-proper-cut powerset scan with an exact sparse basis.
For every V53 carrier of size at least two, Lean proves that the two-anchor
full weight, the three singleton cuts, or the four-plus full-span support and
weight conditions are equivalent to the complete proper-cut constant
equation. A total classifier either supplies that equation to the existing
checked Packet/BN6/BCEL/HB conditional ZeroSlack route or returns one typed
structural obstruction. The raw cells and payloads, terminal construction,
tables, ranks, route-clear and HB data, resolvers, normalizer, and complete
encoded-size polynomial bounds remain supplied or open; a rejected basis is
not a strict gain or globally decreasing route. M199 therefore earns no fixed
proof-completion point and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_canonical_constant_cut_basis.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_canonical_constant_cut_basis.md).

The current M200 sparse activation-route milestone proves that singleton and
order-preserving pair cuts form a complete test family for the V53 proper-cut
constant equation on every finite carrier of size at least two. The family is
duplicate-free and has a direct quadratic length bound. Its total classifier
returns either the complete equation or the first exact small proper-cut
mismatch, which the checked PCCMin adapter reflects through M198's direct raw
positive-cell ledger; the coherent branch retains M199's conditional
ZeroSlack result. Raw cells and payloads, terminal construction, tables,
ranks, route-clear and HB data, resolvers, normalizer, and complete
encoded-input polynomial bounds remain supplied or open. A mismatch is not a
strict gain or globally decreasing route, and the local list-size bound is not
a complete PCCMin runtime theorem. M200 earns no fixed proof-completion point
and closes no global gate. See
[`lean_pccmin_checked_packet_bn6_bcel_sparse_activation_route.md`](./docs/lean_pccmin_checked_packet_bn6_bcel_sparse_activation_route.md).

M201 reconstructs the next manuscript PkgC-to-BN6 edge for arbitrary finite
supplied active V54 consumer systems over one carrier. A total classifier
returns the first exact same-key PkgC cancellation realization, or proves every
system singletonized and derives each raw BN6 positive-cell support and its
size-at-least-two fact from the active consumer data. The resulting list
preserves payload order and exactly conserves activation weight on every cut.
The terminal problem, source systems, active cuts, payloads, typed restorer,
and upstream BN3--BN5 construction remain supplied; a cancellation is not yet
a global gain or rank-decreasing route, and no complete polynomial
construction is proved. M201 therefore leaves the risk-weighted estimate at
35 percent and closes no global gate. See
[`lean_residual_terminal_pkgc_bn6_positive_cellization.md`](./docs/lean_residual_terminal_pkgc_bn6_positive_cellization.md).

M202 composes that source-derived ledger directly with the checked sparse
BN6/BCEL/Packet/HB route. The total classifier retains the exact source-member
PkgC cancellation, returns genuine conditional ZeroSlack on the coherent
checked branch, or reflects one nonempty proper singleton/pair activation
mismatch back to the original PkgC source ledger. No independent raw BN6 cell
list or mismatch is accepted. The source systems, active cuts, payloads,
typed restorer, checked downstream tables, ranks, dependency closure,
route-clear result, and selector silence remain supplied; cancellation or
mismatch is not yet a global gain or rank-decreasing transition. M202 leaves
the risk-weighted estimate at 35 percent and closes no global gate. See
[`lean_pccmin_checked_packet_pkgc_bn6_bcel_source_route.md`](./docs/lean_pccmin_checked_packet_pkgc_bn6_bcel_source_route.md).

M203 removes the supplied-remainder and supplied-permutation seam from M202's
PkgC cancellation branch. Constructive remove-first recursion computes an
exact multiplicity-preserving remainder from any admissible ambient BN4 order
and derives the complete residual-ledger reduction, or proves that no exact
remainder embedding exists. The ambient BN4 kernel is constructed against the
same checked BCEL nucleus, while M202's conditional ZeroSlack and source
activation mismatch branches are preserved. The ambient ledger, source data,
restorer, downstream tables, and selector silence remain supplied; the
remainder is not proved empty and neither an extraction nor a mismatch is a
global gain or rank decrease. M203 leaves the risk-weighted estimate at 35
percent and closes no global gate. See
[`lean_pccmin_checked_packet_pkgc_ambient_bn4_extraction_route.md`](./docs/lean_pccmin_checked_packet_pkgc_ambient_bn4_extraction_route.md).

M204 replaces the always-total supplied typed-restorer branch at this local
PkgC boundary with the existing finite exact-coordinate restoration
classifier. Its failure branch retains the exact Hall deficit and forced Q
route. Complete coverage constructs a balanced opposite-sign BN4 unit pair
for every canonical quotient unit, then arbitrary-order extraction computes
the ambient remainder and exact residual-ledger reduction or proves no exact
embedding. The restoration universe, coordinate maps, consumer system and
ambient ledger remain supplied; coordinate coverage does not materialize
semantic full candidates, the Hall route is not a global gain, and the
remainder is not proved empty. M204 leaves the risk-weighted estimate at 35
percent and closes no global gate. See
[lean_residual_terminal_pkgc_restoration_coverage_ambient_route.md](./docs/lean_residual_terminal_pkgc_restoration_coverage_ambient_route.md).

M205 lifts M204 across the complete arbitrary-finite active PkgC source
ledger. The list-order classifier constructs M201's BN6 positive cells only
when every source system singletonizes, preserving ledger length, payload
order, and activation weight on every cut; otherwise it retains the first
source-member Hall deficit, exact ambient reduction, or no-embedding witness.
The source cells and cuts, payloads, per-source restoration universes and
ambient ledgers remain supplied. No returned obstruction is yet a global gain
or rank decrease, and the computed remainder is not proved empty. Formal
artefact coverage becomes 181 of 183, while the risk-weighted estimate remains
35 percent and zero of five global gates are closed. See
[lean_residual_terminal_pkgc_restoration_coverage_bn6_ledger.md](./docs/lean_residual_terminal_pkgc_restoration_coverage_bn6_ledger.md).

M206 connects that complete typed-restorer-free ledger to the checked
BN6/BCEL route for the same computed candidate nucleus. The exact Hall,
ambient-reduction, and no-embedding branches remain visible; only the proved
all-singletonized branch enters the downstream Packet/HB classifier, yielding
conditional ZeroSlack or one small-cut activation mismatch reflected to the
original enriched source ledger. The source, restoration, ambient, rank, claim,
dependency, and selector data remain supplied, and no returned obstruction is
yet a verified global descent. Formal artefact coverage becomes 182 of 184,
while the risk-weighted estimate remains 35 percent and zero of five global
gates are closed. See
[lean_pccmin_checked_packet_pkgc_restoration_coverage_bn6_bcel_route.md](./docs/lean_pccmin_checked_packet_pkgc_restoration_coverage_bn6_bcel_route.md).

M207 upgrades M206's successful ambient-extraction branch to a computed formal
rank transition. Unsigned BN4 charge is the sum of cell masses, the exact
permutation embedding decomposes ambient charge into strictly positive removed
charge plus remainder charge, and the remainder therefore decreases the exact
ten-coordinate residual rank at its `chargeSize` coordinate for every fixed
surrounding context. Hall, incompatibility, and activation-mismatch branches
remain diagnostics, and the terminal and downstream tables remain supplied.
Formal artefact coverage becomes 183 of 185, while the risk-weighted estimate
remains 35 percent and zero of five global gates are closed. See
[lean_pccmin_checked_packet_pkgc_restoration_coverage_charge_descent.md](./docs/lean_pccmin_checked_packet_pkgc_restoration_coverage_charge_descent.md).

M208 replaces continued fixed-slot Cook-Levin control extension with one
all-input full-schedule controller. Lean computes the complete polynomial body
opportunity count, proves that a semantic cursor returns the entire canonical
token schedule, and runs a deterministic finite raw countdown controller to
the exact terminal coordinate within an explicit polynomial bound. The raw
loop does not yet decode or emit each visited body entry, so the complete
builder and packaged reduction remain open. Formal artefact coverage becomes
184 of 186, while the risk-weighted estimate remains 35 percent and zero of
five global gates are closed. See
[lean_cook_levin_builder_full_schedule_cursor_controller.md](./docs/lean_cook_levin_builder_full_schedule_cursor_controller.md).

M209 adds the first non-repeatable raw decoder layer. One fixed 54-rule
machine compares every arbitrary coordinate with the exact problem-derived
header boundary, agrees with the top-level direct-slot route, and has exact
accept/reject, polynomial compiled-time, one-step-short, and malformed-input
theorems. It does not decode the post-header clause/slot quotient or emit a
body token, so the complete builder and packaged reduction remain open.
Formal artefact coverage becomes 185 of 187, while the risk-weighted estimate
remains 35 percent and zero of five global gates are closed. See
[lean_cook_levin_builder_arbitrary_slot_header_router.md](./docs/lean_cook_levin_builder_arbitrary_slot_header_router.md).

M210 refines every post-header coordinate without returning to fixed schedule
slots. A structurally recursive decoder computes the exact finite clause and
within-clause coordinate, isolates the unique `Finish`, reconstructs every
body coordinate, and agrees with direct token lookup. A checked reader also
recovers the exact shifted remainder from M209's raw result configuration. It
does not implement raw division or raw body-token emission, so the complete
raw builder and packaged reduction remain open. Formal artefact coverage
becomes 186 of 188, while the risk-weighted estimate remains 35 percent and
zero of five global gates are closed. See
[lean_cook_levin_builder_arbitrary_slot_post_header_decoder.md](./docs/lean_cook_levin_builder_arbitrary_slot_post_header_decoder.md).

M211 implements the standalone raw arithmetic kernel that M210 left open. One
fixed 99-rule machine computes exact natural quotient and strict remainder for
every unary dividend and positive unary width, compiles at exactly six raw
transitions per work step, times out one step short, and satisfies a quadratic
bound in the complete unary input length. It is not yet spliced onto M209's raw
result and emits no body token, so the complete builder and packaged reduction
remain open. Formal artefact coverage becomes 187 of 189, while the
risk-weighted estimate remains 35 percent and zero of five global gates are
closed. See
[lean_cook_levin_builder_post_header_raw_divider.md](./docs/lean_cook_levin_builder_post_header_raw_divider.md).

M212 joins M209's checked raw remainder, M210's all-coordinate semantic route
classifier, and M211's exact fixed divider through executable Lean
orchestration. Every natural coordinate derives its launch without a supplied
route witness; both exact traces are retained; body, `Finish`, and out-of-range
results are recovered; and one source-size polynomial bounds their combined
compiled work. This is not yet a literal raw tape-to-tape rewrite, preserves no
complete builder workspace through such a bridge, and emits no body or
`Finish` token, so the complete builder and packaged reduction remain open.
Formal artefact coverage becomes 188 of 190, while the risk-weighted estimate
remains 35 percent and zero of five global gates are closed. See
[lean_cook_levin_builder_post_header_raw_launch.md](./docs/lean_cook_levin_builder_post_header_raw_launch.md).

M213 replaces that value-level launch with a fixed 351-rule literal tape
bridge. Both exact M209 post-header terminal layouts enter distinct branches,
which copy zero or the exact positive shifted remainder plus the positive
problem-derived width into a shielded M211 input. Exact bridge and divider
traces preserve arbitrary exterior workspace, decode the final quotient and
remainder, compile at six raw steps per work step, time out one step short, and
fit one verifier-derived source-size polynomial for every in-range coordinate.
The bridge emits no body or `Finish` token and does not iterate the schedule,
so the complete builder and packaged reduction remain open. Formal artefact
coverage becomes 189 of 191, while the risk-weighted estimate remains 35
percent and zero of five global gates are closed. See
[lean_cook_levin_builder_post_header_raw_tape_bridge.md](./docs/lean_cook_levin_builder_post_header_raw_tape_bridge.md).

M214 continues from that exact divider tape with one fixed 180-rule literal
post-divider classifier. It copies the problem-derived clause count from a
restored workspace sidecar, preserves the full exterior and remainder ledger,
and exposes the quotient to a shielded M209 comparator. Exact bridge and
comparator traces compile at six raw steps per work step, time out one step
short, agree with every in-range M210 body or unique `Finish` route, and fit
one verifier-derived source-size polynomial. It still does not inspect, select,
emit, or append the corresponding token or iterate the complete schedule, so
the complete raw builder, `RawRefinement`, and packaged reduction remain
open. Formal artefact coverage is 190 of 192, the risk-weighted estimate
remains 35 percent, and zero of five global gates are closed. See
[lean_cook_levin_builder_post_divider_raw_route_classifier.md](./docs/lean_cook_levin_builder_post_divider_raw_route_classifier.md).

M215 derives every post-header padding, body-token, or unique `Finish` entry
from the canonical schedule without a supplied route or token. It retains
M214's physical classifier contract for arbitrary workspace, leaves the
canonical emitted prefix unchanged at padding, and runs the existing fixed
59-rule appender for every populated coordinate to reach exactly the next
emitted prefix. Exact work and six-for-one compiled traces, one-step-short
nonhalting, and the combined staged source-size polynomial are all checked.
The selection handoff is still executable Lean orchestration rather than one
literal raw selector, and the schedule is not iterated, so the complete raw
builder, `RawRefinement`, and packaged reduction remain open. Formal artefact
coverage is 191 of 193, the risk-weighted estimate remains 35 percent, and
zero of five global gates are closed. See
[lean_cook_levin_builder_post_divider_selected_token_launch.md](./docs/lean_cook_levin_builder_post_divider_selected_token_launch.md).

M216 recursively consumes every verifier-derived post-header schedule
opportunity without a supplied coordinate, token, route, trace, schedule, or
precomputed formula. Every bounded run equals the canonical emitted prefix and
the complete run equals the exact `encodeCNFTokens problem.formula` stream.
Every coordinate retains M215's physical classifier/appender evidence for
arbitrary workspace, while a recursive accumulator bounds the aggregate staged
compiled work by one source-size polynomial. The recursion is executable Lean
orchestration over separately checked stages, not one literal raw-machine loop
or a physical tape-to-tape inter-stage handoff, so complete builder
`RawRefinement` and the packaged reduction remain open. Formal artefact
coverage is 192 of 194, the risk-weighted estimate remains 35 percent, and zero
of five global gates are closed. See
[lean_cook_levin_builder_complete_schedule_iteration.md](./docs/lean_cook_levin_builder_complete_schedule_iteration.md).

M217 replaces M215's value-level selector-to-appender handoff with one
fixed 64-rule physical work machine for the complete five-symbol request
alphabet. A tape-resident padding or CNF-token request restores the canonical
builder left boundary and either preserves the emitted prefix or enters the
existing renamed 59-rule appender. Exact work and six-for-one compiled traces
cover arbitrary input, exterior workspace and output prefix; malformed blank
requests and one-step-short fuel remain nonhalting. Every canonical
post-header coordinate derives its request from `scheduleEntry`, and the
stage fits one verifier-derived source-size polynomial. The request cell is
still an input to this stage: it is not produced from M214's raw classifier,
connected through a literal classifier-to-dispatcher tape handoff, or iterated
as one physical schedule loop, so complete builder `RawRefinement` and the
packaged reduction remain open. Formal artefact coverage is 193 of 195 current
scoped publication rows earned; the risk-weighted estimate remains 35 percent,
and zero of five global gates are closed. See
[lean_cook_levin_builder_physical_optional_token_dispatch.md](./docs/lean_cook_levin_builder_physical_optional_token_dispatch.md).

M218 composes M217's fixed physical optional-token dispatcher across every
canonical post-header coordinate. The recursive output agrees with the canonical
emitted prefix at every bounded coordinate and reaches the exact
`encodeCNFTokens problem.formula` stream at the complete schedule. Every step
retains exact work, compiled, and one-step-short physical traces for arbitrary
input and exterior workspace, carries the M214 physical classifier evidence, and
the aggregate compiled work fits one verifier-derived source-size polynomial.
Each request is still constructed in Lean from the canonical schedule rather
than produced by the raw classifier on tape, and successive configurations are
not connected by one literal raw-machine loop. Complete builder
`RawRefinement` and the packaged reduction therefore remain open. Formal
artefact coverage is 194 of 196 current scoped publication rows earned; the
risk-weighted estimate remains 35 percent, and zero of five global gates are
closed. See
[lean_cook_levin_builder_physical_dispatch_schedule.md](./docs/lean_cook_levin_builder_physical_dispatch_schedule.md).

M219 closes the first literal classifier-to-request branch for the unique
canonical `Finish` coordinate. The problem derives that coordinate internally,
M214 proves its equal-classifier result, and the canonical builder workspace is
preserved as a protected suffix beyond the comparator's end marker. One fixed
writer produces M217's tape-resident `Finish` request, and one collision-free
137-rule composition runs the classifier, writer, and dispatcher. Exact work,
six-for-one compiled, and one-step-short traces hold for arbitrary classifier
exterior, and one verifier-derived polynomial bounds the compiled work. Body-token
and padding request derivation, the preceding suffix-preserving classifier handoff,
one literal repeated loop, complete builder `RawRefinement`, and the packaged
reduction remain open. Formal artefact coverage is 195 of 197 current scoped
publication rows earned; the risk-weighted estimate remains 35 percent, and zero
of five global gates are closed.
See
[lean_cook_levin_builder_physical_finish_request.md](./docs/lean_cook_levin_builder_physical_finish_request.md).

M220 completes the suffix-preserving physical classifier pipeline for every
canonical post-header coordinate. One fixed collision-free 711-rule machine
composes M213's router-to-divider bridge, M211's exact divider, M214's
divider-to-comparator bridge and comparator through three exact tape handoffs
over arbitrary protected builder workspace. The final raw state agrees with
M214's typed body-or-`Finish` semantics, while exact work, six-for-one compiled
execution, one-step-short nonhalting, component decomposition, and one
source-size polynomial bound are checked. Body-token and padding request symbols
are not yet written and the classifier result is not connected to M217's
dispatcher; one literal repeated schedule loop, complete builder `RawRefinement`,
and the packaged reduction remain open. Formal artefact coverage is 196 of 198
current scoped publication rows earned; the risk-weighted estimate remains 35
percent, and zero of five global gates are closed. See
[lean_cook_levin_builder_physical_classifier_pipeline.md](./docs/lean_cook_levin_builder_physical_classifier_pipeline.md).

M221 closes the full-classifier-to-`Finish`-request-cell edge. It derives the
unique canonical `Finish` coordinate, reinterprets M220's terminal verdict names
without changing classifier transition semantics, and chains the complete
classifier to M219's one-cell writer as one collision-free 721-rule machine.
For arbitrary protected workspace, the final tape is exactly M220's terminal
tape with the focused end marker replaced by M217's `Finish` request; exact work,
six-for-one compiled execution, one-step-short nonhalting, and a source-size
polynomial bound remain checked. Body-token and padding request generation,
dispatcher-ready workspace orientation and connection, one literal repeated
loop, builder `RawRefinement`, and the packaged reduction remain open. Formal
artefact coverage is 197 of 199 current scoped publication rows earned; the
risk-weighted estimate remains 35 percent, and zero of five global gates are
closed. See
[lean_cook_levin_builder_physical_classifier_finish_request.md](./docs/lean_cook_levin_builder_physical_classifier_finish_request.md).

M222 closes the unique full-classifier `Finish` workspace-orientation edge.
It inserts one blank sentinel before the canonical builder workspace, derives
the complete M220/M221 classifier prefix, proves that prefix is blank-free, and
chains M221 to a fixed ten-rule left scanner. The resulting collision-free
740-rule machine crosses exactly the derived prefix and halts with a tape equal
to the spatial mirror of M217's canonical `Finish` request entry, while
retaining the classifier evidence as exterior data. Exact work, six-for-one
compiled execution, one-step-short nonhalting, a prefix-size bound, and one
source-size polynomial bound are checked. M217 is not yet executed on the
mirrored representation; body-token and padding request generation, all-route
dispatcher connection, one literal repeated loop, builder `RawRefinement`, and
the packaged reduction remain open. Formal artefact coverage is 198 of 200
current scoped publication rows earned; the risk-weighted estimate remains 35
percent, and zero of five global gates are closed. See
[lean_cook_levin_builder_physical_classifier_finish_workspace_orientation.md](./docs/lean_cook_levin_builder_physical_classifier_finish_workspace_orientation.md).

M223 closes reflected dispatcher execution for the unique full-classifier
`Finish` path. It defines spatial reflection for arbitrary literal work
machines, proves that rule lookup, steps, and exact runs commute with reflection,
and specializes that result to M217's fixed 64-rule dispatcher. Chained after
M222, the resulting collision-free 813-rule machine runs from the complete
classifier entry to a reflected appender endpoint containing the complete
canonical CNF token stream. Exact work, six-for-one compiled execution,
one-step-short nonhalting, and one source-size polynomial bound are checked.
Body-token and padding request generation, all-route dispatcher connection, one
literal repeated loop, builder `RawRefinement`, and the packaged reduction
remain open. Formal artefact coverage is 199 of 201 current scoped publication
rows earned; the risk-weighted estimate remains 35 percent, and zero of five
global gates are closed. See
[lean_cook_levin_builder_physical_classifier_finish_mirrored_dispatch.md](./docs/lean_cook_levin_builder_physical_classifier_finish_mirrored_dispatch.md).

M224 closes the first populated route through the complete physical classifier
and reflected dispatcher. It derives post-header coordinate zero and proves its
canonical request is the separator beginning the first clause, runs M220's
711-rule classifier through the body terminal, crosses the remaining positive
unary clause count with a fixed two-rule request writer, and reorients the
protected workspace with a fixed ten-rule scanner. M223's reflected 64-rule
dispatcher then reaches the exact canonical prefix ending in the first clause
separator. The collision-free 814-rule composition has exact work,
six-for-one compiled, one-step-short, and source-size polynomial evidence.
Arbitrary body-token and padding requests, all-route dispatcher connection, one
literal repeated loop, builder `RawRefinement`, and the packaged reduction
remain open. Formal artefact coverage is 200 of 202 current scoped publication
rows earned; the risk-weighted estimate remains 35 percent, and zero of five
global gates are closed. See
[lean_cook_levin_builder_physical_classifier_first_body_separator_mirrored_dispatch.md](./docs/lean_cook_levin_builder_physical_classifier_first_body_separator_mirrored_dispatch.md).

## Current claim boundary

The project targets:

```text
P = NP
```

The target is not currently established. Legacy checker records preserve the earlier conditional
assertion and its replay history, but neither those records nor their hashes are active theorem
authority. Future public theorem emission requires the concrete, assumption-audited Lean gate in
[the reconstruction notice](./docs/FORMAL_RECONSTRUCTION.md).

The completed concrete CNF layer is a verifier result: a finite raw machine decides whether a
supplied bounded assignment certificate satisfies a canonically encoded CNF formula. Universal
machine correctness and the explicit polynomial runtime bound prove `CNFSAT ∈ NP`. Existentially
guessing a certificate is not a deterministic polynomial-time SAT algorithm, so this result does
not prove `CNFSAT ∈ P`, NP-hardness, NP-completeness, or `P = NP`.

The framed simulator is a local configuration theorem. If `rawRunExact?` supplies `n` successful
raw transitions and the starting work tape satisfies `PipelineTape.Represents`, Lean constructs
exactly `3 * n` successful work steps to a represented endpoint. For an ordinary raw `run` with
fuel `F`, Lean extracts an exact successful prefix of some length `k ≤ F` reaching the same
endpoint. If that endpoint is a designated accept or reject state, `workRun` with fuel
`3 * F` and compiled raw `run` with fuel `18 * F` reach its representation and encoding. This is
conditional padding, not a termination theorem: `3 * F` and `18 * F` are at-most budgets, not
successful-transition counts, and a stuck nonhalting endpoint is not a verdict. It does not connect
canonical `Tape.ofInput` to the frame inside the simulator theorem or prove target termination. The
separate bridge module composes every supplied exact run with the paired-input framer and a
verdict-indexed internal handoff, preserving accept/reject and reporting a supplied stuck
nonhalting endpoint as timeout at the exact prefix budget. Its cost still depends on the supplied
source-transition count and final output length, and the two-track blank tags are not a canonical
`machineOutput` encoding or an external-input-size polynomial.

The input framer is a separate executable machine. Its all-input theorem starts from ordinary
`startConfig (compileWorkMachine pairedInputFramer) input`, handles empty input, complete two-bit
work cells, and an odd final raw bit through literal transitions, and reaches a represented frame
with permitted exterior garbage. Its exact work costs are `4` for empty input,
`4 * k * k + 9 * k + 7` for `k` complete cells, and `4 * k * k + 9 * k + 5` when the final cell is
partial. The compiled run accepts without timeout within `6 * m * m + 39 * m + 75` for raw length
`m`. The earlier canonical-pair theorem retains its sharper exact bound `6 * m * m + 27 * m + 42`.
`PipelineCompiler` now consumes this endpoint. For every raw bitstring it composes the same literal
framer, simulator, represented-output handoff, and terminal packer table used by the paired
compiler. From the target's proof-bearing termination theorem it extracts an exact target prefix,
preserves accept/reject/timeout classification and ordinary `machineOutput`, and proves no timeout
at an explicit external-input-size polynomial. Thus empty, odd, even, and other non-pair words are
covered by the complete raw pipeline, not merely by the local framer.

The internal output handoff is another separate executable machine. From an already represented
logical tape `raw`, it retains the prefix `raw.outputBits`, installs a fresh represented frame for
`raw.handoffTarget`, and halts accepting after exactly `2 * raw.outputBits.length + 4` work steps.
The compiled theorem uses exactly `12 * raw.outputBits.length + 24` steps, but starts from
`encodeWorkConfiguration` of the internal represented configuration. The bridge module launches
simulator accept and reject sentinels into two disjoint copies and compiles that cumulative trace
from ordinary paired `startConfig`. The new terminal bridge extends the finite rule table with two
further disjoint packer copies and, from either represented handoff endpoint, proves one exact
launch, exact terminal packing, distinct terminal verdicts, and raw-visible output equality under
the local bound `18*n^2 + 36*n + 12`. It also preserves every successful earlier bridge trace in
`terminalBridgeMachine` and composes a complete four-stage trace for each caller-supplied exact
accepting or rejecting target execution. `PipelinePairedCompiler` obtains target termination on
canonical pairs. Its successor `PipelineCompiler` proves the corresponding result on every raw
bitstring with output bound `B(m) = m + p(m) + 1` and complete runtime bound
`R(m) = totalInputFramerRawTimeBound(m) + 6 + 18*p(m) + 6 +
framedOutputHandoffRawTimeBound(B(m)) + terminalBridgeRawTimeBound(B(m))`. This wraps one
already-raw `PolynomialTimeMachine`; it does not yet compile recursive charged function/decision
program composition into the `RawRefinement` contracts.

The next composition layer now nests two such component machines in pairwise-disjoint outer state
images. `PipelineSequentialStateNamespace` proves injective renaming, first-match lookup isolation,
exact local-trace transport, and literal accept/reject launches from the first component into the
second component's internal simulator start. This is the namespace and dispatch milestone only:
an end-to-end two-machine trace, terminal output theorem, external polynomial, and recursive
`RawRefinement` constructors remain to be proved.

## Quick start for reviewers

Requirements: Node.js 20 or newer and npm 10 or newer.

```bash
git clone https://github.com/aisknab/pnp.git
cd pnp
npm ci --ignore-scripts
npm run pnp:verify -- --no-write
```

The verifier must keep every theorem-status flag false while checking the current status/surface and
the byte-exact archive coordinates. Success does not validate the general mathematics.

Run the current-tree validation suite with:

```bash
npm run validate
```

For the frozen 7072f8d release, use the designated command in
[`REPRODUCE.md`](./REPRODUCE.md). Current `main` intentionally runs a small authority-and-archive
suite; it must not be confused with the frozen 1,121-test source release.

## What each verification layer means

| Layer | Command or artefact | What success establishes | What success does not establish |
| --- | --- | --- | --- |
| Current test suite | `npm test` | The formal status, package boundary, archive pins, and replay guards pass in the selected environment. | Legacy checker validation, exhaustive correctness, or polynomial asymptotics. |
| Pinned Lean root | `lake build PNP` and `lake env lean -DwarningAsError=true lean-audit/PNPBridgeAxiomAudit.lean` | Lean 4.31.0 compiles the explicit `PNP` root; the non-theorem root-status data is assumption-free; the conditional bridge's dependencies are printed. The current compiled project-axiom inventory is empty. | A root theorem or a proof of `P = NP`; the explicit PCCMin loop certificate and SAT-hardness obligation remain outside the bridge theorem's construction. |
| Compiled theorem inventory | `node scripts/export-lean-theorem-inventory.mjs --check` | `Lean.Environment.constants` and `Lean.collectAxioms` reproduce the canonical, byte-identical status and public inventory mirrors from the compiled `PNP` environment. Declaration, theorem, module, and private-auxiliary counts come from that generated inventory rather than this prose. | Source-level proof review, a general compiler/refinement from charged pipelines to raw machines, or a publication-eligible theorem. |
| Concrete publication gate and report | `node scripts/generate-formal-publication.mjs --check` and `npm run report:check` | Status and the concise canonical report match the compiled inventory and the false, fail-closed gate. | `P = NP`, permission to emit a theorem, or validation of the historical claim report. |
| Concrete machine and cost kernel | `lean-audit/PNPConcreteBitStringAxiomAudit.lean`, `lean-audit/PNPConcreteMachineAxiomAudit.lean`, and `audits/lean-concrete-machine0.test.mjs` | Canonical bitstring codecs, natural-polynomial syntax, finite rule-list machine semantics, bounded execution, and proof-bearing deterministic runtime witnesses are axiom-free. | The general charged-pipeline interface's compiler/refinement to one raw machine, `CNFSAT ∈ P`, NP-completeness, or `P = NP`. |
| Blank-delimited output and pure handoff target | `lean-audit/PNPConcreteTapeHandoffAxiomAudit.lean` and `audits/lean-concrete-tape-handoff0.test.mjs` | Output stops at the first observable blank; canonical input round trips, explicit/implicit boundaries agree, head-movement round trips preserve output, and the pure canonical handoff target preserves output idempotently. | The target is not an executable normalization/handoff machine and supplies no boundary frame, state reset, composition compiler, runtime theorem, class equivalence, or `P = NP`. |
| Blank-materialization equivalence | `lean-audit/PNPConcreteTapeBlankEquivalenceAxiomAudit.lean` and `audits/lean-concrete-tape-blank-equivalence0.test.mjs` | Finite tapes with different materialized exterior blanks have identical raw execution and observable output; every empty, odd, or even raw input agrees with its packed work view. | This theorem is representation invariance. The input framer now consumes it, but it does not itself establish complete pipeline refinement, a class result, or `P = NP`. |
| Boundary-marked pipeline tape geometry | `lean-audit/PNPConcretePipelineTapeGeometryAxiomAudit.lean` and `audits/lean-concrete-pipeline-tape-geometry0.test.mjs` | Two-track data and distinct left/right markers represent every raw tape with arbitrary exterior garbage; writes, interior moves, and empty-side boundary expansions preserve the representation. | These are pure tape identities, not transition rules, a handoff machine, compiler, runtime proof, class equivalence, or `P = NP`. |
| Executable all-input framer | `lean-audit/PNPConcretePipelineInputFramerAxiomAudit.lean` and `audits/lean-concrete-pipeline-input-framer0.test.mjs` | One literal finite work machine handles every raw bitstring, including empty and odd inputs; reaches a represented boundary frame; proves exact branch costs, the uniform bound `6*m^2 + 39*m + 75`, ordinary-start acceptance/no-timeout, and 70 empty axiom closures. | This module itself ends at the frame. `PipelineCompiler` consumes the endpoint, but general charged-program refinement and class results remain outside the framer theorem. |
| Executable internal represented-output handoff | `lean-audit/PNPConcretePipelineOutputHandoffAxiomAudit.lean` and `audits/lean-concrete-pipeline-output-handoff0.test.mjs` | One literal finite work machine turns an already represented tape into a represented `Tape.handoffTarget`, preserves blank-delimited logical output, halts accepting, and has exact `2 * n + 4` work and `12 * n + 24` compiled costs. | Its theorem is an internal-stage result. The terminal and all-input compilers consume it; this module alone does not prove target termination, an external polynomial, or charged-program refinement. |
| Collision-free pipeline state namespace | `lean-audit/PNPConcretePipelineStateNamespaceAxiomAudit.lean` and `audits/lean-concrete-pipeline-state-namespace0.test.mjs` | Injective renaming preserves first-match lookup and execution; three disjoint stage images give a lookup-isolated rule namespace. | This module is the namespace prerequisite; launch execution is proved separately and terminal output/refinement remain missing. |
| Sequential pipeline component namespace | `lean-audit/PNPConcretePipelineSequentialStateNamespaceAxiomAudit.lean` and `audits/lean-concrete-pipeline-sequential-state-namespace0.test.mjs` | Two complete component rule tables occupy disjoint outer images; first-match dispatch and exact local traces are preserved, and both first-component verdicts launch the second simulator literally. All 26 audited declarations have empty axiom closure. | No end-to-end two-machine run, terminal output equality, external polynomial, or recursive `RawRefinement` is claimed. |
| Executable verdict-preserving stage bridges | `lean-audit/PNPConcretePipelineStageBridgesAxiomAudit.lean` and `audits/lean-concrete-pipeline-stage-bridges0.test.mjs` | One finite bridge-first work machine launches framer to simulator and simulator accept/reject to disjoint handoff copies, preserves ordinary stage dispatch and bounded verdicts, composes supplied exact target traces, and compiles from canonical paired raw input at exactly six times the cumulative work cost. | It assumes a supplied exact target run and ends in internal represented output. Later modules supply the terminal suffix, target termination, and external polynomial; this module itself does not prove charged-program refinement or a complexity-class result. |
| Executable terminal raw-output packer | `lean-audit/PNPConcreteTerminalOutputPackerAxiomAudit.lean` and `audits/lean-concrete-terminal-output-packer0.test.mjs` | One literal finite work machine uniformly packs every logical output word, including empty/odd/even edge cases and arbitrary exterior garbage, into ordinary blank-delimited raw output. It proves exact work and compiled traces, halt/output equality, local bound `18*n^2 + 36*n + 6`, and one-step-short timeout; all 69 public declarations have empty axiom closure. | It starts from an encoded internal configuration. Later modules supply launches, target termination, and an external polynomial; this local theorem does not itself provide charged-program refinement, a class result, or `P = NP`. |
| Executable handoff-to-terminal bridge | `lean-audit/PNPConcretePipelineTerminalBridgeAxiomAudit.lean` and `audits/lean-concrete-pipeline-terminal-bridge0.test.mjs` | One extended finite rule table contains both earlier bridge rules and two disjoint packer copies. It preserves all successful earlier bridge steps and, for each supplied exact accepting or rejecting target run, proves a complete trace from ordinary paired input, the exact verdict, six-for-one compiled execution, raw output equality, and the local suffix bound `18*n^2 + 36*n + 12`; all 59 public declarations have empty axiom closure. | The theorem requires a caller-supplied exact target execution. Downstream compiler/refinement modules supply target termination, every-input behavior, an external polynomial, and recursive charged-program refinement; CNF-SAT class results and `P = NP` remain absent. |
| All-input four-stage compiler | `lean-audit/PNPConcretePipelineCompilerAxiomAudit.lean`, `lean-regression/PNPConcretePipelineCompiler.lean`, and `audits/lean-concrete-pipeline-compiler0.test.mjs` | The same literal compiled table handles every raw bitstring for an already-raw `PolynomialTimeMachine`; target termination is extracted internally, verdict and ordinary output are exact, timeout is excluded at the explicit polynomial bound, stuck nonhalting prefixes remain timeout, and all 29 public declarations have empty axiom closure. | This module supplies one raw target wrapper. The recursive refinement layer consumes it, but neither module proves `CNFSAT ∈ P`, NP-completeness, or `P = NP`. |
| Local framed raw-machine simulation | `lean-audit/PNPConcretePipelineMachineSimulationAxiomAudit.lean` and `audits/lean-concrete-pipeline-machine-simulation0.test.mjs` | Ordered finite rules preserve raw first-match selection, omit terminal-source entries, shift markers across arbitrary exterior garbage, lift every supplied exact `n`-step successful raw execution to exactly `3 * n` successful work steps, and extract a `k ≤ F` exact prefix from an ordinary `F`-fuel raw run. If its endpoint is designated halting, `workRun` with fuel `3 * F` and compiled `run` with fuel `18 * F` reach the representation and encoding. | It starts from an already represented frame and does not prove that the raw run halts. The full budgets are not successful-step counts or input-size bounds; a stuck nonhalting endpoint is not a verdict. It supplies no frame creator, `boundedDecide` or output theorem, connection to the separate handoff machine, composition/precomposition refinement, input-size polynomial end-to-end bound, class equivalence, or `P = NP`. |
| Finite charged-pipeline P/NP interface | `lean-audit/PNPConcreteComplexityAxiomAudit.lean`, `lean-audit/PNPConcreteTargetAxiomAudit.lean`, and `audits/lean-concrete-complexity0.test.mjs` | Finite machine-leaf function/decision syntax, polynomial runtime/output/certificate bounds, canonical paired verification, P contained in NP, polynomial-reduction closure, the NP-complete-in-P implication, and the inactive concrete target are axiom-free. | The machine link is now compiled downstream, but `CNFSAT ∈ P`, concrete NP-hardness, a root theorem, gate activation, and `P = NP` remain absent. |
| Recursive raw charged-pipeline refinement | `lean-audit/PNPConcretePipelineRefinementAxiomAudit.lean`, `lean-regression/PNPConcretePipelineRefinementRecursive.lean`, and `audits/lean-concrete-pipeline-refinement0.test.mjs` | Exact machine leaves, literal two-machine function composition and decision precomposition, recursive compilation of every finite program tree, exact output/verdict preservation, and `PolynomialTimeDecider.compileToMachine` are axiom-free across all 16 public declarations. The recursive bound uses `PipelineRaw(p)(m) + 6 + PipelineRaw(q)(m + p(m) + 1)` at each composition node. | This closes `Formal.ConcreteComplexityMachineLink` only. It does not establish `CNFSAT ∈ P`, CNF-SAT NP-completeness, or `P = NP`; the publication gate remains false. |
| Direct concrete CNF verifier | `node --test audits/lean-concrete-cnf0.test.mjs` plus the four `PNPConcreteCNF*AxiomAudit.lean` transcripts | Canonical CNF and assignment codecs, bounded certificate semantics, exact paired-tape compilation, universal accept/reject/no-timeout correctness for a finite raw machine, an explicit polynomial runtime bound, and `PNP.Concrete.FinalUniversalDesign.cnfSATInNP : InNP CNFSAT` are axiom-free. | A deterministic polynomial-time CNF-SAT decider, `CNFSAT ∈ P`, NP-hardness, NP-completeness, or `P = NP`. |
| External Cook–Levin encoded-formula size | `lean-audit/PNPConcreteCookLevinFormulaSizeAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinFormulaSize.lean`, and `audits/lean-concrete-cook-levin-formula-size0.test.mjs` | Exact canonical codec lengths and all concrete tableau constraint families yield an explicit fixed-verifier polynomial bounding `BitString.size problem.encodedFormula` solely in `BitString.size problem.input`; a second mode-sensitive polynomial evaluates exactly to `problem.FormulaWidth`. Both verifier input modes are covered, and all 110 declarations reach only `propext` and `Quot.sound`. | A raw finite formula builder, a construction-runtime polynomial, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Rectangular Cook–Levin formula schedule | `lean-audit/PNPConcreteCookLevinFormulaScheduleAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinFormulaSchedule.lean`, and `audits/lean-concrete-cook-levin-formula-schedule0.test.mjs` | Exact answer-independent constraint, clause, token, and raw-bit rectangles reproduce the existing canonical program and encoding after empty slots are filtered. The raw-bit slot count equals `encodedFormulaSizePolynomial` at external input length; all 79 declarations reach only `propext` and `Quot.sound`. | Treating a slot as constant-time execution, a raw formula builder, a construction-runtime polynomial, `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Direct Cook–Levin formula cursor | `lean-audit/PNPConcreteCookLevinFormulaCursorAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinFormulaCursor.lean`, and `audits/lean-concrete-cook-levin-formula-cursor0.test.mjs` | Direct constraint, clause, token, and bit decoders preserve out-of-range versus valid-padding semantics and agree pointwise with the canonical schedules. Exact prefix, full, one-step-short, terminal, and excess-fuel bit-cursor theorems yield the canonical encoded output; a token-level cursor exposes exact in-range, done, and terminal step laws. All 136 declarations reach only `propext` and `Quot.sound`. | Constant-time raw interpretation, a raw finite formula builder, a construction-runtime polynomial, `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Literal Cook–Levin input-length tally | `lean-audit/PNPConcreteCookLevinBuilderInputLengthAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderInputLength.lean`, and `audits/lean-concrete-cook-levin-builder-input-length0.test.mjs` | One fixed 19-rule machine preserves every source bit, appends an exact unary length tally in fresh workspace, accepts in `2*n^2 + 4*n + 2` work steps, compiles to exactly `12*n^2 + 24*n + 12` raw steps, times out on malformed scan symbols and one-step-short fuel, and connects definitionally to the all-input framer endpoint. All 39 public declarations reach only `propext` and `Quot.sound`. | Formula-bit emission, raw cursor interpretation, a complete builder or `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Executable Cook–Levin builder input prefix | `lean-audit/PNPConcreteCookLevinBuilderInputPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderInputPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-input-prefix0.test.mjs` | One collision-free finite table composes the total raw-input framer, a total nine-symbol launch, and the fixed unary tally. Every `BitString` reaches the exact preserved-input tally endpoint within `18*n^2 + 63*n + 93` compiled steps; a scan configuration headed by the unused `zeroOne` symbol and one-step-short fuel time out. All 40 public declarations reach only `propext` and `Quot.sound`. | Formula-bit emission, raw cursor interpretation, a complete builder or construction-time `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Standalone Cook–Levin builder token appender | `lean-audit/PNPConcreteCookLevinBuilderTokenAppenderAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderTokenAppender.lean`, and `audits/lean-concrete-cook-levin-builder-token-appender0.test.mjs` | One fixed 59-rule state-selected machine appends any of the four canonical two-bit CNF tokens after the exact tally while preserving the source and arbitrary exterior garbage. Its start state appends the first `T` header token within `24*n + 48` compiled steps and proves those bits equal the first two direct/canonical Cook–Levin formula bits; malformed phases and one-step-short fuel time out. All 68 declarations reach only `propext` and `Quot.sound`. | This module audits the appender independently of its composed use. The remaining header, dynamic cursor interpreter, complete builder, construction-time `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Composed Cook–Levin first-token prefix | `lean-audit/PNPConcreteCookLevinBuilderFirstTokenPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFirstTokenPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-first-token-prefix0.test.mjs` | One literal 184-rule machine places all 116 input-prefix rules and all 59 appender rules in disjoint injective state images behind nine total bridge rules. Every raw `BitString` reaches the preserved input/tally workspace containing exactly `[T]`; its two bits equal `encodedFormula.take 2`, and the compiled run is bounded by `18*n^2 + 87*n + 147`. All 37 declarations reach only `propext` and `Quot.sound`. | Exactly two fixed formula bits are emitted. The rest of the width header, dynamic cursor, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Composed Cook–Levin complete width header | `lean-audit/PNPConcreteCookLevinBuilderUnaryPolynomialAxiomAudit.lean`, `lean-audit/PNPConcreteCookLevinBuilderCompleteHeaderAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderCompleteHeader.lean`, and `audits/lean-concrete-cook-levin-builder-complete-header0.test.mjs` | A literal unary evaluator compiles the verifier-fixed width polynomial without calling `NatPolynomial.eval` from executable rules. One finite table with `363 + evaluator.ruleCount` rules composes it with the raw-input/first-token prefix, five total bridges, a 16-rule controller, and two 59-rule appenders. Every raw input emits exactly `T` repeated `FormulaWidth` times followed by `F`; those bits equal the complete canonical width header, and the compiled trace is bounded by an external `NatPolynomial`. The evaluator's 74 and composition's 85 public declarations reach only `propext` and `Quot.sound`. | Dynamic raw cursor/body emission, a complete formula builder or builder `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Composed Cook–Levin body-start prefix | `lean-audit/PNPConcreteCookLevinBuilderBodyStartPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderBodyStartPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-body-start-prefix0.test.mjs` | One literal table with `440 + Unary.ruleCount(widthPolynomial) + Unary.ruleCount(nextTokenSlotPolynomial)` rules composes the complete header, a unary next-slot evaluator, two total nine-symbol bridges, and a 59-rule separator appender in disjoint state images. Every raw input emits exactly `T^FormulaWidth F Sep`, retains token coordinate `FormulaVariableSlotBound + 2` and its doubled bit cursor, and obeys the external compiled bound `CompleteHeader.rawTimeBound + 72 + 6*Unary.workSteps(nextTokenSlotPolynomial) + 24*n + 12*width`. All 60 public declarations reach only `propext` and `Quot.sound`. | The retained coordinate is data, not a dynamic cursor. Subsequent body emission, a complete builder or builder `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Composed Cook–Levin first-literal prefix | `lean-audit/PNPConcreteCookLevinBuilderFirstLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFirstLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-first-literal-prefix0.test.mjs` | One literal table with `585 + Unary.ruleCount(widthPolynomial) + Unary.ruleCount(bodyStartNextSlotPolynomial) + Unary.ruleCount(firstLiteralNextSlotPolynomial)` rules composes the complete body-start prefix, a unary next-slot evaluator, three total nine-symbol bridges, and fixed `T`/`F` appenders in four disjoint state images. Every raw input emits exactly `T^FormulaWidth F Sep T F`, constructively pins the final pair to positive variable zero, retains token coordinate `FormulaVariableSlotBound + 4` and its doubled bit cursor, and obeys the external compiled bound `BodyStartPrefix.rawTimeBound + 174 + 6*Unary.workSteps(nextTokenSlotPolynomial) + 48*n + 24*width`. Its 74 public declarations, including halt-source separation for safe downstream composition, reach only `propext` and `Quot.sound`. | The retained coordinate is data, not a dynamic cursor. Remaining body emission, a complete builder or builder `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Composed Cook–Levin complete first-clause prefix | `lean-audit/PNPConcreteCookLevinBuilderFirstClausePrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFirstClausePrefix.lean`, and `audits/lean-concrete-cook-levin-builder-first-clause-prefix0.test.mjs` | One literal table with `1138` plus four unary-evaluator rule counts composes the complete first-literal prefix, a fresh next-coordinate evaluator, and a fixed 535-rule eight-token tail. Every raw input emits exactly `T^FormulaWidth F Sep T F T T F T T T F Finish`, constructively identifies the three literals as positive variables zero, one, and two, retains token coordinate `FormulaVariableSlotBound + 12`, and proves its direct outcome is valid padding. The compiled bound evaluates to `FirstLiteralPrefix.rawTimeBound + 1158 + 6*Unary.workSteps(next) + 192*n + 96*width`. All 79 new declarations, and the 80-declaration combined audit including the predecessor separation theorem, reach only the approved Lean-standard closure. | Exactly the first canonical clause is emitted. The retained coordinate alone is data, not a dynamic cursor. Remaining body emission, a complete builder or builder `RawRefinement`, a concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Literal Cook–Levin token-cursor padding step | `lean-audit/PNPConcreteCookLevinBuilderDynamicTokenCursorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderDynamicTokenCursorStep.lean`, and `audits/lean-concrete-cook-levin-builder-dynamic-token-cursor-step0.test.mjs` | One literal table with `1192` plus the four inherited unary-evaluator rule counts composes the complete first clause, nine launch rules, and a fixed 45-rule cursor advance. Every raw input preserves the formula output, consumes the proved first in-range padding opportunity, and changes the retained unary coordinate from `FormulaVariableSlotBound + 12` to `+ 13`. The suffix costs exactly `2*cursorWord.length + 8` work steps including launch and fits `FirstClausePrefix.rawTimeBound + 48 + 12*cursorWord.length` compiled steps. All 47 public declarations, including two reviewed dead-state dispatch facts, reach only `propext` and `Quot.sound`. | This is one padding transition, not a general dynamic cursor loop or arbitrary raw slot decoder. It emits no token and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin first-clause remaining-padding run | `lean-audit/PNPConcreteCookLevinBuilderFirstClausePaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFirstClausePaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-first-clause-padding-run0.test.mjs` | One literal table with `1244` plus six unary-evaluator rule counts composes the prior cursor step, three total bridges, two new evaluators, and a fixed 25-rule countdown loop. For `V = formulaVariableSlotBound`, every raw input traverses exactly `(V-1)*(V+6) = formulaTokensPerClause-12` proved padding opportunities and materializes coordinate `V+1+formulaTokensPerClause`, whose direct outcome is the second clause's `Sep`. The output remains the exact first-clause prefix. The complete 84-declaration audit reaches only `propext` and `Quot.sound`. | This consumes one specifically proved padding block but does not emit clause two or implement a general dynamic cursor/arbitrary decoder, remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Uniform Cook–Levin full-schedule cursor controller | `lean-audit/PNPConcreteCookLevinBuilderFullScheduleCursorControllerAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFullScheduleCursorController.lean`, and `audits/lean-concrete-cook-levin-builder-full-schedule-cursor-controller0.test.mjs` | One finite table with `415` plus the header and two generated evaluator rule counts composes the complete header, three total bridges, the fixed 25-rule positive countdown, and exact count/terminal evaluators. For every raw input, the count is the complete rectangular body schedule plus final `Finish`, the semantic cursor returns the canonical token schedule, the raw controller reaches the exact terminal coordinate, and the compiled run accepts within an explicit input-size polynomial. Malformed countdown geometry, the unlaunched header endpoint, and one-step-short fuel remain timeout. | The raw loop controls all body opportunities but does not decode or emit each visited entry. The dynamic raw decoder, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, and `P = NP` remain absent. |
| Uniform Cook–Levin arbitrary-slot header router | `lean-audit/PNPConcreteCookLevinBuilderArbitrarySlotHeaderRouterAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderArbitrarySlotHeaderRouter.lean`, and `audits/lean-concrete-cook-levin-builder-arbitrary-slot-header-router0.test.mjs` | One fixed 54-rule comparator routes every natural coordinate at the exact problem-derived first-body boundary. It agrees with `formulaTokenSlotDirect`, accepts exactly the header branch, rejects equality and post-header coordinates, compiles within `36 * terminalSlotPolynomial^2` for every in-range coordinate, and remains timeout one step short or on malformed symbols. All 51 public declarations exclude project axioms and `Classical.choice`. | This is only the top-level route. It does not decode the post-header quotient into a clause and within-clause slot, emit a body token, complete the builder, supply builder `RawRefinement` or the concrete `PolynomialReduction`, prove CNFSAT NP-completeness or in P, or prove `P = NP`. |
| Uniform Cook–Levin arbitrary-slot post-header decoder | `lean-audit/PNPConcreteCookLevinBuilderArbitrarySlotPostHeaderDecoderAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderArbitrarySlotPostHeaderDecoder.lean`, and `audits/lean-concrete-cook-levin-builder-arbitrary-slot-post-header-decoder0.test.mjs` | A structurally recursive decoder classifies every natural post-header coordinate as an exact finite clause/within-clause pair, the unique `Finish`, or out of range. It reconstructs every body coordinate, agrees with direct token lookup, and extracts the exact shifted remainder from M209's checked raw result. All 22 public declarations exclude project axioms and `Classical.choice`. | This is semantic decoding plus a checked raw-result reader. It does not implement raw division or raw body-token emission, complete the raw builder, supply builder `RawRefinement` or the concrete `PolynomialReduction`, prove CNFSAT NP-completeness or in P, or prove `P = NP`. |
| Fixed Cook–Levin post-header raw divider | `lean-audit/PNPConcreteCookLevinBuilderPostHeaderRawDividerAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPostHeaderRawDivider.lean`, and `audits/lean-concrete-cook-levin-builder-post-header-raw-divider0.test.mjs` | One fixed 99-rule work machine accepts every unary dividend and positive unary width with the exact natural quotient and strict remainder. The trace reconstructs the dividend, compiles at six raw transitions per work step, times out one step short, and is bounded by `20 * (d + w + 1)^2`. M210 body coordinates instantiate the same decoded pair. All 57 public declarations exclude project axioms and `Classical.choice`. | This is a standalone divider, not a splice from M209 raw output or a token emitter. It does not classify terminal routes inside the raw machine, complete the builder, supply `RawRefinement` or the concrete `PolynomialReduction`, prove CNFSAT NP-completeness or in P, or prove `P = NP`. |
| All-coordinate Cook–Levin post-header raw launch | `lean-audit/PNPConcreteCookLevinBuilderPostHeaderRawLaunchAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPostHeaderRawLaunch.lean`, and `audits/lean-concrete-cook-levin-builder-post-header-raw-launch0.test.mjs` | Executable Lean orchestration reads the exact shifted remainder from M209's checked terminal configuration, conditionally launches M211's fixed divider, retains both exact traces, and recovers every M210 body, `Finish`, and out-of-range route. Every in-range coordinate excludes the out-of-range post-header branch, and one verifier-derived source-size polynomial bounds the combined compiled work. The 24-declaration audit has 10 empty and 14 `propext`/`Quot.sound` closures. | This constructs the divider input from the checked remainder; it is not a literal raw tape rewrite, does not preserve the complete builder workspace through such a bridge, and emits no body or `Finish` token. The complete builder, `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness or in P, and `P = NP` remain absent. |
| Literal Cook–Levin post-header raw tape bridge | `lean-audit/PNPConcreteCookLevinBuilderPostHeaderRawTapeBridgeAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPostHeaderRawTapeBridge.lean`, and `audits/lean-concrete-cook-levin-builder-post-header-raw-tape-bridge0.test.mjs` | One fixed collision-free 351-rule bridge consumes both exact M209 post-header terminal tapes, copies the derived remainder and problem width into a shielded M211 input, preserves arbitrary exterior workspace, retains exact bridge and divider traces, decodes the exact pair, compiles at six raw steps per work step, times out one step short, and fits one source-size polynomial for every in-range coordinate. The 78-declaration audit has 57 empty, 2 `propext`, and 19 `propext`/`Quot.sound` closures. | The bridge emits no selected body or `Finish` token and does not iterate the complete schedule. The complete builder, `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness or in P, and `P = NP` remain absent. |
| Literal Cook–Levin post-divider raw route classifier | `lean-audit/PNPConcreteCookLevinBuilderPostDividerRawRouteClassifierAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPostDividerRawRouteClassifier.lean`, and `audits/lean-concrete-cook-levin-builder-post-divider-raw-route-classifier0.test.mjs` | One fixed collision-free 180-rule bridge consumes both exact M213 divider-terminal tapes, copies the exact problem clause count from a restored sidecar, preserves the exterior and remainder ledger, and constructs a shielded M209 comparator input from the quotient marks. Exact bridge and comparator traces compile at six raw steps per work step, time out one step short, agree with every in-range M210 body or `Finish` route, and fit one source-size polynomial. The 85-declaration audit has 51 empty, 10 `propext`, and 24 `propext`/`Quot.sound` closures. | The classifier does not inspect, select, emit, or append a token or iterate the schedule. The complete builder, `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness or in P, and `P = NP` remain absent. |
| All-coordinate Cook–Levin selected-token launch | `lean-audit/PNPConcreteCookLevinBuilderPostDividerSelectedTokenLaunchAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPostDividerSelectedTokenLaunch.lean`, and `audits/lean-concrete-cook-levin-builder-post-divider-selected-token-launch0.test.mjs` | Every post-header coordinate is embedded in the canonical schedule; direct lookup derives padding, the exact body token, or the unique `Finish`. Padding preserves the emitted prefix, while populated entries run the existing fixed 59-rule appender to the exact next prefix. Exact work and compiled traces, one-step-short nonhalting, retained M214 physical classification, and one combined source-size polynomial are proved. The 30-declaration audit has 6 empty, 4 `propext`, and 20 `propext`/`Quot.sound` closures. | The selector-to-appender handoff is executable Lean orchestration, not yet a literal raw tape rewrite. It does not iterate the schedule, complete the builder or `RawRefinement`, package the concrete `PolynomialReduction`, prove CNFSAT NP-completeness or in P, or prove `P = NP`. |
| Complete semantic Cook–Levin schedule iteration | `lean-audit/PNPConcreteCookLevinBuilderCompleteScheduleIterationAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderCompleteScheduleIteration.lean`, and `audits/lean-concrete-cook-levin-builder-complete-schedule-iteration0.test.mjs` | Structural recursion consumes every verifier-derived post-header opportunity. Every bounded run equals the canonical emitted prefix, the complete run equals the exact `encodeCNFTokens` result, every coordinate retains M215 physical classifier/appender evidence for arbitrary workspace, and aggregate staged compiled work fits one source-size polynomial. The 12-declaration audit has 2 empty, 2 `propext`, and 8 `propext`/`Quot.sound` closures. | This is executable Lean orchestration over separately checked stages, not one literal raw-machine loop or a physical tape-to-tape inter-stage handoff. Complete builder `RawRefinement`, the concrete `PolynomialReduction`, CNFSAT NP-completeness or in P, and `P = NP` remain absent. |
| Physical Cook–Levin optional-token dispatch | `lean-audit/PNPConcreteCookLevinBuilderPhysicalOptionalTokenDispatchAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderPhysicalOptionalTokenDispatch.lean`, and `audits/lean-concrete-cook-levin-builder-physical-optional-token-dispatch0.test.mjs` | One fixed collision-free 64-rule machine reads padding or one of four CNF-token requests from a physical tape cell, restores the canonical builder boundary, and either preserves the prefix or enters the renamed 59-rule appender. Exact work and compiled traces cover arbitrary input, exterior workspace and output prefix; malformed requests and one-step-short fuel fail closed, canonical all-coordinate requests come from `scheduleEntry`, and one source-size polynomial bounds the stage. The 49-declaration audit has 23 empty, 13 `propext`, and 13 `propext`/`Quot.sound` closures. | The request cell remains an input rather than raw-classifier output, and no literal repeated schedule loop is constructed. Complete builder `RawRefinement`, the concrete `PolynomialReduction`, CNFSAT NP-completeness or in P, and `P = NP` remain absent. |
| Cook–Levin second-clause separator step | `lean-audit/PNPConcreteCookLevinBuilderSecondClauseSeparatorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondClauseSeparatorStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-clause-separator-step0.test.mjs` | One literal table with `1366` plus six unary-evaluator rule counts composes the complete padding run, a selected 59-rule `Sep` appender, two total bridges, and the fixed 45-rule cursor advance. Every raw input emits `T^FormulaWidth F Sep T F T T F T T T F Finish Sep`, advances the retained coordinate by one, proves the following direct token is `F`, and runs within `BuilderFirstClausePaddingRun.rawTimeBound + 246 + 24*n + 12*FormulaWidth + 12*cursorWord.length`. The 56-declaration combined audit has exactly 15 empty, 11 `propext`, and 30 `propext`/`Quot.sound` closures. | This emits only the separator beginning clause two. It does not emit the following `F`, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-clause first negative literal | `lean-audit/PNPConcreteCookLevinBuilderSecondClauseFirstLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondClauseFirstLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-second-clause-first-literal-prefix0.test.mjs` | One literal table with `1610` plus six unary-evaluator rule counts composes the separator prefix, two selected 59-rule `F` appenders, four total bridges, and two fixed 45-rule cursor advances. Every raw input emits `T^FormulaWidth F Sep T F T T F T T T F Finish Sep F F`, retains the next negative-sign coordinate, and runs within `BuilderSecondClauseSeparatorStep.rawTimeBound + 564 + 48*n + 24*FormulaWidth + 24*cursorWord.length`. The 87-declaration combined audit has exactly 25 empty, 18 `propext`, and 44 `propext`/`Quot.sound` closures. | This completes only negative variable zero in clause two. It does not emit the next literal sign, complete clause two, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-clause second negative literal | `lean-audit/PNPConcreteCookLevinBuilderSecondClauseSecondLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondClauseSecondLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-second-clause-second-literal-prefix0.test.mjs` | One literal table with `1976` plus six unary-evaluator rule counts composes the first-literal prefix, selected 59-rule `F`, `T`, and `F` appenders, six total bridges, and three fixed 45-rule cursor advances. Every raw input emits `T^FormulaWidth F Sep T F T T F T T T F Finish Sep F F F T F`, retains the following `Finish` coordinate, and runs within `BuilderSecondClauseFirstLiteralPrefix.rawTimeBound + 1026 + 72*n + 36*FormulaWidth + 36*cursorWord.length`. The 115-declaration combined audit has exactly 34 empty, 25 `propext`, and 56 `propext`/`Quot.sound` closures. | This completes only negative variable one in clause two. It does not emit the clause terminator, complete clause two, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin complete second-clause prefix | `lean-audit/PNPConcreteCookLevinBuilderSecondClausePrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondClausePrefix.lean`, and `audits/lean-concrete-cook-levin-builder-second-clause-prefix0.test.mjs` | One literal table with `2098` plus six unary-evaluator rule counts composes the second-literal prefix, a selected 59-rule `Finish` appender, two total bridges, and one fixed 45-rule cursor advance. Every raw input emits `T^FormulaWidth F Sep T F T T F T T T F Finish Sep F F F T F Finish`, proves that this is `encodedFormula.take (2 * (FormulaWidth + 19))`, and retains the following in-range padding coordinate. Its external bound adds `390 + 24*n + 12*FormulaWidth + 12*cursorWord.length` to the predecessor bound. The 57-line combined audit has exactly 15 empty, 10 `propext`, and 32 `propext`/`Quot.sound` closures. | This completes only clause two. It does not traverse clause-two padding, reach clause three, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-clause remaining-padding run | `lean-audit/PNPConcreteCookLevinBuilderSecondClausePaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondClausePaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-second-clause-padding-run0.test.mjs` | One literal table with `2150` plus eight unary-evaluator rule counts composes the complete second clause, three total bridges, two new evaluators, and the reused 25-rule padding countdown. For `V = formulaVariableSlotBound` and `C = formulaTokensPerClause`, every raw input traverses exactly `(V-1)*(V+6)+5 = C-7` padding coordinates and materializes `V+1+2*C`, whose direct outcome is the third clause's `Sep`. The output remains exactly `encodedFormula.take (2 * (FormulaWidth + 19))`. The complete 68-declaration audit has exactly 26 empty, 9 `propext`, and 33 `propext`/`Quot.sound` closures. | This reaches clause three only as a retained coordinate and emits no new token. It does not emit the third-clause separator, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin third-clause separator step | `lean-audit/PNPConcreteCookLevinBuilderThirdClauseSeparatorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderThirdClauseSeparatorStep.lean`, and `audits/lean-concrete-cook-levin-builder-third-clause-separator-step0.test.mjs` | One literal table with `2272` plus eight unary-evaluator rule counts composes the complete second-clause padding run, a reused selected 59-rule `Sep` appender, two total bridges, and the reused 45-rule cursor advance. Every raw input emits the third clause's opening `Sep`, retains `V+1+2*C+1`, proves the following direct token is `F`, and produces exactly `encodedFormula.take (2 * (FormulaWidth + 20))`. The complete 56-declaration audit has exactly 14 empty, 11 `propext`, and 31 `propext`/`Quot.sound` closures. | This emits only the third-clause separator. It does not emit the following `F`, complete clause three, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin third-clause first negative literal | `lean-audit/PNPConcreteCookLevinBuilderThirdClauseFirstLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderThirdClauseFirstLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-third-clause-first-literal-prefix0.test.mjs` | One literal table with `2516` plus eight unary-evaluator rule counts composes the complete third-clause separator prefix with the reused 235-rule two-`F` appender/cursor suffix behind one total bridge. Every raw input emits the complete negative literal on variable zero in clause three, retains `V+1+2*C+3`, constructively identifies the third excluded pair as `(0,2)`, proves the sign, zero terminator, and following sign are `F`, and produces exactly `encodedFormula.take (2 * (FormulaWidth + 22))`. Its compiled bound adds `732 + 48*n + 24*FormulaWidth + 24*cursorWord.length`; the complete 87-declaration audit has exactly 24 empty, 18 `propext`, and 45 `propext`/`Quot.sound` closures. | This emits only one fixed negative literal in clause three. It does not emit the following `F`, complete clause three, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin third-clause second negative literal | `lean-audit/PNPConcreteCookLevinBuilderThirdClauseSecondLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderThirdClauseSecondLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-third-clause-second-literal-prefix0.test.mjs` | One literal table with `3004` plus eight unary-evaluator rule counts composes the first-literal prefix with a fixed 479-rule `F T T F` appender/cursor suffix behind one total bridge. Every raw input emits the complete negative literal on variable two in clause three, retains `V+1+2*C+7`, proves the sign/terminator are `F`, both unary units are `T`, and the following direct token is `Finish`, and produces exactly `encodedFormula.take (2 * (FormulaWidth + 26))`. Its compiled bound adds `1752 + 96*n + 48*FormulaWidth + 48*cursorWord.length`; the complete 145-declaration audit has exactly 46 empty, 32 `propext`, and 67 `propext`/`Quot.sound` closures. | This emits only the second fixed negative literal in clause three. It does not emit the following `Finish`, complete clause three, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin complete third-clause prefix | `lean-audit/PNPConcreteCookLevinBuilderThirdClausePrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderThirdClausePrefix.lean`, and `audits/lean-concrete-cook-levin-builder-third-clause-prefix0.test.mjs` | One literal table with `3126` plus eight unary-evaluator rule counts composes the second-literal prefix, a selected 59-rule `Finish` appender, two total bridges, and one fixed 45-rule cursor advance. Every raw input emits the complete third clause, proves that its bits are `encodedFormula.take (2 * (FormulaWidth + 27))`, and retains the first in-range padding coordinate `V+1+2*C+8`. Its external bound adds `498 + 24*n + 12*FormulaWidth + 12*cursorWord.length` to the predecessor bound. The 57-line combined audit has exactly 14 empty, 10 `propext`, and 33 `propext`/`Quot.sound` closures. | This completes only clause three. It does not traverse clause-three padding, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin third-clause remaining-padding run | `lean-audit/PNPConcreteCookLevinBuilderThirdClausePaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderThirdClausePaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-third-clause-padding-run0.test.mjs` | One literal table with `3178` plus ten unary-evaluator rule counts composes the complete third clause, three total bridges, two new evaluators, and the reused 25-rule padding countdown. For `V = formulaVariableSlotBound` and `C = formulaTokensPerClause`, every raw input traverses exactly `(V-1)*(V+6)+4 = C-8` padding coordinates and materializes `V+1+3*C`, whose direct outcome is the fourth clause's `Sep`. The output remains exactly `encodedFormula.take (2 * (FormulaWidth + 27))`; the combined audit covers 65 new declarations and three reused countdown interfaces. | This reaches clause four only as a retained coordinate and emits no new token. It does not emit the fourth-clause separator, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin fourth-clause separator step | `lean-audit/PNPConcreteCookLevinBuilderFourthClauseSeparatorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFourthClauseSeparatorStep.lean`, and `audits/lean-concrete-cook-levin-builder-fourth-clause-separator-step0.test.mjs` | One literal table with `3300` plus ten unary-evaluator rule counts composes the complete third-clause padding run, one outer total bridge, and the reused 113-rule selected `Sep` appender/cursor suffix. Every raw input emits exactly the fourth clause's opening `Sep`, preserves `encodedFormula.take (2 * (FormulaWidth + 28))`, advances to `V+1+3*C+1`, and proves the following direct token is `F`. The external bound adds `426 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new public declarations plus eight reused interfaces with a `14`/`11`/`31` approved-closure split. | This emits only one fixed separator. It observes but does not emit the following `F`, does not complete clause four or implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin fourth-clause first-literal prefix | `lean-audit/PNPConcreteCookLevinBuilderFourthClauseFirstLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFourthClauseFirstLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-fourth-clause-first-literal-prefix0.test.mjs` | One literal table with `3666` plus ten unary-evaluator rule counts composes the fourth-clause separator prefix, one outer total bridge, and the reused 357-rule `F T F` appender/cursor suffix. Every raw input emits the first complete negative literal on variable one, preserves `encodedFormula.take (2 * (FormulaWidth + 31))`, advances to `V+1+3*C+4`, and proves the following direct token is `F`. The external bound adds `1422 + 72*n + 36*FormulaWidth + 36*cursorWord.length`; the 115-declaration audit covers 97 new declarations, 16 reused suffix interfaces, and two dead-state facts with a `33`/`25`/`57` approved-closure split. | This emits only the first literal of clause four. It observes but does not emit the second literal, does not complete clause four or implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin fourth-clause second-literal prefix | `lean-audit/PNPConcreteCookLevinBuilderFourthClauseSecondLiteralPrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFourthClauseSecondLiteralPrefix.lean`, and `audits/lean-concrete-cook-levin-builder-fourth-clause-second-literal-prefix0.test.mjs` | One literal table with `4154` plus ten unary-evaluator rule counts composes the fourth-clause first-literal prefix, one outer total bridge, and the reused 479-rule `F T T F` appender/cursor suffix. Every raw input emits the second complete negative literal on variable two, preserves `encodedFormula.take (2 * (FormulaWidth + 35))`, advances to `V+1+3*C+8`, and proves the following direct token is `Finish`. The external bound adds `2232 + 96*n + 48*FormulaWidth + 48*cursorWord.length`; the 147-declaration audit covers 124 new declarations, 21 reused suffix interfaces, and two dead-state facts with a `46`/`32`/`69` approved-closure split. | This emits only the second literal of clause four. It observes but does not emit the following `Finish`, does not complete clause four or implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin complete fourth-clause prefix | `lean-audit/PNPConcreteCookLevinBuilderFourthClausePrefixAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFourthClausePrefix.lean`, and `audits/lean-concrete-cook-levin-builder-fourth-clause-prefix0.test.mjs` | One literal table with `4276` plus ten unary-evaluator rule counts composes the second-literal prefix, a selected 59-rule `Finish` appender, two total bridges, and one fixed 45-rule cursor advance. Every raw input emits the complete fourth clause, proves that its bits are `encodedFormula.take (2 * (FormulaWidth + 36))`, and retains the first in-range padding coordinate `V+1+3*C+9`. Its external bound adds `618 + 24*n + 12*FormulaWidth + 12*cursorWord.length` to the predecessor bound. The 57-line combined audit has exactly 14 empty, 10 `propext`, and 33 `propext`/`Quot.sound` closures. | This completes only clause four. It does not traverse clause-four padding, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin fourth-clause remaining-padding run | `lean-audit/PNPConcreteCookLevinBuilderFourthClausePaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFourthClausePaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-fourth-clause-padding-run0.test.mjs` | One literal table with `4328` plus twelve unary-evaluator rule counts composes the complete fourth clause, three total bridges, two new evaluators, and the reused 25-rule padding countdown. For `V = formulaVariableSlotBound` and `C = formulaTokensPerClause`, every raw input traverses exactly `(V-1)*(V+6)+3 = C-9` padding coordinates without emitting a token and materializes `V+1+4*C`. Direct lookup and the specification cursor both prove that this first opportunity in the intentionally empty fifth fixed-width clause slot is padding. The output remains exactly `encodedFormula.take (2 * (FormulaWidth + 36))`; the combined audit covers 65 new declarations and three reused countdown interfaces with a `26`/`9`/`33` approved-closure split. | This reaches only the first blank opportunity in the fifth clause rectangle. It does not traverse that empty rectangle, reach the next constraint, emit another token, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin empty fifth-clause padding run | `lean-audit/PNPConcreteCookLevinBuilderFifthClausePaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFifthClausePaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-fifth-clause-padding-run0.test.mjs` | One literal table with `4380` plus fourteen unary-evaluator rule counts composes the complete fourth-clause padding run, three total bridges, two new evaluators, and the reused 25-rule padding countdown. For `V = formulaVariableSlotBound` and `C = formulaTokensPerClause`, every raw input traverses all `C` opportunities in the intentionally empty fifth fixed-width clause rectangle without emitting a token and materializes `V+1+5*C`. Direct lookup and the specification cursor prove every traversed opportunity and this first opportunity in the intentionally empty sixth rectangle are padding. The output remains exactly `encodedFormula.take (2 * (FormulaWidth + 36))`; the combined audit covers 65 new declarations and three reused countdown interfaces with a `28`/`9`/`31` approved-closure split. | This traverses only the empty fifth clause rectangle. It does not traverse the sixth rectangle, reach the next constraint, emit another token, implement a general dynamic cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin remaining first-constraint padding run | `lean-audit/PNPConcreteCookLevinBuilderFirstConstraintPaddingRunAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderFirstConstraintPaddingRun.lean`, and `audits/lean-concrete-cook-levin-builder-first-constraint-padding-run0.test.mjs` | One literal table with `4432` plus sixteen unary-evaluator rule counts composes the fifth-clause padding run, three total bridges, two new evaluators, and the reused 25-rule padding countdown. For `V = formulaVariableSlotBound`, `C = formulaTokensPerClause`, and `Q = formulaClauseSlotsPerConstraint = 1+V*V`, every raw input traverses exactly `(V-2)*(V+2)*C = (Q-5)*C` remaining empty opportunities of the first scheduled constraint without emitting a token and materializes `V+1+Q*C`. Direct lookup and the specification cursor prove every traversed opportunity is padding and the retained target is the `Sep` beginning the second scheduled constraint. The output remains exactly `encodedFormula.take (2 * (FormulaWidth + 36))`; the combined audit covers 65 new declarations and three reused countdown interfaces with a `26`/`9`/`33` approved-closure split. | This reaches the second constraint only as a retained separator coordinate. It observes but does not emit that `Sep`, does not emit the next constraint's first literal, implement a general cursor/arbitrary decoder, supply the remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint separator step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSeparatorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSeparatorStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-separator-step0.test.mjs` | One literal table with `4554` plus sixteen unary-evaluator rule counts composes the complete first-constraint padding run, one outer total bridge, and the reused 113-rule selected `Sep` appender/cursor suffix. Every raw input emits exactly the separator beginning the second scheduled constraint, preserves `encodedFormula.take (2 * (FormulaWidth + 37))`, advances to `V+1+Q*C+1`, and proves the following direct token is `T`. The external bound adds `534 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a `14`/`11`/`31` approved-closure split. | This emits only one fixed separator. It observes but does not emit the following `T`, does not emit the second constraint's first literal or traverse that constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal sign step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSignStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSignStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-sign-step0.test.mjs` | One literal table with `4676` plus sixteen unary-evaluator rule counts composes the complete second-constraint separator step, one outer total bridge, and the reused 113-rule selected `T` appender/cursor suffix. Every raw input emits exactly the positive sign beginning the second constraint's first literal, preserves `encodedFormula.take (2 * (FormulaWidth + 38))`, advances to `V+1+Q*C+2`, and proves the following direct token is the first unary `T` of a nonzero variable index. The external bound adds `546 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a `14`/`11`/`31` approved-closure split. | This emits only one fixed sign. It observes but does not emit the following unary `T`, does not complete the first literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal first unary-unit step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralFirstUnaryUnitStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralFirstUnaryUnitStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-first-unary-unit-step0.test.mjs` | One literal table with `4798` plus sixteen unary-evaluator rule counts composes the complete first-literal sign step, one outer total bridge, and the reused 113-rule selected `T` appender/cursor suffix. Every raw input emits exactly the first unary `T` of the second constraint's first variable index, preserves `encodedFormula.take (2 * (FormulaWidth + 39))`, advances to `V+1+Q*C+3`, and proves the following direct token is the second unary `T` of an index that is at least three. The external bound adds `558 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a measured `14`/`11`/`31` approved-closure split. | This emits only one fixed unary unit. It observes but does not emit the following unary `T`, does not complete the first literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal second unary-unit step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSecondUnaryUnitStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSecondUnaryUnitStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-second-unary-unit-step0.test.mjs` | One literal table with `4920` plus sixteen unary-evaluator rule counts composes the complete first-unary-unit step, one outer total bridge, and the reused 113-rule selected `T` appender/cursor suffix. Every raw input emits exactly the second unary `T` of the second constraint's first variable index, preserves `encodedFormula.take (2 * (FormulaWidth + 40))`, advances to `V+1+Q*C+4`, and proves the following direct token is the third unary `T` of an index that is at least three. The external bound adds `570 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a measured `14`/`11`/`31` approved-closure split. | This emits only one additional fixed unary unit. It observes but does not emit the following third unary `T`, does not complete the first literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal third unary-unit step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralThirdUnaryUnitStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralThirdUnaryUnitStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-third-unary-unit-step0.test.mjs` | One literal table with `5042` plus sixteen unary-evaluator rule counts composes the complete second-unary-unit step, one outer total bridge, and the reused 113-rule selected `T` appender/cursor suffix. Every raw input emits exactly the third and final unary `T` of the second constraint's first variable index, preserves `encodedFormula.take (2 * (FormulaWidth + 41))`, advances to `V+1+Q*C+5`, and proves by an exact index-three schedule case split that the following direct token is the terminating `F`. The external bound adds `582 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a measured `14`/`11`/`31` approved-closure split. | This emits only one additional fixed unary unit. It observes but does not emit the following terminating `F`, does not complete the first literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal terminator step | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralTerminatorStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralTerminatorStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-terminator-step0.test.mjs` | One literal table with `5164` plus sixteen unary-evaluator rule counts composes the complete third-unary-unit step, one outer total bridge, and the reused 113-rule selected `F` appender/cursor suffix. Every raw input emits exactly the terminating `F`, preserves `encodedFormula.take (2 * (FormulaWidth + 42))`, advances to `V+1+Q*C+6`, and proves the following direct token is `Finish` at width one and `T` at wider widths. The external bound adds `594 + 24*n + 12*FormulaWidth + 12*cursorWord.length`; the 56-declaration audit covers all 48 new declarations plus eight reused interfaces with a measured `14`/`11`/`31` approved-closure split. | This emits only the literal terminator. It observes but does not emit the following width-dependent `Finish` or `T`, does not traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint first-literal successor token | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSuccessorTokenStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFirstLiteralSuccessorTokenStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-first-literal-successor-token-step0.test.mjs` | One literal table with `5284` plus eighteen unary-evaluator rule counts evaluates the represented tape width, takes a finite `Finish`/`T` branch through one reused appender, emits exactly one canonical token, preserves `encodedFormula.take (2 * (FormulaWidth + 43))`, and advances to `V+1+Q*C+7`. The following opportunity is proved to be padding at width one and unary `T` at wider widths. The external bound adds `600 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the dedicated 82-declaration audit covers all 80 new public declarations plus two strengthened predecessor boundaries with a measured `37`/`12`/`33` approved-closure split. | This emits only the width-selected successor. It observes but does not emit the following padding or unary token, does not traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint padding-or-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintPaddingOrUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintPaddingOrUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-padding-or-unary-opportunity-step0.test.mjs` | One literal table with `5404` plus twenty unary-evaluator rule counts consumes the opportunity after the first literal's width-selected successor. At width one it takes a direct skip bridge and emits nothing; at wider widths it appends exactly the first unary `T` of the second literal. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 1))` and advances to `V+1+Q*C+8`. The external bound adds `612 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the dedicated 82-declaration audit covers all 80 new public declarations plus two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one width-dependent schedule opportunity. It observes but does not consume the following padding or unary slot, does not complete the second literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint second padding-or-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSecondPaddingOrUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSecondPaddingOrUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-second-padding-or-unary-opportunity-step0.test.mjs` | One literal table with `5524` plus twenty-two unary-evaluator rule counts reuses the reviewed 93-rule optional appender to consume the next opportunity. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the second unary `T` of the second literal. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 2))` and advances to `V+1+Q*C+9`. The external bound adds `624 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers all 66 new declarations, fourteen reused optional-appender interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or third unary slot, does not complete the second literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint third padding-or-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintThirdPaddingOrUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintThirdPaddingOrUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-third-padding-or-unary-opportunity-step0.test.mjs` | One literal table with `5644` plus twenty-four unary-evaluator rule counts reuses the reviewed 93-rule optional appender to consume the next opportunity. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the third unary `T` of the second literal. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 3))` and advances to `V+1+Q*C+10`. The external bound adds `636 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers all 66 new declarations, fourteen reused optional-appender interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or fourth unary slot, does not complete the second literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint fourth padding-or-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFourthPaddingOrUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFourthPaddingOrUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-fourth-padding-or-unary-opportunity-step0.test.mjs` | One literal table with `5764` plus twenty-six unary-evaluator rule counts reuses the reviewed 93-rule optional appender to consume the next opportunity. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the fourth unary `T` of the second literal. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 4))` and advances to `V+1+Q*C+11`. The external bound adds `648 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers all 66 new declarations, fourteen reused optional-appender interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or terminating `F`, does not complete the second literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint fifth padding-or-terminator opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintFifthPaddingOrTerminatorOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-fifth-padding-or-terminator-opportunity-step0.test.mjs` | One literal table with `5884` plus twenty-eight unary-evaluator rule counts uses a new reviewed 93-rule optional-terminator controller over the audited token-appender rules. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the second literal’s terminating `F`. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 5))` and advances to `V+1+Q*C+12`, where the following direct slot is padding or the opening unary `T`. The external bound adds `660 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers 66 new outer declarations, fourteen new optional-terminator interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or opening `T`, does not complete the following literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint sixth padding-or-opening-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSixthPaddingOrOpeningUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSixthPaddingOrOpeningUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-sixth-padding-or-opening-unary-opportunity-step0.test.mjs` | One literal table with `6004` plus thirty unary-evaluator rule counts reuses the reviewed 93-rule optional appender. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the following literal’s opening positive `T`. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 6))` and advances to `V+1+Q*C+13`, where the following direct slot is padding or the first unary-index `T`. The external bound adds `672 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers 66 new declarations, fourteen reused optional-appender interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or first unary-index `T`, does not complete the following literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Cook–Levin second-constraint seventh padding-or-unary opportunity | `lean-audit/PNPConcreteCookLevinBuilderSecondConstraintSeventhPaddingOrUnaryOpportunityStepAxiomAudit.lean`, `lean-regression/PNPConcreteCookLevinBuilderSecondConstraintSeventhPaddingOrUnaryOpportunityStep.lean`, and `audits/lean-concrete-cook-levin-builder-second-constraint-seventh-padding-or-unary-opportunity-step0.test.mjs` | One literal table with `6124` plus thirty-two unary-evaluator rule counts reuses the reviewed 93-rule optional appender. At width one it takes the direct skip bridge and emits nothing; at wider widths it appends exactly the following literal’s first unary-index `T`. It preserves `encodedFormula.take (2 * (FormulaWidth + 43 + if tapeWidth = 1 then 0 else 7))` and advances to `V+1+Q*C+14`, where the following direct slot is padding or the second unary-index `T`. The external bound adds `684 + 24*n + 12*FormulaWidth + 12*width + 12*widthRootPrefixLength + 6*widthWorkSteps + 6*targetWorkSteps`; the 82-declaration audit covers 66 new declarations, fourteen reused optional-appender interfaces, and two strengthened schedule boundaries with a measured `37`/`12`/`33` approved-closure split. | This handles only one additional width-dependent schedule opportunity. It observes but does not consume the following padding or second unary-index `T`, does not complete the following literal or traverse the second constraint, does not implement a general cursor, and supplies no remaining body, complete builder, builder `RawRefinement`, concrete `PolynomialReduction`, CNFSAT NP-completeness, CNFSAT in P, or `P = NP`. |
| Direct-wire NAND semantics | `lake env lean -DwarningAsError=true lean-audit/PNPNANDSemanticsAxiomAudit.lean` | The typed topological NAND syntax, Boolean evaluation, output-wiring laws, and small semantic examples are assumption-free. | Enumeration, minimum size, replacement/slack, the locked builder or threshold, SAT, or `P = NP`. |
| Exact-width NAND enumerator | `lake env lean -DwarningAsError=true lean-audit/PNPNANDEnumeratorAxiomAudit.lean` | Every typed source, ordered gate, topological program, and output tuple appears; every existing program/word pair has an enumerated reification with the same program and pointwise output sources. | Canonical or duplicate-free enumeration, semantic equivalence, minimum size, replacement/slack, threshold, SAT, or `P = NP`. |
| Exhaustive direct-wire reference minimum | `lake env lean -DwarningAsError=true lean-audit/PNPNANDMinimumAxiomAudit.lean` | Finite truth tables decide semantic equivalence; exact candidate sizes are scanned from zero through the target size; the selected size has an equivalent witness and is a global lower bound; residual slack is zero exactly at semantic minimum. | Any practical or polynomial runtime, the report's residual-band minimizer, locked-NAND threshold, SAT, or `P = NP`. |
| Concrete framed replacement/slack | `lake env lean -DwarningAsError=true lean-audit/PNPNANDSlackAxiomAudit.lean` | Serial environment/support/continuation frames preserve equivalent support replacement and expose the corresponding additive slack identity. | Arbitrary support subsets/profiles, the report's global replacement theorem, or the locked-NAND family. |
| Local locked-NAND baseline bridge | `node --test audits/lean-locked-nand-baseline0.test.mjs` plus the four locked-baseline Lean axiom transcripts | Six typed local candidates have honest output widths and constant-free internal programs; semantic outputs inject into gates; counts come from typed sources; the five square local macros have exact empty-context minima. | Cross-instance `BaselineDistinct`, the locked builder or threshold, residual slack at most four, polynomiality, SAT, or `P = NP`. |
| Conditional locked-NAND threshold boundary | `node --test audits/lean-locked-nand-threshold-boundary0.test.mjs` and `lean-audit/PNPLockedNANDThresholdBoundaryAxiomAudit.lean` | From actual typed candidates plus six explicit semantic premises, Lean derives the conditional unsat/sat minimum boundary and residual slack at most four. The global semantic-threshold milestone now instantiates all six fields for the real typed candidate. | The generic boundary itself is not the report threshold theorem and does not supply an encoded polynomial builder, SAT-in-P result, or `P = NP`. |
| Global locked-NAND candidates | `node --test audits/lean-locked-nand-global-candidates0.test.mjs` and `lean-audit/PNPLockedNANDGlobalCandidatesAxiomAudit.lean` | For every finite typed circuit, Lean constructs the exact `B`/`B` baseline and `B+4`/`B+1` extension, preserves all baseline outputs, proves the final conjunction semantics, excludes internal constants, and isolates every baseline output from `z`. | Candidate assembly alone does not prove `BaselineDistinct`, the unsatisfiable/satisfiable final branch laws, the polynomial bitstring builder, the threshold, residual slack at most four, SAT, or `P = NP`. |
| Global locked-NAND baseline distinctness | `node --test audits/lean-locked-nand-global-baseline-distinct0.test.mjs`, `lean-audit/PNPLockedNANDGlobalBaselineDistinctAxiomAudit.lean`, and `lean-regression/PNPLockedNANDGlobalBaselineDistinct.lean` | Every exposed baseline coordinate is nonconstant, is not any positive carrier-input projection, and computes a Boolean function distinct from every other exposed coordinate; the square baseline therefore has exact exhaustive reference minimum `B`. | Either whole-carrier final branch law, completion of the six-field threshold premise package, the uniform polynomial builder, the locked-NAND threshold, global residual slack at most four, SAT, or `P = NP`. |
| Global locked-NAND unsatisfiable final-zero branch | `node --test audits/lean-locked-nand-global-unsatisfiable-final-zero0.test.mjs`, `lean-audit/PNPLockedNANDGlobalUnsatisfiableFinalZeroAxiomAudit.lean`, and `lean-regression/PNPLockedNANDGlobalUnsatisfiableFinalZero.lean` | If the source circuit is unsatisfiable, the complete final output is false for every carrier valuation and the exhaustive reference minimum is exactly `B`. | This branch by itself does not supply the satisfiable laws, encoded polynomial builder, report-level threshold, SAT-in-P result, or `P = NP`. |
| Global locked-NAND typed semantic threshold | `node --test audits/lean-locked-nand-global-semantic-threshold0.test.mjs`, `lean-audit/PNPLockedNANDGlobalSemanticThresholdAxiomAudit.lean`, and `lean-regression/PNPLockedNANDGlobalSemanticThreshold.lean` | For every finite topological NAND circuit, one answer-independent candidate supplies all six threshold premises. Satisfiable sources have exhaustive minimum in `[B+1, B+4]`, unsatisfiable sources have minimum exactly `B`, satisfiability is equivalent to crossing `B+1`, and residual slack is at most four. All eight public declarations use exactly `propext` and `Quot.sound`. | This semantic theorem alone has no encoded polynomial-time builder or runtime/certificate bound. Downstream milestones now supply the concrete reduction and active compatibility link, but no target decider, CNFSAT-in-P or NP-hardness result, and no `P = NP`. |
| Encoded locked-NAND semantic boundary | `node --test audits/lean-concrete-locked-nand-semantic-reduction0.test.mjs`, `lean-audit/PNPConcreteLockedNANDSemanticReductionAxiomAudit.lean`, and `lean-regression/PNPConcreteLockedNANDSemanticReduction.lean` | A strict version-zero grammar, direct semantics-preserving legacy output normalization, exact codecs for source circuits and complete candidate/threshold instances, fail-closed malformed input, and an all-bitstring source/target equivalence. The 50 audited declarations use only empty closure, `propext`, and `propext` plus `Quot.sound`. | This pure transformation is not a parser or emitter machine, `RawRefinement`, `PolynomialReduction`, runtime/output-size bound, abstract threshold discharge, CNFSAT-in-P result, or `P = NP`. |
| Strict-v0 locked-NAND source parser | `node --test audits/lean-concrete-locked-nand-source-parser0.test.mjs`, `lean-audit/PNPConcreteLockedNANDSourceParserAxiomAudit.lean`, and `lean-regression/PNPConcreteLockedNANDSourceParser.lean` | A direct nine-symbol table with 228 states and 2,052 pairwise query-distinct rules decides every bitstring exactly. It accepts iff the strict-v0 source is valid, preserves valid bytes, clears invalid bytes, and cannot time out within `6 × 4096 × (n+1)^3` compiled raw steps. Polynomial-time machine/function witnesses and the validator's exact leaf `RawRefinement` are proved. | This parser alone emits no target and establishes no `PolynomialReduction`; the downstream target-emitter milestone now supplies exact target bytes and strict composition. Abstract threshold-language discharge, CNFSAT-in-P and NP-hardness transport, and `P = NP` remain absent. |
| Strict-v0 locked-NAND target emitter | `node --test audits/lean-concrete-locked-nand-target-emitter0.test.mjs`, `lean-audit/PNPConcreteLockedNANDTargetEmitterAxiomAudit.lean`, and `lean-regression/PNPConcreteLockedNANDTargetEmitter.lean` | One fixed 1,387,921-rule grammar-only controller has exact all-input halted behavior, emits the direct raw target, has explicit polynomial runtime and output-size bounds, and supplies compiled machine/function and leaf `RawRefinement` witnesses. Composition with the strict parser computes `buildLockedNANDInstance` and has recursive `RawRefinement`. | Standalone grammar acceptance is intentionally broader than intrinsic validity. Downstream milestones now package the reduction and active compatibility link; CNFSAT-in-P, NP-hardness transport, and `P = NP` remain absent. |
| Strict-v0 locked-NAND polynomial reduction | `node --test audits/lean-concrete-locked-nand-polynomial-reduction0.test.mjs`, `lean-audit/PNPConcreteLockedNANDPolynomialReductionAxiomAudit.lean`, and `lean-regression/PNPConcreteLockedNANDPolynomialReduction.lean` | The exact strict parser/emitter function is packaged as `PolynomialReduction EncodedNANDSAT EncodedLockedNANDThreshold`, with exact output, all-bitstring language equivalence, a `ReducesTo` witness, and recursive `RawRefinement`. | M186 now makes this exact target the active report-facing endpoint. A target decider, CNFSAT NP-hardness or membership in P, ZeroSlack/PCCMin, and `P = NP` remain absent. |
| CNF-to-NAND semantic compiler | `node --test audits/lean-concrete-cnf-to-nand0.test.mjs`, `lean-audit/PNPConcreteCNFToNANDAxiomAudit.lean`, and `lean-regression/PNPConcreteCNFToNAND.lean` | Every strict canonical CNF formula is structurally compiled to a well-formed intrinsically topological NAND circuit, with exact satisfiability preservation, strict malformed-input rejection, exact gate count, a quadratic serialized-output bound, and semantic composition with the concrete locked-NAND threshold builder. | This semantic layer alone is not a finite work machine or complexity witness; the following milestone now supplies those interfaces. CNFSAT in P, the abstract threshold link, ZeroSlack/PCCMin, and `P = NP` remain absent. |
| All-input CNF-to-NAND polynomial reduction | `node --test audits/lean-concrete-cnf-to-nand-polynomial-reduction0.test.mjs`, `lean-audit/PNPConcreteCNFToNANDPolynomialReductionAxiomAudit.lean`, and `lean-regression/PNPConcreteCNFToNANDPolynomialReduction.lean` | One fixed parser/carrier/controller work graph halts on every bitstring, emits exactly `compileEncodedCNFToNAND`, has an external-input polynomial bound, compiles to a non-timeout `PolynomialTimeFunction`, retains literal `RawRefinement`, packages `PolynomialReduction CNFSAT EncodedNANDSAT`, and composes it with the strict locked-NAND reduction. | This syntax-directed compiler does not decide CNF-SAT or put either language in P. The following publication theorem now exposes its composed concrete reduction; ZeroSlack/PCCMin and `P = NP` remain absent. |
| Concrete locked-NAND threshold publication | `node --test audits/lean-concrete-locked-nand-threshold-publication0.test.mjs` and `lean-audit/PNPConcreteLockedNANDThresholdPublicationAxiomAudit.lean` | `PNP.Main.locked_nand_threshold` is a uniform all-bitstring `ReducesTo CNFSAT EncodedLockedNANDThreshold` theorem backed by the fixed polynomial-time parser/compiler/emitter pipeline. Its exact closure uses only `propext` and `Quot.sound`. | This is a concrete many-one reduction, not a polynomial decider for the target. M186 consumes it directly, but concrete CNFSAT NP-hardness, residual-band/ZeroSlack/PCCMin, and `P = NP` remain open. |
| Concrete report-facing locked-NAND compatibility | `node --test audits/lean-concrete-legacy-locked-nand-compatibility0.test.mjs`, `lean-audit/PNPConcreteLegacyLockedNANDCompatibilityAxiomAudit.lean`, and `lean-regression/PNPConcreteLegacyLockedNANDCompatibility.lean` | Every report-facing complexity interface reuses the concrete finite-pipeline model; `SAT` and `LockedNANDThreshold` are exact concrete definitions, the concrete NP verifier and all-bitstring reduction are reused, and `CheckerTrustModel` no longer accepts duplicate locked-NAND trust. The named endpoint uses only `propext` and `Quot.sound`. | This removes one project axiom and one caller trust edge. It does not supply a target decider, residual-band reduction, ZeroSlack/PCCMin, concrete NP-hardness, the eligible root theorem, or `P = NP`. |
| Concrete report-facing residual-band compatibility | `node --test audits/lean-concrete-residual-band-compatibility0.test.mjs`, `lean-audit/PNPConcreteResidualBandCompatibilityAxiomAudit.lean`, and `lean-regression/PNPConcreteResidualBandCompatibility.lean` | `ResidualBandExactMinimization` is the concrete fail-closed encoded direct-wire minimum-threshold predicate. Arbitrary typed candidate encodings have exact `threshold + 1 ≤ referenceMinimum` semantics, and locked-to-residual transport is the compiled identity polynomial reduction. The named endpoint uses only `propext`. | This removes one project axiom and one caller trust edge. Exhaustive reference minimization remains only a semantic specification; no executable polynomial minimizer, residual-band promise theorem, ZeroSlack/PCCMin result, SAT-in-P result, global gate, eligible root theorem, or `P = NP` follows. |
| Typed PCCPack generation and reflection | `node --test audits/lean-typed-pccpack-reflection0.test.mjs`, `lean-audit/PNPTypedPCCPackReflectionAxiomAudit.lean`, and `lean-regression/PNPTypedPCCPackReflection.lean` | `GeneratePCCPack` and `CheckPCCPackexp` are transparent typed definitions over an explicit `PCCMinLoopCertificate`; generated packages accept, preserve the exact certificate, and mismatched identifiers reject. The compiled project-axiom inventory is empty. | This removes opaque packaging/checking proof authority; it does not construct or validate the mathematical content of the loop certificate, prove unconditional ZeroSlack/PCCMin or polynomial runtime, establish SAT in P, close a global gate, or prove `P = NP`. |
| Explicit-list residual routes | `lake env lean -DwarningAsError=true lean-audit/PNPResidualRoutesAxiomAudit.lean` | A supplied finite list is scanned for a strictly smaller equivalent implementation; gains are sound and strictly descend in residual slack; exact and ZeroSlack results require semantic-minimality proofs. | List or global route completeness, absence of unlisted gains, the report ZeroSlack contradiction, PCCMin exactness, or polynomial runtime. |
| Verified residual-gain chain bound | `node --test audits/lean-residual-gain-chain0.test.mjs`, the two residual-gain axiom transcripts, and `lean-regression/PNPResidualGainChain.lean` | Every finite proof-bearing or executably verified chain of adjacent strict equivalent gains preserves semantics and has length at most the starting residual slack. The existing locked-family slack bound specializes this to at most four accepted gain steps. | Gain generation, route completeness, a stopping certificate, ZeroSlack, an exact minimizer, polynomial checker/PCCMin runtime, SAT in P, or `P = NP`. |
| Global strict-gain stopping specification | `node --test audits/lean-residual-gain-stopping0.test.mjs`, `lean-audit/PNPResidualGainStoppingAxiomAudit.lean`, and `lean-regression/PNPResidualGainStopping.lean` | For every finite direct-wire implementation, positive residual slack is equivalent to existence of a strict equivalent gain; zero slack and semantic minimality are equivalent to global absence of one. A verified endpoint with that global premise packages an exact minimum result. | A route generator, finite-list or route completeness, derivation of global absence from scan failure, the report's ZeroSlack certificate, polynomial runtime, SAT in P, or `P = NP`. |
| Terminal full-carrier residual bridge | `node --test audits/lean-residual-terminal-full-bridge0.test.mjs`, `lean-audit/PNPResidualTerminalFullBridgeAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFullBridge.lean` | Terminalization preserves the whole direct-wire implementation, exact gate count, and every input/output semantic coordinate. An independently stated universal terminal minimum equals the exhaustive reference minimum; a cheaper whole-span realization exists exactly at positive slack and gives strict descent. | The quotient carrier/firewall, proper supports, SaturatePositive, BCEL/BN2–BN6, route generation, ZeroSlack/PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Terminal quotient/full mode firewall | `node --test audits/lean-residual-terminal-mode-firewall0.test.mjs`, `lean-audit/PNPResidualTerminalModeFirewallAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalModeFirewall.lean` | A computed finite terminal profile records all ten manuscript roles. Forgetful projection preserves the exact implementation, gate count, and complete Boolean semantics, while promotion back to the full carrier is possible exactly when every forgotten coordinate agrees. A concrete forgotten mismatch blocks promotion. | Proper or governed supports, arbitrary quotient coarsening, projection minima, saturation, Package E, BCEL/BN2–BN6, ZeroSlack/PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Terminal full/quotient projection minima | `node --test audits/lean-residual-terminal-projection-minimum0.test.mjs`, `lean-audit/PNPResidualTerminalProjectionMinimumAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalProjectionMinimum.lean` | For every finite direct-wire implementation and computed finite profile, exhaustive scans through the current gate count attain universal full and quotient minima. Forgetting coordinates cannot increase the minimum; the exact projection defect is zero precisely when an attained quotient minimum has a checked full lift. | These are finite reference computations, not polynomial algorithms. Proper/governed supports, SaturatePositive, Package E, BCEL/BN2–BN6, route completeness, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| Terminal projection transfer identity | `node --test audits/lean-residual-terminal-projection-transfer0.test.mjs`, `lean-audit/PNPResidualTerminalProjectionTransferAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalProjectionTransfer.lean` | For every finite four-corner family sharing one computed terminal-profile observer and one projection, signed full and quotient deltas satisfy the manuscript’s exact transfer identity. The constant-cut hypotheses turn a positive join defect into equal positive projection excess. | This supplies arithmetic, not the proper-support or saturated square needed to instantiate the later contradiction. SaturatePositive, Package E, BCEL/BN2–BN6, route completeness, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Terminal saturation closure | `node --test audits/lean-residual-terminal-saturation0.test.mjs`, `lean-audit/PNPResidualTerminalSaturationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSaturation.lean` | For every finite primitive-record universe and every explicit Boolean rule-tagged dependency system, the reflexive transitive saturation is extensive, closed, least, monotone, and idempotent; fixed points are exactly closed supports. | The dependency relation remains explicit data. Proper-support construction, support completion, legitimate projection squares, SaturatePositive, Package E, BCEL/BN2–BN6, route completeness, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Terminal executable and physical support completion | `node --test audits/lean-residual-terminal-physical-support-completion0.test.mjs`, `lean-audit/PNPResidualTerminalPhysicalSupportCompletionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPhysicalSupportCompletion.lean` | A deterministic finite work list now computes exactly the inductive terminal saturation for every finite seed. For every selected gate set, the actual direct-wire program then computes canonically ordered incoming boundary and outgoing interface wires, with proofs that no crossing wire is omitted or added and that the combined support is compatible. | The dependency system remains explicit rather than extracted as the manuscript profile frontier. Proper positive support, full support completion, square legitimacy and the required projection square, SaturatePositive, Package E, BCEL/BN2–BN6, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Arbitrary terminal support extraction | `node --test audits/lean-residual-terminal-support-extraction0.test.mjs`, `lean-audit/PNPResidualTerminalSupportExtractionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSupportExtraction.lean` | Every finite, possibly noncontiguous selected gate set is extracted into an actual open direct-wire candidate over its computed incoming boundary and ordered interface, with exact open semantics and induced whole-circuit recovery. | Profile-frontier derivation, square legitimacy, `SaturatePositive`, route completeness, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Governed proper-positive terminal support search | `node --test audits/lean-residual-terminal-proper-support0.test.mjs`, `lean-audit/PNPResidualTerminalProperSupportAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalProperSupport.lean` | Every canonical finite seed is searched under the explicit dependency system. A result is nonempty, proper, closed, physically compatible, semantically exact, and has an equivalent strictly smaller exact-minimum replacement; `none` excludes every canonical qualifying seed. | This is exhaustive reference computation, not a polynomial algorithm. Full frontier derivation, square legitimacy, `SaturatePositive`, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| Saturated terminal support-square closure | `node --test audits/lean-residual-terminal-support-square-closure0.test.mjs`, `lean-audit/PNPResidualTerminalSupportSquareClosureAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSupportSquareClosure.lean` | For every pair of finite seeds, Lean computes closed left, right, canonical meet, and saturated-union join corners; proves exact greatest-lower-bound and least-upper-bound laws; and supplies compatible physical completion and exact semantic extraction for all four corners. | The dependency system remains explicit. Frontier pushout, projection compatibility, BN2 square legitimacy, `SaturatePositive`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Governed terminal support completion | `node --test audits/lean-residual-terminal-governed-support-completion0.test.mjs`, `lean-audit/PNPResidualTerminalGovernedSupportCompletionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalGovernedSupportCompletion.lean` | For every saturated support-square corner, Lean computes its exact physical boundary, ordered interface, and a disjoint partition of selected profile coordinates among all ten terminal roles, while retaining closure and compatibility. | The dependency system remains explicit. This milestone alone does not prove frontier pushout, projection compatibility, BN2 square legitimacy, `SaturatePositive`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Governed terminal frontier pushout | `node --test audits/lean-residual-terminal-frontier-pushout0.test.mjs`, `lean-audit/PNPResidualTerminalFrontierPushoutAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFrontierPushout.lean` | For every computed saturated support square, Lean proves that the independently completed join frontier is exactly the canonical gluing of the two side frontiers over their meet profile. Physical wires that become internal are characterized explicitly. | The dependency system remains explicit. Projection compatibility, BN2 square legitimacy, `SaturatePositive`, obstruction routing, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Governed terminal projection square | `node --test audits/lean-residual-terminal-projection-square0.test.mjs`, `lean-audit/PNPResidualTerminalProjectionSquareAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalProjectionSquare.lean` | For every computed saturated support square and forgetful terminal projection, Lean preserves the physical frontier and proves that projected meet and join commute exactly with overlap and side-only pushout. | The dependency system remains explicit. Side-tight minima, BN2 square legitimacy, `SaturatePositive`, obstruction routing, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Side-tight four-corner minimum arithmetic | `node --test audits/lean-residual-terminal-side-tight-minimum0.test.mjs`, `lean-audit/PNPResidualTerminalSideTightMinimumAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSideTightMinimum.lean` | For every finite terminal four-corner family, typed full and quotient bases satisfy the signed four-slack identity. The fail-closed extractor returns the existing delta only when all four exact minima are attained; canonical independently attained minima pass. | Coherent four-corner completion, BN2 square legitimacy, tight-family maximization, `SaturatePositive`, `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Checked four-corner carrier transport | `node --test audits/lean-residual-terminal-four-corner-carrier0.test.mjs`, `lean-audit/PNPResidualTerminalFourCornerCarrierAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFourCornerCarrier.lean` | For every computed saturated terminal support square, all four exact extracted endpoints and canonical coordinate lists remain in one common ambient carrier. Profile coordinates transport exactly, while physical coordinates are computed as retained or internalized through fail-closed queries. | This does not transport four optimum realizers, prove `fourCornerOptimaCarrierCompatible`, construct a coherent four-corner optimum, establish BN2 square legitimacy, `SaturatePositive`, `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Candidate-derived terminal saturation cost balance | `node --test audits/lean-residual-terminal-saturation-cost-balance0.test.mjs`, `lean-audit/PNPResidualTerminalSaturationCostBalanceAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSaturationCostBalance.lean` | For every finite direct-wire candidate, physical and context-sensitive profile dependencies are derived without a caller-supplied relation. A deterministic rule-labelled trace either proves exact transparent cost balance through the linked history, preserving full slack and positivity, or returns the exact first nontransparent event with a typed reason. | This closes only the finite forms of `transparentSaturationCostBalanced` and `firstNontransparentStepRecorded`. `interfaceExposureRoutesToE`, `originKernelObligationClosureRouted`, full `SaturatePositive`, `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Finite terminal interface-exposure routing | `node --test audits/lean-residual-terminal-interface-exposure-routing0.test.mjs`, `lean-audit/PNPResidualTerminalInterfaceExposureRoutingAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalInterfaceExposureRouting.lean` | Candidate-derived interface-consumer events are classified as transparently balanced or routed to a proof-bearing local E coordinate. The trace classifier binds the route to the exact first nontransparent event and complete transparent prefix; outgoing-coordinate completion is an exact zero-cost retract. | This closes only the finite local form of `interfaceExposureRoutesToE`. The route is not Package E `VerifyDW`; `originKernelObligationClosureRouted`, full `SaturatePositive`, `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Finite terminal `SaturatePositive` composition | `node --test audits/lean-residual-terminal-finite-saturate-positive0.test.mjs`, `lean-audit/PNPResidualTerminalFiniteSaturatePositiveAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFiniteSaturatePositive.lean` | Exact candidate-derived origin, kernel, and obligation closures are checked for cost transparency, discharged obligations, and stability of forgotten profiles. Unsafe events carry deterministic proof-bearing reasons. Given an existing candidate BCEL anchor problem and positive initial full slack, the total composite preserves full positivity into the projection firewall or returns the exact first interface, closure, or fail-closed route. | This closes the finite local closure-routing edge and a proof-bearing composition of the five reconstructed terminal sub-obligations. Local routes are not the manuscript's global outcome map or Package E, and the positive premise is explicit. Full manuscript-wide `SaturatePositive`, `BCELReady`, RankWF, route completeness, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Checked finite `SaturatePositive`-to-BCEL-ready composition | `node --test audits/lean-residual-terminal-finite-bcel-ready0.test.mjs`, `lean-audit/PNPResidualTerminalFiniteBCELReadyAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFiniteBCELReady.lean` | One recomputed checker accepts exactly the positive-projection / ready-nucleus branch of the production finite classifier. Its proof-bearing result retains the exact selection equality, safe trace, positive final slack and whole-support defect, at-least-two anchor bound, exact proper-cut equation, and local BN2 conclusion. | The terminal problem and initial positive premise remain supplied. Local failures are not globally routed, and BN3--BN6 data, constant activation, manuscript-wide `SaturatePositive` or `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Same-candidate finite BCEL-ready and Packet carrier coherence | `node --test audits/lean-residual-terminal-finite-bcel-packet-carrier-coherence0.test.mjs`, `lean-audit/PNPResidualTerminalFiniteBCELPacketCarrierCoherenceAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFiniteBCELPacketCarrierCoherence.lean` | One proof-bearing certificate indexes the checked ready problem by the exact Packet certificate candidate and model, proves its anchor map injective and surjective, and reflects exact equality between the computed nucleus image and the grouped Packet carrier. The M183 size bound and M180 same-family Packet exclusion then rule out constant activation on the report-facing family. | The problem, initial positive premise, family, map, payloads, tables, and ranks remain supplied. Carrier identity does not identify activation weights with projection excess or derive constant activation from positive slack; unconditional ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Finite BCEL/Packet activation-coherence obstruction | `node --test audits/lean-residual-terminal-finite-bcel-packet-activation-obstruction0.test.mjs`, `lean-audit/PNPResidualTerminalFiniteBCELPacketActivationObstructionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalFiniteBCELPacketActivationObstruction.lean` | The checker compares the exact M183 terminal defect with the declared M184 Packet cut value and exhaustively with every canonical nonempty proper-cut activation weight. Hypothetical acceptance reconstructs the mapped projection equation; same-family Packet exclusion proves rejection, and the classifier returns a proof-bearing cut-value or first proper-cut mismatch. | This diagnoses rather than repairs the missing activation bridge. All terminal, family, map, activation, payload, table, and rank inputs remain supplied; exhaustive cut enumeration may be exponential. No unconditional ZeroSlack/PCCMin, polynomial-runtime, SAT-in-P, axiom-removal, root-theorem, or risk-weighted checkpoint claim is made. |
| Residual terminal `RankWF` | `node --test audits/lean-residual-terminal-rank-wf0.test.mjs`, `lean-audit/PNPResidualTerminalRankWFAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalRankWF.lean` | The manuscript's fixed ten natural coordinates are represented in their exact priority order. The nested `Prod.lex` relation has an executable Boolean projection, true/false equivalence proofs, coordinate-priority witnesses, accessibility and induction surfaces, and a kernel-checked well-foundedness theorem. | This establishes the rank domain and `RankWF` only. Current finite routes are not mapped to the global outcome system and are not proved to decrease the rank. Route completeness, Package E, full `SaturatePositive`, `BCELReady`, ZeroSlack/PCCMin, polynomial runtime, SAT in P, and `P = NP` remain open. |
| Candidate-derived finite BN3 request envelope | `node --test audits/lean-residual-terminal-bn3-request-envelope0.test.mjs`, `lean-audit/PNPResidualTerminalBN3RequestEnvelopeAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBN3RequestEnvelope.lean` | Every successful computed BCEL nucleus supplies one canonical duplicate-free request-atom list across every proper cut, exact executable monotone and transport-stable membership predicates, singleton minimal consumers, duplicate-free exact incidence, and one canonical full/quotient side-tight coherent basis selection function. The total wrapper preserves every upstream proof-bearing failure. | Proper cuts are enumerated through all subsets, so this is exponential finite reference computation. It does not derive the dependency system, map all routes into a decreasing global outcome system, construct the full historical BN4–BN6 chain, prove selector/realizer completeness, ZeroSlack/PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| BN3 joint-realizability gap witness | `lake env lean -DwarningAsError=true lean-regression/PNPBN3JointRealizabilityGap.lean`, `node --test audits/bn3-joint-realizability-gap0.test.mjs`, and `review/bn3_joint_realizability_gap.md` | A kernel-checkable two-cut model still proves that arbitrary per-cut basis existence alone cannot construct a cross-cut-stable realizing family. The new finite milestone repairs that inference only by deriving one canonical family from a successful computed BCEL nucleus. | The witness remains a sound boundary on the invalid generic inference. The candidate-derived repair is exponential and does not supply the full historical BN4–BN6 chain, global selector/realizer closure, ZeroSlack, polynomial PCCMin, SAT in P, or `P = NP`. |
| Finite BN4 activation-exact cancellation kernel | `node --test audits/lean-residual-terminal-bn4-activation-cancellation0.test.mjs`, `lean-audit/PNPResidualTerminalBN4ActivationCancellationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBN4ActivationCancellation.lean` | After a successful finite BN3 envelope, every request atom has a canonical singleton activation code whose equality is exactly activation-function equality. Complete typed keys retain the atom, explicit semantic signature, and explicit transport type. Per-key positive/negative natural mass is classified into a canonical balanced or one-sign residual with exact integer conservation, key preservation, and no opposite-sign residual pair. The total wrapper preserves all upstream failures and rejects foreign atoms. | The typed cell ledger, semantic signatures, and transport types are explicit inputs rather than derived from four-corner bases. This is not the full historical BN4 theorem, supplies no polynomial construction or size bound, and does not construct PkgC/BN6, complete global routes or selectors, prove ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Finite BN5 full-shadow localization kernel | `node --test audits/lean-residual-terminal-bn5-full-shadow-localization0.test.mjs`, `lean-audit/PNPResidualTerminalBN5FullShadowLocalizationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBN5FullShadowLocalization.lean` | For arbitrary finite explicit negative-unit refinements and quotient-shadow lists, complete coordinates preserve the BN4 key plus frontier, charge-owner, obligation, origin-kernel, and mode-projection data. A total classifier validates exact negative mass, computes cut silence, and returns complete multiplicity coverage or a proof-bearing strict Hall deficit routed to local X1. | Payloads and the shadow universe remain explicit finite inputs. Matching is not yet connected back to a BN4 contradiction, and this is not the full historical BN5 theorem. CritC/Q/E/L/X2-X4, PkgC/BN6, global routes and selectors, polynomial generation/runtime, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| Finite PkgC separating-consumer restoration dichotomy | `node --test audits/lean-residual-terminal-pkgc-separating-consumers0.test.mjs`, `lean-audit/PNPResidualTerminalPkgCSeparatingConsumersAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPkgCSeparatingConsumers.lean` | For an arbitrary finite explicit minimal-consumer antichain, the canonical scan either proves exactly V54's singletonization premise or selects the first disjoint nonsingleton pair, generates quotient units from its atoms, and classifies an explicit exact-coordinate restoration universe into complete multiplicity coverage or a strict Hall deficit with a deterministic local Q route. | The restoration universe remains explicit. Coverage is not connected to a BN4/BN5 contradiction, the local route is not embedded globally, and full PkgC route silence, terminal-candidate derivation, full BN6/Packet routing, polynomial runtime, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| Finite PkgC typed restoration realization | `node --test audits/lean-residual-terminal-pkgc-typed-restoration0.test.mjs`, `lean-audit/PNPResidualTerminalPkgCTypedRestorationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPkgCTypedRestoration.lean` | For an arbitrary finite explicit minimal-consumer antichain and typed coordinate-preserving restorer, Lean materializes one full candidate for every atom of the canonical first disjoint nonsingleton pair, proves exact candidate count and positional coordinates, derives complete equality-fibre multiplicity coverage, and rules out a strict Hall deficit for that graph. If no pair exists, it proves V54 singletonization. | The typed restoration operation and coordinate-preservation proof remain explicit inputs. This does not construct the operation from a terminal candidate, prove its full semantic adequacy, connect complete restoration to a BN4/BN5 contradiction, embed routes globally, establish full PkgC or BN6/Packet completeness, prove polynomial runtime, ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Finite PkgC same-key cancellation | `node --test audits/lean-residual-terminal-pkgc-same-key-cancellation0.test.mjs`, `lean-audit/PNPResidualTerminalPkgCSameKeyCancellationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPkgCSameKeyCancellation.lean` | The explicit typed restorer mechanically produces opposite-sign unit cells with identical nested BN4 keys, exact count, equal positive and negative mass at every key, empty executable residuals, and zero signed mass; exact cancellation silence implies V54 singletonization. | The restorer remains explicit, and this theorem alone does not bind the generated cells to an ambient candidate ledger, derive semantic restoration, complete global routes, prove polynomial runtime, ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Finite PkgC ambient BN4 ledger embedding | `node --test audits/lean-residual-terminal-pkgc-ambient-bn4-ledger0.test.mjs`, `lean-audit/PNPResidualTerminalPkgCAmbientBN4LedgerAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPkgCAmbientBN4Ledger.lean` | An exact permutation certificate identifies the generated balanced cells as an order-independent multiplicity-preserving subledger of an explicit ambient BN4 ledger plus remainder. Lean proves mass decomposition, exact remainder signed mass and residual contribution, candidate-derived canonical-atom linkage, and the corresponding silence-to-V54 implication. | The ambient ledger, restorer, certificate, and successful BN4 kernel are explicit proof-bearing inputs; they are not derived from the terminal candidate. Semantic restoration, complete global routes and silence, full PkgC/BN6/Packet completeness, polynomial runtime, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| Finite PkgC ambient BN4 residual reduction | `node --test audits/lean-residual-terminal-pkgc-ambient-bn4-residual-reduction0.test.mjs`, `lean-audit/PNPResidualTerminalPkgCAmbientBN4ResidualReductionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPkgCAmbientBN4ResidualReduction.lean` | Exact removal of the generated balanced subledger preserves every executable per-key residual cell and the complete residual ledger over the ambient canonical keys. Every remainder key is covered; a fail-closed classifier constructs the reduction mechanically; and an empty explicit remainder forces an empty ambient residual ledger. | The ambient ledger, restorer, embedding, and remainder remain explicit. This does not derive them from a terminal candidate, prove that the remainder is empty or route-producing, complete global routes or PkgC/BN6/Packet, establish polynomial runtime, prove ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| V54 consumer-antichain normal form | `node --test audits/lean-residual-terminal-consumer-antichain-normal-form0.test.mjs`, `lean-audit/PNPResidualTerminalConsumerAntichainNormalFormAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalConsumerAntichainNormalForm.lean` | For every arbitrary finite carrier and explicit minimal-consumer antichain, the induced request is monotone and empty-false; two-sided activation is nonzero exactly when the antichain has a disjoint pair. Under the exact PkgC singletonization premise, Lean proves literal equality with the Boolean cut indicator of the singleton footprint. | This closes the manuscript's unbounded V54 combinatorial edge, but consumes an explicit antichain. The adjacent finite PkgC classifier still consumes an explicit restoration universe and does not connect coverage to contradiction or embed its local route globally. Terminal-candidate derivation, full PkgC route silence, the V54-to-V53/BN6 payload connection, global selectors, polynomial runtime, ZeroSlack/PCCMin, SAT in P, and `P = NP` remain open. |
| V53 constant-cut hypergraph rigidity | `node --test audits/lean-residual-terminal-constant-cut-hypergraph-rigidity0.test.mjs`, `lean-audit/PNPResidualTerminalConstantCutHypergraphRigidityAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalConstantCutHypergraphRigidity.lean` | For an arbitrary finite duplicate-free carrier and sparse positive hyperedges, exact equality of every nonempty proper cut proves the full q=2, q=3, and q>=4 V53 classification: full-span weight `D`; equal pair weight `p` with `w_A + 2p = D`; or zero proper-footprint weight and full-span weight `D`. | This closes the manuscript's V53 constant-cut classification after an explicit V54 consumer antichain has become a hypergraph. It does not construct PkgC, derive the hypergraph from a terminal candidate, build BN6 cells or payloads, complete global routes or selectors, prove polynomial runtime, ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Finite BN6 hypergraph-packet bridge | `node --test audits/lean-residual-terminal-bn6-hypergraph-packet0.test.mjs`, `lean-audit/PNPResidualTerminalBN6HypergraphPacketAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBN6HypergraphPacket.lean` | For an arbitrary finite duplicate-free anchor carrier and explicit already-grouped positive payload-bearing survivor family, V54 activation becomes exactly the constructed V53 cut sum. A positive constant-cut premise yields the q=2 pair, q=3 mixed balanced-triple/full-span, or q>=4 full-span packet classification while retaining original payload witnesses. | This closes the finite explicit-group bridge only. It does not construct PkgC, derive or group survivors from a terminal candidate, establish the full historical BN6 or Packet selector/realizer route, prove polynomial generation/runtime, ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Finite Packet selector-seed extraction | `node --test audits/lean-residual-terminal-packet-selector-seeds0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorSeedsAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorSeeds.lean` | Every exact BN6 conclusion over an arbitrary finite family yields a raw payload-backed pair seed, a seed for every positive balanced-triple pair footprint, or a positive full-span seed. The mixed three-anchor alternative is preserved without assuming both masses are positive. | This closes seed-input extraction only. It does not prove selector-universe membership, faithfulness or compatibility, construct realizers or routes, establish enumeration or polynomial runtime, complete PkgC/ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Finite Packet payload-selector universe membership | `node --test audits/lean-residual-terminal-packet-selector-universe0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorUniverseAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorUniverse.lean` | Every explicit finite grouped BN6 family has one exact duplicate-free list of grouped footprints, and every raw payload-backed Packet seed belongs to that list while retaining its carrier, size, cell, and atom evidence. | This is an input-relative finite universe, not the manuscript's encoded or polynomial selector universe. It does not prove manuscript-level faithfulness or compatibility, construct a realizer or route, derive the grouped family, or establish polynomial bounds, ZeroSlack, PCCMin, SAT in P, or `P = NP`. |
| Canonical finite Packet selector handles | `node --test audits/lean-residual-terminal-packet-selector-handles0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorHandlesAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorHandles.lean` | Every exact grouped-footprint payload selector has a unique input-relative handle; decoding is injective and retains universe membership, carrier containment, length at least two, and original grouped cell-and-atom evidence across every Packet branch. | These list positions are not the manuscript's bit encoding or polynomial selector universe. They do not prove manuscript-level faithfulness or compatibility, construct a realizer or route, derive the grouped family, or establish polynomial bounds, ZeroSlack, PCCMin, SAT in P, or `P = NP`. |
| Canonical Packet selector-handle codec | `node --test audits/lean-residual-terminal-packet-selector-codec0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorCodecAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorCodec.lean` | Each input-relative handle has one unary code; the total decoder rejects missing delimiters, trailing bits, and out-of-range indices. Lean proves round trip, injectivity, canonical successful decoding, exact length, unique accepted codes, retained payload evidence, and all Packet branches. | The length bound is by the supplied explicit selector list, not encoded circuit size. This does not prove polynomial enumeration/runtime, encode atom or payload data, establish manuscript-level faithfulness or compatibility, construct a realizer or route, derive the family, complete PkgC/ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Total fail-closed Packet source-payload realization | `node --test audits/lean-residual-terminal-packet-selector-payload-realization0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorPayloadRealizationAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorPayloadRealization.lean` | Every accepted canonical code materializes its exact original grouped cell, decoded footprint, and a canonical original positive payload atom; rejection is exactly decoder rejection, and all Packet branches are preserved. | This is lookup relative to the supplied explicit family, not the manuscript's gain-or-blocker realizer. It does not serialize payload data, construct a replacement circuit or route, prove faithfulness/compatibility or polynomial bounds, complete PkgC/ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Checked Packet selector candidate-gain scan | `node --test audits/lean-residual-terminal-packet-selector-gain-scan0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorGainScanAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorGainScan.lean` | Every accepted canonical code scans all direct-wire implementation payloads in its exact original source cell. The proof-bearing result is either an original atom carrying a genuine strict equivalent gain and residual descent or exact absence of such a gain among that cell's candidates; every Packet branch is preserved. | Candidates and the grouped family are explicit inputs. Cell-local no-gain is not an HN, budget, or lower-rank manuscript blocker, global minimality, or ZeroSlack. This does not construct candidates, prove selector faithfulness/compatibility or polynomial bounds, complete PkgC/ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Exhaustive Packet selector-universe gain scan | `node --test audits/lean-residual-terminal-packet-selector-universe-gain-scan0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorUniverseGainScanAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorUniverseGainScan.lean` | Every canonical handle in an arbitrary finite explicit grouped family is enumerated, and every original payload in each exact source cell is checked. The result is a canonical source-atom strict equivalent gain with residual descent or proof of no such gain anywhere in that supplied selector universe; the complete Packet conclusion is retained literally. | The family and candidates remain explicit inputs. Family-wide no-gain is not manuscript selector silence, `BotHN`, `BotBUD`, a lower-rank `BotSeed`, global minimality, or ZeroSlack. This does not prove faithfulness/compatibility, construct replacements or blockers, connect charge surplus, establish encoded-size or polynomial bounds, complete PkgC/ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Finite Packet charge-surplus realizer kernel | `node --test audits/lean-residual-terminal-packet-charge-surplus0.test.mjs`, `lean-audit/PNPResidualTerminalPacketChargeSurplusAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketChargeSurplus.lean` | Over arbitrary finite support and replacement charge ledgers, exact multiplicity-preserving occurrence matching, pairwise weight preservation, and an unmatched positive support charge derive strict total weight. Exact NAND gate accounting and separately proved semantic equivalence then construct a genuine strict equivalent gain and residual descent. | The ledgers, pairing, gate accounting, and semantics remain explicit inputs. This does not construct a replacement or its ledger from Packet or terminal data, prove selector faithfulness or typed blockers, close HB/rank routing, establish polynomial bounds, or complete global PkgC, ZeroSlack, or PCCMin. It is not unconditional ZeroSlack and does not put SAT in P or prove `P = NP`. |
| Checked Packet unit-charge blueprint realizer | `node --test audits/lean-residual-terminal-packet-unit-charge-blueprint-realizer0.test.mjs`, `lean-audit/PNPResidualTerminalPacketUnitChargeBlueprintRealizerAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketUnitChargeBlueprintRealizer.lean` | A constructive exact-occurrence validator derives canonical unit-charge gate ledgers from each supplied replacement blueprint, checks semantic equivalence, constructs the charge-surplus realization and genuine strict gain, and scans every original blueprint atom behind every canonical handle in one supplied explicit family. The complete Packet conclusion is retained. | The family, candidates, pairings, and blueprints remain explicit inputs. Supplied-family validator silence is not `BotHN`, `BotBUD`, a lower-rank `BotSeed`, global no-gain, or ZeroSlack. This does not prove selector faithfulness/compatibility, derive terminal inputs, close HB/rank routing, establish polynomial bounds, complete PkgC/PCCMin, put SAT in P, or prove `P = NP`. |
| Checked Packet typed-realizer contract | `node --test audits/lean-residual-terminal-packet-typed-realizer-contract0.test.mjs`, `lean-audit/PNPResidualTerminalPacketTypedRealizerContractAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketTypedRealizerContract.lean` | Over arbitrary finite selector lists and supplied finite-rank executable tables, every faithful row is accepted only as a checked unit-charge gain, active same-or-lower-rank HN bot, active same-or-lower-rank budget bot, or faithful strictly lower-rank seed bot. The grouped-BN6 specialization covers every canonical handle. | The family, ranks, faithfulness predicate, claims, and blocker tables remain explicit inputs. Finite indices are not the manuscript rank tuple. This does not construct blockers or blueprints from terminal data, establish selector compatibility, blocker semantics, HB acyclicity, global silence, polynomial runtime, ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Checked exact-rank HB blocker-graph acyclicity | `node --test audits/lean-residual-terminal-hb-blocker-graph-acyclicity0.test.mjs`, `lean-audit/PNPResidualTerminalHBBlockerGraphAcyclicityAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHBBlockerGraphAcyclicity.lean` | Over arbitrary finite supplied HN/BUD edge graphs, every strict finite-index comparison and every dependency edge must descend the exact ten-coordinate residual rank. Acceptance derives accessibility, well-foundedness, and absence of every nonempty directed cycle, and composes with the canonical typed-realizer table. | The graph, edges, rank mapping, blocker semantics, and dependency completeness remain supplied or open. This does not construct the graph from terminal data, establish rank-complete selector silence or the full HB negative closure, prove polynomial bounds, complete ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Checked total-table HB dependency closure | `node --test audits/lean-residual-terminal-hb-dependency-table-closure0.test.mjs`, `lean-audit/PNPResidualTerminalHBDependencyTableClosureAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHBDependencyTableClosure.lean` | Every HN and budget node at every finite rank has one total data-only dependency row. The graph is materialized from all rows, and every listed dependency must descend the exact ten-coordinate rank. Acceptance proves exact row-to-edge coverage, well-founded rank induction with an explicit local premise, cycle exclusion, covered HN/BUD typed-bot rows, and exact lower-seed descent. | The table, rank mapping, blocker semantics, semantic dependency completeness, and local invariant premise remain explicit or open. This does not construct the table from terminal data, silence active blockers, establish rank-complete selector silence or the full HB negative closure, prove polynomial bounds, complete ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Checked HB active-dependency closure | `node --test audits/lean-residual-terminal-hb-active-dependency-closure0.test.mjs`, `lean-audit/PNPResidualTerminalHBActiveDependencyClosureAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHBActiveDependencyClosure.lean` | Every active supplied HN/BUD node must have an active dependency in its own total row, while every row dependency independently descends the exact ten-coordinate rank. Well-founded induction proves all supplied HN/BUD activity false and typed-realizer composition eliminates HN/budget bot branches, leaving a checked gain or faithful lower seed. | Activity bits, rows, rank mapping, selector data, blocker semantics, and semantic dependency completeness remain supplied or open. This does not derive the tables from terminal data, exclude gains or lower seeds, establish rank-complete selector silence or the full HB negative closure, prove polynomial bounds, complete ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Conditional HB selector-silence rank closure | `node --test audits/lean-residual-terminal-hb-selector-silence-closure0.test.mjs`, `lean-audit/PNPResidualTerminalHBSelectorSilenceClosureAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHBSelectorSilenceClosure.lean` | Checked HN/BUD inactivity plus explicit global semantic gain exclusion leaves only faithful strictly lower-rank seeds; strong induction proves every canonical handle in the accepted supplied table nonfaithful. The gain-coverage specialization obtains global exclusion from an explicit certificate and exact source-cell no-gain. | The family, ranks, faithfulness, realizer claims, blocker tables, and gain exclusion or coverage certificate remain supplied. This does not establish selector faithfulness or compatibility, derive the interface from terminal data, prove unconditional HB negative closure or ZeroSlack, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Executable selector-silence induction | `node --test audits/lean-residual-terminal-hb-executable-selector-silence-induction0.test.mjs`, `lean-audit/PNPResidualTerminalHBExecutableSelectorSilenceInductionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHBExecutableSelectorSilenceInduction.lean` | An exhaustive checker proves every canonical realizer claim is a typed bottom. Checked HB closure removes HN/BUD bottoms, and strong finite-rank induction removes faithful lower seeds, so no global semantic no-gain premise is required. | The family, ranks, faithfulness, claims, activity and dependency tables remain explicit terminal-relative data. This does not construct them from terminal data, establish selector faithfulness or compatibility, prove the full unconditional HB negative closure or ZeroSlack, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Packet selector-faithfulness routing and HB contradiction | `node --test audits/lean-residual-terminal-packet-selector-faithfulness-routing0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorFaithfulnessRoutingAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorFaithfulnessRouting.lean` | Ten data-only payload fields are checked in a fixed first-failure order for every canonical handle and bound exactly to the supplied HB table. Every positive Packet yields a faithful handle, contradicting accepted executable selector silence. | The grouped family, payload checks, ranks, HB binding, claims, activity, and dependencies remain explicit inputs. This does not derive positive slack, `SaturatePositive`, `BCELReady`, terminal data, complete route silence, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Canonical Packet faithfulness-table construction | `node --test audits/lean-residual-terminal-packet-selector-faithfulness-table0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorFaithfulnessTableAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorFaithfulnessTable.lean` | The typed-realizer table is rebuilt with faithfulness computed from each canonical positive source payload; rank, claims, and HN/BUD activity are preserved exactly. Binding accepts by construction, so the Packet-to-HB contradiction no longer needs an independent binding premise. | Payload checks, rank assignment, grouped-family derivation, claims, blocker semantics, activity and dependency tables remain explicit or open. This is not full external selector compatibility, terminal-data construction, complete route silence, unconditional HB negative closure or ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Total Packet selector first-route outcome | `node --test audits/lean-residual-terminal-packet-selector-first-route-outcome0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorFirstRouteOutcomeAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorFirstRouteOutcome.lean` | Payload rejection is equivalent to a concrete earliest typed first route. Every positive Packet under the canonical table and accepted executable HB selector silence yields such a route without route-clear or binding premises. | Payload fields and all terminal-relative tables remain explicit. This does not prove route semantics or decreasing global coverage, construct terminal data, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Exact Packet first-route failure semantics | `node --test audits/lean-residual-terminal-packet-selector-first-route-semantics0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSelectorFirstRouteSemanticsAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSelectorFirstRouteSemantics.lean` | Each of the ten route values is equivalent to its exact earliest failed supplied field; failure is unique, and the canonical positive-Packet/HB endpoint retains the field proof alongside the route. | This interprets supplied Boolean payload fields only. It does not derive them from terminal data, prove their external manuscript semantics, establish decreasing global coverage, close unconditional HB negative closure or ZeroSlack/PCCMin, put SAT in P, or prove `P = NP`. |
| Rank-reflected Packet descent route | `node --test audits/lean-residual-terminal-packet-descent-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketDescentRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketDescentRouteReflection.lean` | The final payload field is computed from the exact ten-coordinate `RankWF` comparison. A forced positive-Packet/HB failure is either an earlier exact field route or proof that the supplied transition is nondecreasing. | The first nine fields and all rank, family, claim, activity, and dependency data remain explicit. This does not map the other nine routes globally, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Canonical Packet rank-tag route reflection | `node --test audits/lean-residual-terminal-packet-rank-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketRankRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketRankRouteReflection.lean` | The payload rank tag is copied from the authoritative handle rank while exact residual descent remains computed. The first route cannot be `.rank`; a final `.descent` route proves nondecrease. | The rank map, residual ranks, seven earlier Booleans, exact-route field, family, claims, activity, and dependencies remain explicit. This does not map the eight remaining routes globally, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Canonical Packet exact-route reflection | `node --test audits/lean-residual-terminal-packet-exact-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketExactRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketExactRouteReflection.lean` | The canonical handle-to-cell-to-positive-payload source route is marked clear by construction while the authoritative rank and computed residual descent are retained. The first route can return neither `.exactRoute` nor `.rank`; a final `.descent` route proves nondecrease. | This internal route is not an external exact minimum. The seven semantic fields, rank map, residual ranks, family, claims, activity, and dependencies remain explicit. This does not map the seven remaining routes globally, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Canonical Packet charge-route reflection | `node --test audits/lean-residual-terminal-packet-charge-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketChargeRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketChargeRouteReflection.lean` | The canonical source atom's proved positive mass replaces the caller charge bit while the source route, authoritative rank, and computed residual descent are retained. The first route can return none of `.charge`, `.exactRoute`, or `.rank`; a final `.descent` route proves nondecrease. | Positive source mass is not full charge-surplus or replacement semantics. Six semantic fields, rank map, residual ranks, family, claims, activity, and dependencies remain explicit. This does not map the six remaining routes globally, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Canonical Packet colour-route reflection | `node --test audits/lean-residual-terminal-packet-colour-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketColourRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketColourRouteReflection.lean` | Canonical grouped-footprint eligibility replaces the caller colour bit while positive charge, the internal source route, authoritative rank, and computed residual descent are retained. The first route can return none of `.colour`, `.charge`, `.exactRoute`, or `.rank`; a final `.descent` route proves nondecrease. | The internal size check plus carrier-sublist evidence is not full external manuscript colour equivalence. Five semantic fields, rank map, residual ranks, family, claims, activity, and dependencies remain explicit. This does not map the five remaining routes globally, close unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Canonical Packet typed-frontier route reflection | `node --test audits/lean-residual-terminal-packet-frontier-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketFrontierRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketFrontierRouteReflection.lean` | Executable equality of explicit typed source and selector frontier signatures replaces the caller frontier bit. A `.frontier` result carries exact signature inequality; `.colour`, `.charge`, `.exactRoute`, and `.rank` remain excluded, and `.descent` carries nondecrease. | The supplied signatures are not constructed from terminal data or bound to the manuscript BN5 frontier. Four semantic fields and routes remain explicit. This does not close global route silence, unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| BN5-bound Packet frontier and obligation route reflection | `node --test audits/lean-residual-terminal-packet-bn5-obligation-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBN5ObligationRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBN5ObligationRouteReflection.lean` | Exact equality of the typed terminal BN5 coordinate's frontier and obligation fields replaces both caller bits. A `.frontier` route carries frontier inequality; an `.obligation` route carries prior frontier equality plus obligation inequality. Canonical colour, charge, exact route, and rank remain excluded, and `.descent` carries nondecrease. | The BN5 coordinates remain explicit rather than terminal-derived. Activation, direction, and budget are the three remaining fields and routes. This does not close global route silence, unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| BN4 activation-exact Packet route reflection | `node --test audits/lean-residual-terminal-packet-bn4-activation-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBN4ActivationRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBN4ActivationRouteReflection.lean` | Equality of the nested BN4 activation atoms replaces the caller activation bit and is proved equivalent to equality of their canonical activation predicates on every cut. An `.activation` route carries prior frontier and obligation equality plus activation-atom inequality. Canonical colour, charge, exact route, and rank remain excluded, and `.descent` carries nondecrease. | The BN5 coordinates remain explicit rather than terminal-derived. Direction and budget are the two remaining fields and routes. This does not close global route silence, unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Typed Packet direction-route reflection | `node --test audits/lean-residual-terminal-packet-direction-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketDirectionRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketDirectionRouteReflection.lean` | Exact equality of explicit typed source and selector direction values replaces the caller direction bit. A `.direction` route carries prior frontier, obligation, and activation equality plus typed-direction inequality. Canonical colour, charge, exact route, and rank remain excluded, and `.descent` carries nondecrease. | The direction values remain explicit rather than terminal-derived and are not a complete construction of manuscript `Dir(u)`. Budget is the sole remaining supplied Boolean field and route. This does not close global route silence, unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Typed Packet budget-route reflection | `node --test audits/lean-residual-terminal-packet-budget-route-reflection0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBudgetRouteReflectionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBudgetRouteReflection.lean` | Exact equality of explicit typed source and selector budget values replaces the last caller Boolean. A `.budget` route carries prior frontier, obligation, activation, and direction equality plus typed-budget inequality. Canonical colour, charge, exact route, and rank remain excluded, and `.descent` carries nondecrease. | The budget values remain explicit rather than terminal-derived and are not a complete construction of manuscript `Bud(u)`, its budget envelope, BudgetResolve, or HB budget activity. Computing every local Packet field does not close global route silence, unconditional HB negative closure or ZeroSlack/PCCMin, establish polynomial bounds, put SAT in P, or prove `P = NP`. |
| Checked Packet budget/HB activity binding | `node --test audits/lean-residual-terminal-packet-budget-hb-activity-binding0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBudgetHBActivityBindingAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBudgetHBActivityBinding.lean` | An exhaustive checker maps every typed budget mismatch to HB budget activity at the table-owned handle rank. Combined with the supplied checked well-founded HB closure, it forces typed budget equality and excludes `.budget`, leaving frontier, obligation, activation, direction, or exact residual nondecrease. | The binding, rank map, activity environment, dependency table, and closure premise remain explicit rather than constructed from terminal data. This is not BudgetResolve, blocker semantic completeness, full Packet adequacy, remaining-route exclusion, unconditional HB negative closure or ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Checked Packet semantic/HN activity binding | `node --test audits/lean-residual-terminal-packet-semantic-hn-activity-binding0.test.mjs`, `lean-audit/PNPResidualTerminalPacketSemanticHNActivityBindingAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketSemanticHNActivityBinding.lean` | An exhaustive checker maps any frontier, obligation, activation, or direction mismatch to HN activity at the table-owned handle rank. Checked HB closure forces those four fields to agree; composed with the separate budget/HB binding and selector silence, it leaves the sole exact residual-nondecrease route. | The semantic/HN binding, typed fields, rank map, activity environment, dependency table, and closure premise remain explicit rather than constructed from terminal data. This is not HN blocker semantics, semantic dependency completeness, a decreasing transition, a no-lower contradiction, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Checked Packet descent no-lower binding | `node --test audits/lean-residual-terminal-packet-descent-no-lower-binding0.test.mjs`, `lean-audit/PNPResidualTerminalPacketDescentNoLowerBindingAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketDescentNoLowerBinding.lean` | An exhaustive checker recognizes exactly when no canonical handle's fully computed Packet first route is `.descent`. Under the checked positive-Packet, semantic/HN, budget/HB, selector-silence, and HB-closure premises, that local no-lower row is forced to reject; accepting it yields a contradiction. | This closes one local Packet no-lower row over supplied data, not the complete manuscript ledger. It does not construct terminal inputs, cover HResolve, BudgetResolve, normalization, named routes, saturation, or replay, establish unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Checked Packet no-lower ledger | `node --test audits/lean-residual-terminal-packet-no-lower-ledger0.test.mjs`, `lean-audit/PNPResidualTerminalPacketNoLowerLedgerAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketNoLowerLedger.lean` | One executable ledger recomputes the semantic/HN binding, budget/HB binding, selector-silence, HB no-outcome closure, and Packet descent/no-lower rows. Its exact reflection theorem makes acceptance equivalent to those five checks, and an accepted ledger excludes a positive Packet over every supplied arbitrary finite grouped family. | This closes only the Packet branch, not the complete no-lower ledger. Terminal data and bindings remain supplied; HResolve, BudgetResolve, normalization, other named obstructions, saturation, replay, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, and `P = NP` remain open. |
| Checked finite HResolve coverage ledger | `node --test audits/lean-residual-terminal-hresolve-coverage-ledger0.test.mjs`, `lean-audit/PNPResidualTerminalHResolveCoverageLedgerAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHResolveCoverageLedger.lean` | A fixed-priority classifier generates exact, gain, blocked, or unresolved rows for every candidate in an arbitrary finite supplied family. The NoHereditary checker recomputes duplicate-free enumeration and all-candidate blocked coverage; accepted sidecar data excludes exact and gain routes for every listed candidate. | The candidate family and decidable predicates remain supplied. This is not terminal-derived HN grammar, BWL exactness, H-disjointness, exact-minimum/gain or blocker semantics, full HResolve, BudgetResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Terminal-derived HResolve support resolver | `node --test audits/lean-residual-terminal-hresolve-support-resolver0.test.mjs`, `lean-audit/PNPResidualTerminalHResolveSupportResolverAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHResolveSupportResolver.lean` | For every finite direct-wire candidate, the complete duplicate-free canonical terminal support-seed family is constructed mechanically. Candidate-derived saturation and actual residual slack give every governed seed either an exact route with semantic-minimum evidence or a gain route with a strict-equivalent-gain witness. | Subset enumeration and reference minimization are exhaustive and may be exponential. This is not HN grammar, BWL, ParseOrExit, H-disjointness, blocker semantics or NoHereditary, polynomial HResolve, BudgetResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Terminal HResolve maximal H-disjoint family | `node --test audits/lean-residual-terminal-hresolve-maximal-h-disjoint-family0.test.mjs`, `lean-audit/PNPResidualTerminalHResolveHDisjointFamilyAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHResolveHDisjointFamily.lean` | For every arbitrary finite duplicate-free supplied footprint family, Lean checks all eight H-disjointness domains, constructs a governed duplicate-free pairwise H-disjoint maximal family, and gives every rejected candidate a selected blocker with an exact first interference route. | The footprints remain supplied. This is not HN grammar, BWL, ParseOrExit, leaf tightness, the full `NoHereditary` sidecar or HResolve, HB blocker semantics, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, a polynomial algorithm, SAT in P, or `P = NP`. |
| Terminal HN BWL certified-path minimum | `node --test audits/lean-residual-terminal-hn-bwl-certified-path-minimum0.test.mjs`, `lean-audit/PNPResidualTerminalHNBWLCertifiedPathMinimumAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHNBWLCertifiedPathMinimum.lean` | For every nonempty finite supplied family of certified hereditary paths, Lean computes the exact four-coordinate lexicographic minimum, proves it is listed and lower-bounds every governed alternative under an explicit family-completeness premise, and preserves semantic, frontier, block, and shape evidence. | The paths and family completeness remain supplied. This is not terminal path derivation, grammar completeness, LN confluence, ParseOrExit, leaf tightness, the full BWL theorem or HResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, a polynomial algorithm, SAT in P, or `P = NP`. |
| Terminal HResolve certified-path family | `node --test audits/lean-residual-terminal-hresolve-certified-path-family0.test.mjs`, `lean-audit/PNPResidualTerminalHResolveCertifiedPathFamilyAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHResolveCertifiedPathFamily.lean` | For every duplicate-free finite supplied family of proof-bearing hereditary candidates, Lean constructs a maximal pairwise H-disjoint selected family, gives every selected candidate an exact four-coordinate certified-path minimum with footprint coherence, and gives every rejected candidate a selected blocker with an exact first interference route. | The candidates, paths, footprints, governed predicates, completeness proofs, and coherence proofs remain supplied. This is not terminal candidate derivation, HN grammar completeness, LN confluence, ParseOrExit, leaf tightness, the H0-H4 sidecar, full or polynomial HResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Proof-bearing HResolve ZeroSlack sidecar | `node --test audits/lean-residual-terminal-hresolve-zeroslack-sidecar0.test.mjs`, `lean-audit/PNPResidualTerminalHResolveZeroSlackSidecarAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalHResolveZeroSlackSidecar.lean` | The report-facing ZeroSlack certificate consumes a checked finite `NoHereditary` sidecar rather than string handles. Acceptance recomputes duplicate-free total blocked coverage, excludes exact and gain routes for every governed candidate, and retains semantic minimum and strict equivalent gain meanings for those constructive predicates. | The governed family, implementation map, route predicates, decidability witnesses, and blocker semantics remain supplied. This is not terminal candidate derivation, H0-H4 blocker semantics, full or polynomial HResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Proof-bearing Budget ZeroSlack sidecar | `node --test audits/lean-residual-terminal-budget-zeroslack-sidecar0.test.mjs`, `lean-audit/PNPResidualTerminalBudgetZeroSlackSidecarAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBudgetZeroSlackSidecar.lean` | The report-facing ZeroSlack certificate consumes a checked terminal-derived `NoBudget` sidecar rather than string handles. A failed exhaustive search excludes every canonical support from the supplied envelope, while exact and gain routes retain semantic minimum and strict equivalent gain meanings. | The caps remain supplied, and enumeration, saturation, and reference minimization may be exponential. This is not the BUD grammar or B0-B4 sidecar, full or polynomial BudgetResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Proof-bearing Selector/HB ZeroSlack sidecar | `node --test audits/lean-residual-terminal-selector-hb-zeroslack-sidecar0.test.mjs`, `lean-audit/PNPResidualTerminalSelectorHBZeroSlackSidecarAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalSelectorHBZeroSlackSidecar.lean` | The report-facing ZeroSlack certificate consumes the exact executable selector-silence and ranked HB-closure equations rather than eight strings. Reflection and strong finite-rank induction prove all canonical selectors nonfaithful, all claims typed bottoms, all HN/BUD activity false, and dependency well-foundedness. | The grouped family, realizer and dependency tables, environment, claims, activity bits, and rank map remain supplied. This is not blocker semantics, semantic dependency completeness, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Proof-bearing Packet/budget no-lower ZeroSlack sidecar | `node --test audits/lean-residual-terminal-packet-budget-no-lower-zeroslack-sidecar0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBudgetNoLowerZeroSlackSidecarAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBudgetNoLowerZeroSlackSidecar.lean` | The report-facing ZeroSlack certificate consumes the exact executable same-candidate Packet/budget composition equation rather than a no-lower string. Reflection proves every governed budget-feasible support semantic minimum, excludes every such strict equivalent gain, and excludes a positive Packet conclusion for the supplied family. | The caps, candidate-derived model, grouped family, typed payloads, ranks, claims, activity, dependencies, and rank maps remain supplied. This is the existing finite two-branch boundary, not the complete manuscript no-lower ledger, unconditional ZeroSlack/PCCMin, polynomial bounds, SAT in P, or `P = NP`. |
| Proof-bearing BCEL/Packet no-lower ZeroSlack sidecar | `node --test audits/lean-residual-terminal-bcel-packet-no-lower-zeroslack-sidecar0.test.mjs`, `lean-audit/PNPResidualTerminalBCELPacketNoLowerZeroSlackSidecarAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBCELPacketNoLowerZeroSlackSidecar.lean` | A dependent certificate binds the exact M180 no-lower evidence to its same grouped family and reflects a decidable at-least-two-anchor check. BCEL constant activation would then construct a positive BN6 Packet, contradicting the accepted same-family Packet exclusion. | The family and all M180 inputs remain supplied. This does not derive the family or BCEL constant activation from positive residual slack, construct BCELReady, complete all no-lower rows, establish unconditional ZeroSlack/PCCMin, prove polynomial bounds, put SAT in P, or prove `P = NP`. |
| Same-family Selector/HB, Packet, and BCEL ZeroSlack coherence | `node --test audits/lean-residual-terminal-zeroslack-packet-selector-hb-coherence0.test.mjs`, `lean-audit/PNPResidualTerminalZeroSlackPacketSelectorHBCoherenceAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalZeroSlackPacketSelectorHBCoherence.lean` | The report-facing Selector/HB sidecar is derived from the exact grouped family, computed realizer table, and dependency table already accepted by M180. Its selector silence, HB closure, Packet exclusion, and dependent BCEL contradiction therefore share one proof-bearing family boundary. | The family and all terminal, budget, Packet, realizer, dependency, rank, and BCEL inputs remain supplied. This does not derive them or BCEL constant activation from positive residual slack, establish unconditional ZeroSlack/PCCMin, prove polynomial bounds, put SAT in P, or prove `P = NP`. |
| Terminal budget-envelope resolver | `node --test audits/lean-residual-terminal-budget-envelope-resolver0.test.mjs`, `lean-audit/PNPResidualTerminalBudgetEnvelopeResolverAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBudgetEnvelopeResolver.lean` | For every finite direct-wire candidate and supplied gate/record caps, an exhaustive scan of the canonical terminal support universe recomputes nonempty gate and interface feasibility after candidate-derived saturation. A feasible support carries either semantic-minimum or strict-equivalent-gain evidence; search failure excludes every canonical seed and supplies the strong `NoBudget` branch. | The caps remain supplied, and subset enumeration, saturation, and reference minimization may be exponential. This is not the manuscript HN/BUD grammar, BWL or budget-envelope dynamic program, blocker semantics, polynomial BudgetResolve, the complete no-lower ledger, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Terminal budget no-lower ledger | `node --test audits/lean-residual-terminal-budget-no-lower-ledger0.test.mjs`, `lean-audit/PNPResidualTerminalBudgetNoLowerLedgerAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalBudgetNoLowerLedger.lean` | Every canonical terminal support is classified from the recomputed envelope and actual residual slack as exact, strict gain, or `NoBudget`. One exhaustive Boolean ledger accepts exactly when every budget-feasible governed support is a semantic minimum and excludes every feasible strict-equivalent-gain witness. | The caps remain supplied, and enumeration, saturation, and reference minimization may be exponential. This closes only the terminal-derived budget branch, not polynomial BudgetResolve, Packet or complete no-lower composition, unconditional ZeroSlack/PCCMin, SAT in P, or `P = NP`. |
| Terminal Packet-budget no-lower composition | `node --test audits/lean-residual-terminal-packet-budget-no-lower-composition0.test.mjs`, `lean-audit/PNPResidualTerminalPacketBudgetNoLowerCompositionAxiomAudit.lean`, and `lean-regression/PNPResidualTerminalPacketBudgetNoLowerComposition.lean` | One Boolean recomputes the canonical terminal budget-support ledger and the five-row Packet ledger over the same direct-wire candidate. Acceptance makes every budget-feasible canonical support a semantic minimum, excludes its feasible strict gains, and excludes a positive Packet over the supplied family. | The caps, Packet family, typed payloads, ranks, realizer claims, activity environment, and dependency rows remain supplied. This is a finite two-branch composition, not the complete no-lower ledger, unconditional ZeroSlack/PCCMin, a polynomial algorithm, SAT in P, or `P = NP`. |
| Archive integrity | `npm run legacy:v0:check` | Three annotated-tag identities and the pinned release digests match the archive manifest. | Signed provenance, theorem correctness, or checker soundness. |
| Historical checker replay | `npm run legacy:v0:replay -- --output /tmp/pnp-legacy-v0-7072f8d` | The pinned legacy implementation and selected tests reproduce their recorded behavior outside the active checkout. | Current theorem status, independent checker soundness, or validation of every mathematical implication. |
| Release checksums | `SHA256SUMS` and `SHA256SUMS.sha256` | Published artefact bytes match the sealed ledger. | Correctness of the artefact contents. |
| Independent audit | Reviewer derivations, counterexamples, clean-room checkers, and reproduction logs | Evidence about mathematics, checker soundness, complexity, and provenance at the audited boundary. | Broader claims outside the audit's stated scope. |

## Frozen release coordinates

```text
source tag:      final-pnp-proof-report-hardened-7072f8d
source commit:   7072f8d0bda6d44d240f9bb3fad624fd357e1278
artefact tag:    final-pnp-proof-report-artifacts-hardened-7072f8d-sealed
artefact commit: 9d1de19f827e5cb6880741352eb2349cbbb45994
artefact path:   proof-artifacts/final-pnp-proof-report-hardened-7072f8d/
archive manifest: archive/legacy-v0/ARCHIVE.json
```

The current canonical [PDF](./canonical_proof_report.pdf) and
[TeX](./canonical_proof_report.tex) form a generated, concise reconstruction report. It
records the false concrete publication gate and does not state an established `P = NP` theorem.
The historical 56-page claim manuscript is retained only at the pinned legacy source coordinate
recorded by [`archive/legacy-v0/`](./archive/legacy-v0/README.md).

## Reviewer map

- [Compiled Lean theorem inventory](./docs/lean_theorem_inventory.md): current deterministic environment inventory, concrete publication gate, milestone bindings, and generated-report boundary.
- [Formal reconstruction notice](./docs/FORMAL_RECONSTRUCTION.md): current authority, earned scope, blockers, and nonclaims.
- [Reviewer guide](./docs/reviewer_guide.md): historical checker-route overview, audit paths, and fast falsification checklist.
- [Proof pipeline](./docs/proof_pipeline.md): historical proposed mathematical route, executable evidence route, and hidden-search risks.
- [Terminology crosswalk](./docs/terminology_crosswalk.md): historical-report definitions and standard-language mappings for bespoke terms.
- [Trust model](./docs/trust_model.md): historical mathematical, parser, checker, runtime, build, seal, report, and website trust boundaries.
- [Audit questions](./docs/audit_questions.md): historical claim-by-claim worksheet with concrete refutation criteria.
- [Reproducibility protocol](./docs/reproducibility.md): fresh-clone, checksum, pinned-test, regeneration, and comparison instructions.
- [Minimal examples](./examples/minimal/README.md): eight small accepted/rejected demonstrations.
- [External review status](./EXTERNAL_REVIEW_STATUS.md): public record of substantive feedback and what has not been independently verified.
- [Lean direct-wire NAND semantics](./docs/lean_nand_semantics.md): exact scope of the axiom-free Boolean semantics milestone.
- [Lean concrete machine and cost kernel](./docs/lean_concrete_machine.md): executable codecs, finite rule-list semantics, bounded execution, runtime witnesses, and exact nonclaims.
- [Lean blank-delimited tape output](./docs/lean_tape_handoff.md): observable first-blank output semantics, the pure canonical handoff target, and the remaining executable-handoff boundary.
- [Lean boundary-marked pipeline tape geometry](./docs/lean_pipeline_tape_geometry.md): two-track data/marker framing, arbitrary exterior garbage, and proved local boundary expansion.
- [Lean executable all-input framer](./docs/lean_pipeline_input_framer.md): a literal finite machine from every raw bitstring to a represented frame, with exact branch costs and a uniform compiled raw polynomial.
- [Lean executable internal output handoff](./docs/lean_pipeline_output_handoff.md): a literal finite machine from an already represented tape to its represented blank-delimited handoff target, with exact linear work and compiled budgets.
- [Lean local framed machine simulation](./docs/lean_pipeline_machine_simulation.md): ordered first-match exact-run lifting, `k ≤ F` prefix extraction, and conditional designated-halting padding at work fuel `3 * F` and compiled fuel `18 * F` from an already represented start.
- [Lean literal Cook–Levin input-length tally](./docs/lean_cook_levin_builder_input_length.md): fixed-rule input preservation, exact unary tally, exact work/raw polynomials, timeout behavior, and the total-framer endpoint connection.
- [Lean executable Cook–Levin builder input prefix](./docs/lean_cook_levin_builder_input_prefix.md): collision-free framer/tally composition, literal launch execution, exact cumulative trace, external raw polynomial, and fail-closed timeout behavior.
- [Lean standalone Cook–Levin builder token appender](./docs/lean_cook_levin_builder_token_appender.md): four-token finite control, exact append/rewind execution, first canonical formula bits, external raw bound, and fail-closed malformed/short-fuel behavior.
- [Lean composed Cook–Levin first-token prefix](./docs/lean_cook_levin_builder_first_token_prefix.md): literal 184-rule raw-input composition, exact bridge and final tape, first canonical token bits, external quadratic bound, and fail-closed negative cases.
- [Lean composed Cook–Levin complete width header](./docs/lean_cook_levin_builder_complete_header.md): literal unary width-polynomial evaluation, collision-free five-component composition, exact complete header bits, external polynomial fuel, and fail-closed boundaries.
- [Lean composed Cook–Levin body-start prefix](./docs/lean_cook_levin_builder_body_start_prefix.md): complete-header composition with a unary next-slot evaluator and fixed separator appender, exact retained coordinate and token bits, external polynomial fuel, and fail-closed boundaries.
- [Lean composed Cook–Levin first-literal prefix](./docs/lean_cook_levin_builder_first_literal_prefix.md): body-start composition with a unary next-slot evaluator and fixed sign/zero appenders, a constructive positive-variable-zero proof, exact retained coordinate and token bits, external polynomial fuel, and fail-closed boundaries.
- [Lean composed Cook–Levin complete first-clause prefix](./docs/lean_cook_levin_builder_first_clause_prefix.md): first-literal composition with a unary next-slot evaluator and a fixed eight-token tail, constructive positive-variable-zero/one/two semantics, exact retained coordinate and complete clause bits, external polynomial fuel, and fail-closed boundaries.
- [Lean literal Cook–Levin token-cursor padding step](./docs/lean_cook_levin_builder_dynamic_token_cursor_step.md): complete-first-clause composition with one total launch and a fixed 45-rule cursor advance, exact valid-padding semantics, retained coordinate advancement, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin first-clause remaining-padding run](./docs/lean_cook_levin_builder_first_clause_padding_run.md): two unary evaluators around a literal 25-rule countdown loop, exact traversal of the remaining first-clause padding block, second-clause-start semantics, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin full-schedule cursor controller](./docs/lean_cook_levin_builder_full_schedule_cursor_controller.md): one all-input polynomial schedule count, exact semantic full traversal, a literal raw countdown controller, exact terminal coordinate, external polynomial fuel, and explicit decoder/emitter nonclaims.
- [Lean Cook–Levin arbitrary-slot header router](./docs/lean_cook_levin_builder_arbitrary_slot_header_router.md): one fixed 54-rule comparator for every coordinate, exact top-level direct-slot routing, an input-size polynomial compiled bound, and fail-closed timing and malformed-input boundaries.
- [Lean Cook–Levin second-clause separator step](./docs/lean_cook_levin_builder_second_clause_separator_step.md): selected separator appender plus cursor advance, exact canonical prefix through clause two's opening token, retained following-`F` coordinate, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin second-clause first-literal prefix](./docs/lean_cook_levin_builder_second_clause_first_literal_prefix.md): two selected `F` appenders and two cursor advances, exact canonical prefix through negative variable zero, retained variable-one sign coordinate, external polynomial fuel, and four fail-closed launch boundaries.
- [Lean Cook–Levin second-clause second-literal prefix](./docs/lean_cook_levin_builder_second_clause_second_literal_prefix.md): selected `F`/`T`/`F` appenders and three cursor advances, exact canonical prefix through negative variable one, retained clause-terminator coordinate, external polynomial fuel, and six fail-closed launch boundaries.
- [Lean Cook–Levin complete second-clause prefix](./docs/lean_cook_levin_builder_second_clause_prefix.md): selected `Finish` appender plus cursor advance, exact canonical prefix through the complete second clause, retained first-padding coordinate, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin second-clause remaining-padding run](./docs/lean_cook_levin_builder_second_clause_padding_run.md): two unary evaluators around the reused literal 25-rule countdown loop, exact traversal of the remaining second-clause padding block, third-clause-start semantics, unchanged canonical output, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin third-clause separator step](./docs/lean_cook_levin_builder_third_clause_separator_step.md): reused selected separator appender plus cursor advance, exact canonical prefix through clause three's opening token, retained following-`F` coordinate, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin third-clause first-literal prefix](./docs/lean_cook_levin_builder_third_clause_first_literal_prefix.md): reused two-`F` appender/cursor suffix, exact canonical prefix through negative variable zero, retained variable-two sign coordinate, external polynomial fuel, and four fail-closed launch boundaries.
- [Lean Cook–Levin third-clause second-literal prefix](./docs/lean_cook_levin_builder_third_clause_second_literal_prefix.md): fixed `F T T F` appender/cursor suffix, exact canonical prefix through negative variable two, retained clause-terminator coordinate, external polynomial fuel, and eight fail-closed launch boundaries.
- [Lean Cook–Levin complete third-clause prefix](./docs/lean_cook_levin_builder_third_clause_prefix.md): selected `Finish` appender plus cursor advance, exact canonical prefix through the complete third clause, retained first-padding coordinate, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin third-clause padding run](./docs/lean_cook_levin_builder_third_clause_padding_run.md): exact `C-8` no-emission traversal, retained fourth-clause `Sep` coordinate, compiled polynomial fuel, and fail-closed countdown boundaries.
- [Lean Cook–Levin complete fourth-clause prefix](./docs/lean_cook_levin_builder_fourth_clause_prefix.md): selected `Finish` appender plus cursor advance, exact canonical prefix through the complete fourth clause, retained first-padding coordinate, external polynomial fuel, and fail-closed boundaries.
- [Lean Cook–Levin second-constraint first-literal successor token](./docs/lean_cook_levin_builder_second_constraint_first_literal_successor_token_step.md): represented-width branching through one appender, exact `Finish`/`T` emission, retained following padding-or-unary coordinate, external polynomial fuel, and audited fail-closed boundaries.
- [Lean Cook–Levin second-constraint second padding-or-unary opportunity](./docs/lean_cook_levin_builder_second_constraint_second_padding_or_unary_opportunity_step.md): reused represented-width optional appender, exact width-one skip or second-unary-`T` emission, retained following padding-or-third-unary coordinate, external polynomial fuel, and audited fail-closed boundaries.
- [Lean Cook–Levin second-constraint third padding-or-unary opportunity](./docs/lean_cook_levin_builder_second_constraint_third_padding_or_unary_opportunity_step.md): reused represented-width optional appender, exact width-one skip or third-unary-`T` emission, retained following padding-or-fourth-unary coordinate, external polynomial fuel, and audited fail-closed boundaries.
- [Lean Cook–Levin second-constraint fourth padding-or-unary opportunity](./docs/lean_cook_levin_builder_second_constraint_fourth_padding_or_unary_opportunity_step.md): reused represented-width optional appender, exact width-one skip or fourth-unary-`T` emission, retained following padding-or-terminating-`F` coordinate, external polynomial fuel, and audited fail-closed boundaries.
- [Lean collision-free pipeline state namespace](./docs/lean_pipeline_state_namespace.md): injective three-stage renaming, lookup-isolated finite rule-table concatenation, and exact stage-local trace transport.
- [Lean sequential pipeline state namespace](./docs/lean_pipeline_sequential_state_namespace.md): disjoint whole-component images, isolated dispatch, local-trace transport, and the exact remaining composition boundary.
- [Lean executable pipeline stage bridges](./docs/lean_pipeline_stage_bridges.md): exact framer/simulator/handoff launches, verdict-indexed handoff copies, and cumulative work/raw costs through the internal endpoint.
- [Lean executable terminal raw-output packer](./docs/lean_terminal_output_packer.md): universal local packing, blank-delimited output equality, exact compiled cost, and one-step-short timeout.
- [Lean executable handoff-to-terminal bridge](./docs/lean_pipeline_terminal_bridge.md): disjoint verdict-indexed packer copies, exact endpoint launches, local terminal output equality, and the remaining earlier-trace transport boundary.
- [Lean all-input four-stage pipeline compiler](./docs/lean_pipeline_compiler.md): complete raw-input verdict/output preservation for an already-raw proof-bearing target, explicit external polynomials, timeout behavior, and the remaining charged-program refinement boundary.
- [Lean finite charged-pipeline complexity interface](./docs/lean_concrete_complexity.md): concrete bitstring P/NP witnesses, bounded certificates, polynomial reductions, the inactive target, and the raw-machine-link boundary.
- [Lean direct-wire NAND enumerator](./docs/lean_nand_enumerator.md): scope and limits of exact-width syntactic completeness.
- [Lean exhaustive reference minimum](./docs/lean_nand_reference_minimum.md): decidable truth tables, exact finite minimum, residual slack, and the concrete framed boundary.
- [Lean locked-NAND local baselines](./docs/lean_locked_nand_baseline.md): typed candidates, semantic output lower bounds, source-derived accounting, five exact local minima, and the quarantined legacy fixture.
- [Lean conditional locked-NAND threshold boundary](./docs/lean_locked_nand_threshold_boundary.md): the six proof-bearing premises, derived semantic boundary, hostile-review mapping, and exact missing instantiations.
- [Lean locked-NAND carrier layout and trace equivalence](./docs/lean_locked_nand_carrier_trace.md): an exact unbounded carrier partition and both directions of the legacy Section 17 trace induction for arbitrary finite NAND circuits.
- [Lean locked-NAND global candidates](./docs/lean_locked_nand_global_candidates.md): exact source-derived macro and prefix assembly, the square baseline, the four-gate extension, final conjunction semantics, and the remaining conditional fields.
- [Lean locked-NAND global baseline distinctness](./docs/lean_locked_nand_global_baseline_distinct.md): constructive global nonconstancy, nonprojection, pairwise function separation, and exact square-baseline reference minimum.
- [Lean locked-NAND global semantic threshold](./docs/lean_locked_nand_global_semantic_threshold.md): both final branches, all six typed premises, the exact threshold split, and residual slack at most four.
- [Lean encoded locked-NAND semantic boundary](./docs/lean_concrete_locked_nand_semantic_reduction.md): strict external bytes, direct normalization semantics, complete candidate serialization, fail-closed decoding, and the next executable-machine boundary.
- [Lean strict-v0 locked-NAND source parser](./docs/lean_concrete_locked_nand_source_parser.md): literal rule table, all-input exact accept/reject behavior, byte-preserving-or-empty output, compiled cubic non-timeout, polynomial machine/function witnesses, and leaf raw refinement.
- [Lean strict-v0 locked-NAND target emitter](./docs/lean_concrete_locked_nand_target_emitter.md): direct legacy target reconstruction, a fixed grammar-only controller, exact all-input output, polynomial runtime/output bounds, compiled witnesses, strict parser composition, and recursive raw refinement.
- [Lean strict-v0 locked-NAND polynomial reduction](./docs/lean_concrete_locked_nand_polynomial_reduction.md): exact concrete source/target languages, the packaged many-one reduction, exact output and language equivalence, and recursive raw refinement.
- [Lean concrete report-facing locked-NAND compatibility](./docs/lean_concrete_legacy_locked_nand_compatibility.md): exact concrete aliases for the report-facing complexity layer, direct reuse of the compiled reduction, removal of duplicate locked-NAND trust, and the remaining deterministic-SAT and residual obligations.
- [Lean all-input CNF-to-NAND semantic compiler](./docs/lean_concrete_cnf_to_nand_semantic_compiler.md): strict encoded CNF semantics, exact syntax-directed NAND construction, fail-closed malformed inputs, and the direct language-equivalence boundary.
- [Lean all-input CNF-to-NAND polynomial reduction](./docs/lean_concrete_cnf_to_nand_polynomial_reduction.md): one fixed finite compiler, exact all-bitstring output, explicit polynomial bounds, compiled non-timeout behavior, raw refinement, and the direct and locked-NAND-composed reductions.
- [Lean concrete locked-NAND threshold publication](./docs/lean_concrete_locked_nand_threshold_publication.md): the report-facing all-bitstring reduction, exact kernel type and axiom closure, and the remaining decision/root boundary.
- [Lean explicit-list residual routes](./docs/lean_residual_routes.md): sound gain scanning, proof-bearing terminal results, and fail-closed unresolved outcomes.
- [Lean verified residual-gain chain bound](./docs/lean_residual_gain_chain.md): universal semantic preservation, strict residual descent, and the locked-family four-step specialization.
- [Lean global strict-gain stopping specification](./docs/lean_residual_gain_stopping.md): positive-slack existence, zero-slack global absence, semantic minimality, and proof-supplied chain endpoint stopping.
- [Lean terminal full-carrier residual bridge](./docs/lean_residual_terminal_full_bridge.md): complete multi-output semantics, exact terminalization, the direct-wire terminal minimum bridge, and whole-span strict descent.
- [Lean terminal quotient/full mode firewall](./docs/lean_residual_terminal_mode_firewall.md): computed finite carrier profiles, exact forgetful projection, checked full lifting, obligation preservation, and a mismatch-based constructive firewall.
- [Lean saturated terminal support-square closure](./docs/lean_residual_terminal_support_square_closure.md): exact all-finite meet/join closure, seed extensionality, computed physical compatibility, and semantic extraction for every corner.
- [Lean governed terminal support completion](./docs/lean_residual_terminal_governed_support_completion.md): exact computed boundaries and interfaces plus a disjoint ten-role profile partition for every saturated square corner.
- [Lean governed terminal frontier pushout](./docs/lean_residual_terminal_frontier_pushout.md): exact all-finite side gluing, meet-profile overlap, and witnessed physical internalization for every computed saturated support square.
- [Lean exhaustive Packet selector-universe gain scan](./docs/lean_residual_terminal_packet_selector_universe_gain_scan.md): complete canonical-handle enumeration and checked source-cell gains or exact family-local no-gain, with the manuscript/global boundary kept explicit.
- [Lean conditional Packet selector gain coverage](./docs/lean_residual_terminal_packet_selector_gain_coverage.md): an explicit gain-coverage certificate upgrades exhaustive family silence to proof-bearing ZeroSlack while the certificate-construction and unconditional boundary remain open.
- [Lean finite Packet charge surplus](./docs/lean_residual_terminal_packet_charge_surplus.md): exact occurrence matching plus an unmatched positive support charge derives strict replacement weight, a genuine equivalent gain under exact accounting and semantics, and residual descent; replacement construction remains open.
- [Lean checked Packet unit-charge blueprint realizer](./docs/lean_residual_terminal_packet_unit_charge_blueprint_realizer.md): a constructive exact-occurrence validator derives canonical gate-charge ledgers and gains for supplied replacement blueprints across every canonical handle; blueprint construction and typed failure routes remain open.
- [Lean checked Packet typed-realizer contract](./docs/lean_residual_terminal_packet_typed_realizer_contract.md): arbitrary finite faithful-selector tables accept only checked blueprint gains or active bounded-rank HN, budget, and strictly lower faithful seed bots; construction and global blocker closure remain open.
- [Lean checked exact-rank HB blocker-graph acyclicity](./docs/lean_residual_terminal_hb_blocker_graph_acyclicity.md): an exhaustive finite-to-exact rank embedding and every supplied HN/BUD dependency edge are checked for strict descent, yielding well-foundedness and no directed cycle while dependency completeness and blocker semantics remain open.
- [Lean checked total-table HB dependency closure](./docs/lean_residual_terminal_hb_dependency_table_closure.md): every finite HN/BUD node has one row, every listed dependency is materialized exactly, and strict rank descent yields well-founded induction and cycle exclusion while semantic dependency completeness and the blocker invariant remain open.
- [Lean checked HB active-dependency closure](./docs/lean_residual_terminal_hb_active_dependency_closure.md): exhaustive active-to-active row closure plus exact-rank descent forces every supplied HN/BUD activity bit false and removes HN/budget bots while semantic table construction and unconditional selector-rank closure remain open.
- [Lean conditional HB selector-silence rank closure](./docs/lean_residual_terminal_hb_selector_silence_closure.md): explicit global semantic gain exclusion plus checked blocker inactivity and strong finite-rank induction eliminates every faithful canonical handle in an accepted supplied table, while selector faithfulness or compatibility and terminal-data construction remain open.
- [Lean executable selector-silence induction](./docs/lean_residual_terminal_hb_executable_selector_silence_induction.md): an exhaustive typed-bottom scan replaces the global semantic no-gain premise, while terminal-data construction, selector faithfulness or compatibility, and the full unconditional HB negative closure remain open.
- [Lean Packet selector-faithfulness routing](./docs/lean_residual_terminal_packet_selector_faithfulness_routing.md): canonical payload checks and exact HB binding turn every positive Packet into a faithful handle that contradicts executable selector silence; construction of the route-clear terminal data and unconditional ZeroSlack remain open.
- [Lean canonical Packet faithfulness-table construction](./docs/lean_residual_terminal_packet_selector_faithfulness_table.md): the HB table's faithfulness function is now computed from canonical Packet payloads and binds by construction, while payload semantics and terminal-data derivation remain open.
- [Lean total Packet selector first-route outcome](./docs/lean_residual_terminal_packet_selector_first_route_outcome.md): rejection now yields a concrete earliest typed route and canonical HB selector silence forces that outcome for every positive Packet, while external route semantics and decreasing global coverage remain open.
- [Lean exact Packet first-route failure semantics](./docs/lean_residual_terminal_packet_selector_first_route_semantics.md): every route now carries the exact earliest supplied-field failure it names, while terminal-data derivation and external manuscript semantics remain open.
- [Lean rank-reflected Packet descent route](./docs/lean_residual_terminal_packet_descent_route_reflection.md): the final descent field is computed from the ten-coordinate `RankWF` relation, while the first nine fields and complete global route integration remain open.
- [Lean canonical Packet rank-tag route reflection](./docs/lean_residual_terminal_packet_rank_route_reflection.md): the duplicate payload rank tag is copied from the authoritative handle rank and the `.rank` route is excluded, while the eight remaining routes and terminal-data construction remain open.
- [Lean canonical Packet exact-route reflection](./docs/lean_residual_terminal_packet_exact_route_reflection.md): the canonical internal handle-to-cell-to-payload route is reflected and both `.exactRoute` and `.rank` are excluded, while the seven remaining routes, terminal-data construction, and unconditional ZeroSlack remain open.
- [Lean canonical Packet charge-route reflection](./docs/lean_residual_terminal_packet_charge_route_reflection.md): positive source mass reflects the internal charge check and excludes `.charge`, `.exactRoute`, and `.rank`, while the six remaining routes, terminal-data construction, and unconditional ZeroSlack remain open.
- [Lean canonical Packet colour-route reflection](./docs/lean_residual_terminal_packet_colour_route_reflection.md): canonical grouped-footprint eligibility reflects the internal colour check and excludes `.colour`, `.charge`, `.exactRoute`, and `.rank`, while the five remaining routes, terminal-data construction, and unconditional ZeroSlack remain open.
- [Lean canonical Packet typed-frontier route reflection](./docs/lean_residual_terminal_packet_frontier_route_reflection.md): executable equality of explicit typed frontier signatures replaces the caller frontier bit and gives `.frontier` exact inequality semantics, while the four remaining routes, signature construction from terminal data, and unconditional ZeroSlack remain open.

## Install and current package surface

Use the lockfile-preserving installation command:

```bash
npm ci --ignore-scripts
```

The root package deliberately exports only current formal-status and archive-verification APIs. The
legacy checker modules remain in repository history and at the pinned source tag, but are not active
package exports.

```js
import {
  CheckFormalReconstructionStatus0,
  CheckLegacyV0Archive0,
} from '@aisknab/pnp';

const status = await CheckFormalReconstructionStatus0({ writeOutput: false });
const archive = await CheckLegacyV0Archive0();
```

Useful top-level commands:

```bash
npm run check
npm test
npm run validate
npm run formal:status
node scripts/export-lean-theorem-inventory.mjs --check
node scripts/generate-formal-publication.mjs --check
npm run report:check
npm run legacy:v0:check
npm run pnp:verify -- --no-write
```

## Historical Proof-development scripts

The assertion-checker release used narrowly scoped proof-development entrypoints under the `proof:*`
namespace. They are not scripts on current `main`; they exist only in the pinned legacy-v0 source
tree reached by the designated replay. They do not determine current theorem status. The current authority is
[`status/FORMAL_RECONSTRUCTION_STATUS.json`](./status/FORMAL_RECONSTRUCTION_STATUS.json), checked with:

```bash
node pcc-formal-reconstruction-status0.mjs --json
npm run formal:progress
```

In that legacy interface, a proof script was a direct checker invocation of this form:

```text
node pcc-<checker-name>0.mjs --json
```

The following commands are historical examples from the pinned source tag and are not commands on
the active package or formal proof gates:

```bash
npm run proof:uniform-final-soundness-target -- --historical-replay
npm run proof:uniform-input-family -- --historical-replay
npm run proof:uniform-locked-nand-construction -- --historical-replay
npm run proof:uniform-locked-nand-threshold -- --historical-replay
npm run proof:uniform-residual-band-minimizer -- --historical-replay
npm run proof:uniform-zeroslack-closure -- --historical-replay
npm run proof:no-hidden-oracle-semantic -- --historical-replay
npm run proof:uniform-complexity-conclusion -- --historical-replay
node pcc-formal-reconstruction-status0.mjs --json
```

The uniform scripts above replay legacy assertion-checker records. They do not determine current
theorem status or establish any proposition named by those records.

## Historical Public RunAll0 entry point

`RunAll0` was the public entry point for the frozen assertion-checker release. It is not exported on
current `main`. Reproduce it only through the pinned legacy-v0 runner described in
[`REPRODUCE.md`](./REPRODUCE.md). The current status check is:

```bash
node pcc-formal-reconstruction-status0.mjs --json
```

At the pinned source tag, the legacy commands were:

```bash
npm run smoke -- --historical-replay
npm run smoke:full -- --historical-replay
```

The legacy replay encoded this conditional assertion:

```text
CheckPCCPackexp(GeneratePCCPack())=accept implies P = NP
```

The generator is untrusted. The legacy checker validates the materialized package, compares canonical bytes rather than digest equality, and records the conditional conclusion only after its final replay
accepts. A reject run emits a replayable first failure and no public theorem conclusion. Acceptance of
that replay does not establish the named mathematical conclusion.

The package entry point is:

```text
index.mjs
```

## Historical Release audit replay

The release audit belongs to the frozen assertion-checker release. It is not a current package script
or part of the current formal verification gate. Use the pinned runner in
[`REPRODUCE.md`](./REPRODUCE.md); any historical commands below apply only inside its detached source
worktree and must not be used to infer current theorem status.

```bash
npm run release:audit -- --historical-replay
```

For the full release audit record:

```bash
npm run release:audit:full -- --historical-replay
```

The release audit checks the public package surface, package exports, README claim boundary, orphaned
tests, syntax of checker modules, deterministic repeated `RunAll0` execution, the public surface freeze
phase, and the materialized public-status release gate. Those checks describe legacy checker replay,
not a mathematical proof.

### Release audit hard-gate default

Inside the pinned source worktree, the unflagged legacy form
`npm run release:audit -- --fast-local` rejects at the reconstruction boundary. Historical replay
must be explicit:

```bash
npm run release:audit -- --historical-replay --fast-local
```

Fast local mode keeps the public surface freeze enabled while skipping the costly materialized
public-status gate. This remains checker-replay behavior, not theorem verification.

### Release audit materialized gate flags

The old forms `npm run release:audit -- --materialized-gate` and
`npm run release:audit -- --no-materialized-gate` now reject unless `--historical-replay` is also
present. The historical CLI retains `--materialized-gate-out` and `--no-materialized-gate-cli` for
reproducing the concrete gate as a path separate from synthetic `RunAll0`.

### Release audit materialized gate summary

Historical full-mode records retain these fields:

```text
materializedPublicStatusGateDigest
materializedPublicStatusGateFileCount
materializedPublicStatusGateDirectRecordCount
materializedPublicStatusGateCliRecordCount
materializedPublicStatusGateAcceptedPublicConclusionOnly
syntheticRunAll = false
acceptedPublicConclusionOnly = true
```

These are preserved audit fields only. They do not describe current theorem status.

### Release audit surface freeze

The historical `surfaceFreeze` record includes `materializedPublicStatusGateDigest` and
`materializedPublicStatusGateAcceptedPublicConclusionOnly`. The reconstruction-era replacement is
`CheckFormalPublicSurface0`, which verifies that legacy routes are absent from the closed current
package surface.

## Historical Internal materialized package path

The remaining release-audit sections document the frozen assertion-checker machinery. Their fields,
normal forms, and negative tests are preserved for auditability only. They are subordinate to the
[formal reconstruction status](./status/FORMAL_RECONSTRUCTION_STATUS.json).

Materialized package checks use explicit JSON fixtures rather than implicit source state:

```text
MaterializedPCCPack0.json
  -> CheckMaterializedShell0
  -> CheckMaterializedAggregate0
```

Historical replay commands for this layer are:

```bash
npm run materialized:shell
npm run materialized:aggregate -- --historical-replay
npm run materialized:bridge -- --historical-replay
```

An accepted historical bridge recorded `CheckPCCPackexp status = accepted` and
`ExternalAcceptRunReplay verdict = accept` before emitting its conditional record. These fields are
legacy checker outputs, not current theorem authority.

## Historical Release audit README wording freeze

`CheckReadmeReleaseBoundary0` preserves the legacy conditional theorem boundary and its
stale-layout exclusions for reproducible checker replay. Passing that wording check does not
validate the mathematics or reactivate theorem emission.

## Historical Public entry release surface freeze

The public release surface is checked by `CheckPublicEntryReleaseSurface0`.

The exact portions are:

```text
index.mjs public export names
package.json exports keys and values
package.json bin keys and values
```

The script surface is intentionally extensible under the narrow `proof:*` namespace during proof development. Non-proof script additions and unsafe proof-script commands still reject.

## Historical Release audit public surface freeze phase

The release audit executes the public entry release surface freeze checker as a ledger phase named `publicSurfaceFreeze`.

The phase verifies:

```text
index.mjs public export names
package.json exports map
package.json bin map
package.json script map
```

During active proof development, the script map check is exact for existing release scripts and permits only the constrained `proof:*` checker-script namespace.

## Historical Release audit public surface freeze summary

The release audit exposes the public-surface check as a first-class summary, not only as a side effect.

The summary includes:

```text
publicSurfaceFreezeDigest
publicSurfaceFreezePublicEntryExportCount
publicSurfaceFreezePackageExportCount
publicSurfaceFreezePackageBinCount
publicSurfaceFreezePackageScriptCount
publicSurfaceFreezeSurfaceFrozen
```

When enabled, the release audit requires:

```text
surfaceFrozen = true
```

During active proof development, `surfaceFrozen = true` means exports and bin entries remain exact while package scripts may grow only through the constrained `proof:*` checker-script namespace.

## Historical Release audit public surface freeze negative coverage

The release audit includes negative coverage for the public surface freeze phase.

The negative checks prove that `CheckReleaseAudit0` rejects if the public surface freeze checker returns an accepted record with:

```text
wrong normal-form kind
surfaceFrozen = false
zero public entry export count
zero package export count
zero package bin count
zero package script count
missing normal form
```

All such failures surface at:

```text
CheckReleaseAudit0.publicSurfaceFreeze
```
