# Formal reconstruction status artifacts

Run:

```bash
npm run proof:formal-reconstruction-status
```

The checker writes:

```text
artifacts/formal-reconstruction-status/latest-verdict.json
```

An accepted verdict establishes only that the current and public status payloads agree, theorem emission is disabled, the legacy activation payload is superseded, and the formal release gate remains incomplete.
