# Computed closed-support full-profile physical gain

Current coordinate: `PNP-FORMAL-RECONSTRUCTION-STATUS-2026-09-19-278`.

Formal artefact coverage: 254 of 256 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%.
Uncertainty range: 20% to 40%.
Global gates closed: 0 of 5.

## Exact scope

For arbitrary finite wire carriers, keep masks and seed record families under the computed computational-wire-profile model, the full-profile reference minimum of the actual closed support is specialized to primary inputs and reconnected to the derived physical complement. The resulting whole carrier preserves every ordered output and computational field for every input, with exact gate and whole-carrier full-slack balance. Positive computed local full slack yields a checked strict equivalent gain; zero local full slack returns no gain. The support, minimum, source bindings and reconnection are computed, not supplied correctness data.

## Limits

The full-profile minimum, source matching and saturation influence tests remain exhaustive finite reference computations. This does not derive a proper positive support, make whole-span replacements locally VerifyDW-eligible, implement the manuscript's complete noncomputational profile grammar, establish global route coverage or unconditional SaturatePositive, BCELReady or ZeroSlack, or prove complete PCCMin polynomial runtime, output-size or certificate bounds. The eligible root theorem remains absent and P = NP is not proved.

## Construction and proof interfaces

The [prefix construction](../lean/PNP/NANDClosedSupportFullGainPrefix.lean)
uses the actual closed support and its canonical full-profile reference
minimum. It specializes ambient inputs to the original primary inputs,
sets unused ambient gate inputs to false, and proves preservation of the
retained source values, support outputs and computational fields.

The [whole-carrier construction](../lean/PNP/NANDClosedSupportFullGain.lean)
extracts the physical complement, binds the actual crossing interface and
reconnects the two pieces. Retained field sources use the minimum; exterior
field sources use the reconstructed complement. All ordinary outputs and
computational fields are preserved uniformly, including zero-width cases.

The exact gate balance turns positive computed local full slack into strict
physical gain. Full-profile equivalence preserves the whole reference minimum,
so the same saving is retired from whole-carrier full slack. The checked
optional route returns no gain exactly when local full slack is zero, and
its positive result feeds the existing strict-equivalent-gain interface.

These are full-profile statements. A quotient minimum that forgets a hidden
field cannot replace the full minimum in this construction.

## Independent regression boundaries

The [regression](../lean-regression/PNPClosedSupportFullGain.lean) checks eight
general type contracts, five kernel-checked guards with axiom audits and ten
bounded executable cases. It covers empty dimensions, constants and input
fields, hidden-field/quotient disagreement, full-span duplicate elimination,
field-only reconstruction, a proper constant-gate saving with a nonconstant
exterior field, a genuine support-to-complement edge, empty selected support
and profile-seeded reconstruction. The runtime checks are not proof authority.

The [axiom audit](../lean-audit/PNPClosedSupportFullGainAxiomAudit.lean) imports
the explicit root and checks all 27 reviewed theorems. Their compiled closures
contain only the allowed standard axioms `propext` and `Quot.sound`, with no
project-specific authority.

The [source contract](../audits/lean-closed-support-full-gain0.test.mjs) pins
the reviewed construction. The [compiled publication contract](../audits/lean-closed-support-full-gain-publication0.test.mjs)
rejects weakened conclusions, supplied correctness premises, missing evidence,
added authority, altered fingerprints and widened publication claims.

## Progress and publication

M278 connects an actual computed closed support to a full-profile-preserving whole-circuit replacement, with an exact gate saving and exact retirement of whole-carrier full slack. The reference minimum, physical complement, input bindings and field sources are derived rather than supplied as correctness premises. This closes a bounded positive-branch reconstruction dependency, but not proper-positive support discovery, the complete manuscript profile grammar, terminal-derived families, global route coverage or polynomial minimization. No fixed load-bearing checkpoint changes state; weights, checkpoint statuses, proof estimate and uncertainty are unchanged.

Publication decision: defer. This completes the physical reconstruction of a computational-field-preserving finite reference improvement, not proper-positive support discovery, complete manuscript profiles, terminal-derived families, global route coverage or polynomial construction. No fixed weighted checkpoint or global gate changes, and the public global-proof bottom line remains unchanged. Preserve the coherent M264 website pin and batch pending core evidence at the next major publication.

See the [integration plan](./plans/2026-09-19-computed-closed-support-full-profile-gain.md),
[canonical progress ledger](../status/PROOF_PROGRESS.json),
[preceding computed observations](./lean_closed_support_profile.md) and
[pinned manuscript archive](../archive/legacy-v0/ARCHIVE.json).
