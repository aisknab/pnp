# Cook-Levin physical Finish-request handoff

M219 closes the first literal request-generation branch after M218.  The
unique canonical final post-header coordinate is derived from every concrete
verifier problem.  M214's checked route classifier proves that coordinate is
in range and reaches the equal quotient-versus-clause-count terminal; no
coordinate, route, request, token, trace, schedule, or success certificate is
supplied to the public endpoint.

The comparator receives the canonical builder workspace as a protected suffix
beyond its right end marker.  Lean proves that every exact comparator step is
unchanged by that suffix.  At the equal terminal, a fixed one-rule writer
changes the protected end marker into M217's physical `Finish` request symbol.
The writer and M217 dispatcher are composed with the 54-rule comparator into
one collision-free 137-rule work machine.

For every verifier problem and arbitrary classifier exterior, Lean proves the
complete exact work trace, the compiled six-for-one trace, and one-step-short
nonhalting.  The final configuration is M217's exact dispatcher result after
appending `CNFToken.finish` to the canonical pre-Finish output.  The total
compiled work is bounded by one verifier-derived polynomial combining the
quadratic equal-comparator bound, three fixed bridge/writer steps, and M217's
canonical dispatch bound.

The reviewed endpoint is:

```text
PNP.Concrete.CookLevin.BuilderPhysicalFinishRequest.cook_levin_builder_physical_finish_request_checked_complete
```

This theorem is deliberately narrower than a raw formula builder.  It covers
only the unique canonical `Finish` branch.  It does not derive padding or body
token requests from raw clause/token coordinates, preserve the builder suffix
through every preceding classifier stage, connect successive schedule
configurations into one literal loop, prove builder `RawRefinement`, package
the Cook-Levin `PolynomialReduction`, establish concrete NP-hardness or
NP-completeness, put CNFSAT in P, close a fixed checkpoint or global gate,
create the eligible root theorem, or prove P = NP.

The risk-weighted proof-completion estimate remains 35 percent with the 20 to
40 percent uncertainty range.  Formal artefact coverage is tracked separately
and changes only when the new compiled publication row is earned.
