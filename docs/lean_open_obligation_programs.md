# Open obligations across complete descendant-support programs

M269 connects actual cross-support obligation transport to a source-only, proper-support verifier. Its specification anchors are sections 6.1 and 6.3 (Package E, VerifyDW and composed splice) and section 4 (physical ownership and materializer charging) of the [pinned manuscript](../archive/legacy-v0/ARCHIVE.json). It closes one computational dependency after the [proper descendant certificate boundary](lean_proper_descendant_certificates.md), not the full manuscript verifier.

## Raw input and complete acceptance

The caller supplies a source computational wire carrier, raw outer support records and a list of raw events. Dimensions, support sizes, event graphs and program lengths are arbitrary finite data. An event contains its identity, explicit predecessor identifiers and either a primitive action or a raw nested-support program. Discharge actions also contribute their intrinsic creation dependencies. Duplicate identities, missing references and cycles reject.

The verifier constructs its initial state, computes the complete order and executes every event. An accepted result requires all raw coordinates to decode, successful complete execution, final ambient ledger closure, nonempty physical exterior and strict final local saving. The outer literal splice is derived from execution-preserved causal bounds; the caller supplies no successful-splice or wiring-order witness. A rejected later event rejects the complete certificate, never just its suffix.

## Persistent obligations, values and physical costs

A support transition preserves the exact pending snapshot function while changing the actual current carrier. An ambient creation can remain open through multiple support changes; it must later be discharged by an actual full-mode R6, R7 or R8 operation. Final closure recovers every full interface value. Inner descendant programs keep their existing closed-history boundary.

The actual outer splice preserves every ordinary output and literal field and contains the unchanged exterior exactly once. Complete execution determines its physical ownership map. Original gate coordinates and each allocation’s computed outer position, inner stage, event identity and local gate distinguish every physical owner, even when nested and ambient numeric event identifiers coincide.

Every original or allocated gate occurs exactly once as live or removed. Historical allocation charges remain after later deletion. The final gate count plus cumulative removals equals the source gate count plus cumulative charges. Intermediate growth is allowed; strict final saving yields StrictEquivalentGain and strict residual descent. No intermediate carrier, captured snapshot, order, correctness proof, owner map, cost total, rank or semantic oracle is a certificate input.

## Source and verification boundaries

| Source | Verified boundary |
| --- | --- |
| [State splice](../lean/PNP/NANDWireOpenSupportSplice.lean) | Literal pending snapshots, available full values and actual nested costs |
| [Input and ordering](../lean/PNP/NANDWireOpenProgramInput.lean), [complete execution](../lean/PNP/NANDWireOpenProgram.lean) | Full dependency graph, computed schedule, creation lifecycle and final closure |
| [Nested ownership](../lean/PNP/NANDWireOpenSupportOwnership.lean), [primitive ownership](../lean/PNP/NANDWireOpenPrimitiveOwnership.lean), [whole-program ownership](../lean/PNP/NANDWireOpenProgramOwnership.lean) | Actual compiler maps, computed namespaces and full allocation/removal history |
| [Proper embedding](../lean/PNP/NANDWireOpenProperSupport.lean), [outer ownership](../lean/PNP/NANDWireOpenProperOwnership.lean) | Derived literal splice, one-copy exterior, complete observations and physical partition |
| [Source-only certificate](../lean/PNP/NANDWireOpenCertificate.lean) | Exact complete acceptance, proper support, strict saving and fail-closed rejection |

The [source contracts](../audits/lean-open-obligation-programs0.test.mjs), [compiled publication contracts](../audits/lean-open-obligation-programs-publication0.test.mjs) and [explicit-root axiom audit](../lean-audit/PNPOpenObligationProgramAxiomAudit.lean) bind 144 reviewed theorem closures. Run npm run audit:m269 after status generation is reconciled. The durable workflow checks the exact root audit and eight regression families.

Regressions cover multiple support changes under the same open creation, actual R7 and R8 restoration, full-mode R6 cancellation, reads before and after closure, identity collisions, allocations later removed, temporary growth, malformed coordinates, missing/duplicate/cyclic dependencies, failed tails, whole supports, nondecrease and zero dimensions. These executions are fixtures; the arbitrary-dimension Lean statements establish the mathematical results.

## Reviewed publication boundary

As of PNP-FORMAL-RECONSTRUCTION-STATUS-2026-09-17-269:

For arbitrary finite computational wire carriers, raw outer support records and raw mixed programs, source-only verification computes the complete dependency order, including intrinsic creation references, and executes every primitive or actual descendant-support splice from its internally constructed initial state. The same pending creations and captured full-value snapshots persist across intervening support changes until genuine full-mode R6, R7 or R8 discharge; final ambient closure is required. Execution-derived causal bounds construct the literal outer splice. Acceptance is exactly complete decoding and finally closed execution on a proper physical support with strict final saving. The result preserves all ordinary outputs and literal fields, includes the exterior once, and reconstructs unique original/allocation ownership through the actual compiler positions. Computed outer-position namespaces distinguish nested event identities; historical charges and removals survive later allocation deletion. Temporary expansion is allowed; malformed, cyclic, missing or duplicate references, unfinished ledgers, failed tails, whole supports and final nondecrease reject. No intermediate carrier, schedule, snapshot, correctness, cost, owner, rank or splice witness is supplied.

This verifies offered arbitrary finite mixed programs in the computational wire-carrier language; inner descendant histories retain their existing closed-history boundary. It does not find an accepted certificate for every nonminimal input, establish local minimality after rejection, or construct a globally successful strategy. Full manuscript profiles and all R1-R9 and N1-N10 rule families, matched-kappa arbitrary-support Pull/Expand, full manuscript VerifyDW, ChargeSoundness and Package E, and terminal-family derivation remain open. There is no bound on temporary growth or encoded-input polynomial runtime, output size or certificate size. Global route coverage, unconditional SaturatePositive, BCELReady and ZeroSlack, exact general PCCMin, deterministic CNFSAT in P and the eligible root remain open. Finite runtime fixtures are regression evidence, not theorem authority. No fixed weighted checkpoint or global gate closes, and P = NP is not proved.

Formal artefact coverage: 245 of 247 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%.
Uncertainty range: 20% to 40%.
Global gates closed: 0 of 5.

Publication decision: defer. This closes the computational mixed-program obligation-lifecycle and physical-accounting edge for offered certificates, but does not establish full manuscript VerifyDW, global certificate discovery or an unconditional checkpoint. No global proof gate or fixed weighted checkpoint changes, and the published global bottom line remains unchanged. Preserve the coherent M264 website pin and batch pending core evidence at the next major publication.

The [fixed progress ledger](../status/PROOF_PROGRESS.json) is authoritative. Its risk-weighted estimate is neither confidence that the route is correct nor a delivery or time-remaining estimate.
