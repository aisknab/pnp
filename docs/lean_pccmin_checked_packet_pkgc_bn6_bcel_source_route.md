# Source-derived PkgC/BN6 sparse BCEL route

M202 composes the M201 PkgC-to-BN6 cellization boundary with M200's checked
sparse BN6/BCEL/Packet/HB route. The theorem is uniform over an arbitrary
finite supplied source-cell list on the exact checked BCEL nucleus, the
candidate and terminal model, rank count, full-candidate type, and all typed
BN5 restoration-coordinate components.

The new adapter constructs the only admitted
`PCCMinCheckedPacketBN6BCELPositiveCells` value directly from
`terminalPkgCBN6PositiveCells sourceCells singletonized`. Callers cannot
supply a parallel raw BN6 cell list, support list, footprint-size certificate,
or raw activation mismatch. The downstream realizer table, accepted claims,
rank assignment, dependency table, checked HB closure, route-clear result,
and selector silence remain explicit supplied data over that exact derived
family.

The total classifier first invokes M201 on the original PkgC source ledger.
If it finds a same-key cancellation, M202 retains the exact source cell, its
list membership, separating pair, and typed restoration realization. If all
source systems are singletonized, their derived positive cells enter M200's
canonical grouping and quadratic singleton/pair classifier.

The coherent downstream branch returns a genuine `ZeroSlackResult` under the
supplied checked selector-silence premise. A routed branch retains one
nonempty proper carrier sublist of length at most two. M201's all-cut
conservation theorem rewrites M200's raw-cell mismatch to the original source
ledger equation, so the result is exactly

```text
terminalPkgCBN6SourceActivationWeight sourceCells cut !=
  checkedBCELDefect.
```

The public endpoint is
`PNP.DirectWire.pccmin_checked_packet_pkgc_bn6_bcel_source_route_or_zeroslack_checked_complete`.

## Claim boundary

The terminal problem, checked finite BCEL-ready certificate, active V54
source systems and cuts, payload atoms, typed restorer, realizer table,
accepted claims, ranks, dependency table, checked HB closure, route-clear
result, and selector silence remain supplied. M202 does not derive those
objects from every valid terminal input or construct upstream BN3 through BN5
data.

A returned PkgC cancellation or source-ledger activation mismatch is exact
proof-bearing evidence, but it is not yet a verified gain or globally
rank-decreasing transition. The conditional ZeroSlack branch does not make
ZeroSlack unconditional because its checked downstream data and complete
selector silence are explicit inputs.

M202 therefore does not complete PkgC/BN3--BN6 route integration, derive HN,
BUD, or HB blocker semantics, prove semantic dependency completeness,
establish manuscript-wide SaturatePositive or BCELReady, prove unconditional
ZeroSlack, construct executable polynomial PCCMin, prove encoded-input
polynomial construction or runtime bounds, put CNFSAT in P, create the
eligible root theorem, or prove P = NP.

Formal artefact coverage is 178 of 180 current scoped rows. The separate
risk-weighted proof-completion estimate remains 35 percent, its uncertainty
range remains 20 to 40 percent, and zero of five global gates are closed.

## Verification

```text
lake build PNP
lake env lean -DwarningAsError=true lean-regression/PNPPCCMinCheckedPacketPkgCBN6BCELSourceRoute.lean
lake env lean -DwarningAsError=true lean-audit/PNPPCCMinCheckedPacketPkgCBN6BCELSourceRouteAxiomAudit.lean
node --test audits/lean-pccmin-checked-packet-pkgc-bn6-bcel-source-route0.test.mjs
```

The axiom transcript covers seven reviewed declarations. Every declaration
reaches only the approved Lean-standard closure (`propext` and `Quot.sound`);
no project-specific axiom, `sorryAx`, or `Classical.choice` is present.
