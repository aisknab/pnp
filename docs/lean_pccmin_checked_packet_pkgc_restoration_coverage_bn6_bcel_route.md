# Candidate-bound PkgC restoration-coverage BN6/BCEL route

M206 connects M205's typed-restorer-free, arbitrary-finite PkgC source-ledger
classifier to the checked BN6/BCEL sparse activation boundary. Every source
and ambient activation atom is specialized to the same computed BCEL nucleus,
and each ambient ledger is proved to use that nucleus's canonical atoms.

The total classifier returns one of five proof-bearing outcomes:

- genuine conditional `ZeroSlackResult` after every source consumer system is
  proved singletonized and the M205-constructed BN6 ledger passes the checked
  Packet/HB boundary;
- the first exact restoration `TerminalBN5HallDeficit`, including its forced
  `qRestorationHall` route and strict neighbourhood deficit;
- the first coverage-constructed ambient BN4 remainder, exact embedding, and
  complete residual-ledger reduction;
- the first generated ambient cell together with proof that no exact embedding
  exists; or
- one nonempty proper singleton/pair cut whose original enriched source
  activation weight differs from the checked BCEL defect.

The successful branch cannot accept an independent BN6 ledger. It uses M205's
source projection and constructed positive cells definitionally, then reflects
any downstream raw-cell mismatch to the original enriched ledger through
M205's all-cut activation conservation theorem. The public endpoint is
`PNP.DirectWire.pccmin_checked_packet_pkgc_restoration_coverage_bn6_bcel_route_or_zeroslack_checked_complete`.

## Claim boundary

The terminal problem, checked BCEL-ready certificate, source cells and active
cuts, restoration universes and maps, ambient ledgers, ranks, claims,
dependency table, checked HB closure, route-clear result, and selector silence
remain supplied. M206 does not derive these objects from every valid terminal
input or construct semantically adequate full candidates.

A Hall deficit is not yet a verified global gain or rank-decreasing
transition. The computed remainder is not proved empty, and neither an ambient
incompatibility nor a sparse activation mismatch is yet embedded in the
complete decreasing route system. The theorem does not finish PkgC/BN3--BN6,
manuscript-wide `SaturatePositive` or `BCELReady`, unconditional `ZeroSlack`,
executable polynomial `PCCMin`, any global proof gate, `CNFSAT ∈ P`, the
eligible root theorem, or `P = NP`.

Formal artefact coverage is 182 of 184 current scoped rows. The separate
risk-weighted proof-completion estimate remains 35 percent, its uncertainty
range remains 20 to 40 percent, and zero of five global gates are closed.

## Verification

```text
lake build PNP
lake env lean -DwarningAsError=true lean-regression/PNPPCCMinCheckedPacketPkgCRestorationCoverageBN6BCELRoute.lean
lake env lean -DwarningAsError=true lean-audit/PNPPCCMinCheckedPacketPkgCRestorationCoverageBN6BCELRouteAxiomAudit.lean
node --test audits/lean-pccmin-checked-packet-pkgc-restoration-coverage-bn6-bcel-route0.test.mjs
```

The axiom transcript covers four reviewed declarations. Every declaration
reaches only the approved Lean-standard closure (`propext` and `Quot.sound`);
no project-specific axiom, `sorryAx`, or `Classical.choice` is present.
