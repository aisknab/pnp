# PkgC restoration-coverage ambient route

M204 closes one local structural seam in the manuscript Section 11.5
separating-consumer branch. M203 computes ambient extraction after receiving
a PkgC cancellation through a supplied typed restorer. M204 instead runs the
existing arbitrary-finite restoration-coordinate matcher first.

The total classifier returns one of four proof-bearing outcomes:

- every disjoint consumer pair is singletonized;
- the first separating pair has an exact TerminalBN5HallDeficit, its forced
  qRestorationHall route, and a strict neighbourhood deficit;
- complete exact-coordinate coverage constructs a balanced opposite-sign BN4
  unit pair for every canonical quotient unit, then exact extraction computes
  the arbitrary-order ambient remainder and complete residual-ledger equality;
- or one generated cell witnesses that no exact ambient embedding exists.

The construction proves exact cell count, keywise positive/negative mass
equality, empty executable residual cells, and zero signed mass. It accepts no
typed restorer, semantic full-candidate function, supplied cancellation,
remainder, embedding, or success certificate.

The public endpoint is
PNP.DirectWire.terminalPkgC_restorationCoverage_ambientBN4_route_checked_complete.

## Claim boundary

The consumer system, finite restoration-coordinate universe, quotient map,
full-restoration coordinate lists, and ambient BN4 ledger remain supplied.
Complete coordinate coverage is finite equality-fibre multiplicity evidence;
it does not materialize semantic full candidates, derive restoration data from
every terminal input, or prove restoration semantic adequacy.

The exact Hall deficit is a local Q route, not yet a verified gain or globally
rank-decreasing transition. The extracted balanced subledger preserves the
ambient residual ledger in its computed remainder; it does not prove the
remainder empty. No-embedding is missing compatibility evidence, not a
complete global route. M204 therefore does not complete PkgC/BN3--BN6, prove
manuscript-wide SaturatePositive or BCELReady, establish unconditional
ZeroSlack, construct polynomial PCCMin, put CNFSAT in P, open a global gate,
create the eligible root theorem, or prove P = NP.

Formal artefact coverage is 180 of 182 current scoped rows. The separate
risk-weighted proof-completion estimate remains 35 percent, its uncertainty
range remains 20 to 40 percent, and zero of five global gates are closed.

## Verification

lake build PNP
lake env lean -DwarningAsError=true lean-regression/PNPResidualTerminalPkgCRestorationCoverageAmbientRoute.lean
lake env lean -DwarningAsError=true lean-audit/PNPResidualTerminalPkgCRestorationCoverageAmbientRouteAxiomAudit.lean
node --test audits/lean-residual-terminal-pkgc-restoration-coverage-ambient-route0.test.mjs

The axiom transcript covers 17 reviewed declarations. Every declaration
reaches only the approved Lean-standard closure (propext and Quot.sound); no
project-specific axiom, sorryAx, or Classical.choice is present.
