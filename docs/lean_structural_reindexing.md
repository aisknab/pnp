# Structural reordering and arbitrary-support replacement transport

M270 reconstructs the computational structural-reordering edge required by sections 4, 6.1 (R1) and 6.2 (N1) of the [pinned manuscript](../archive/legacy-v0/ARCHIVE.json). It extends the [complete open-obligation program boundary](lean_open_obligation_programs.md) without replacing the manuscript route.

## Construction, not supplied correspondence

The caller supplies a finite source candidate, raw index swaps, descendant support records and an offered replacement. Both bounds of every swap are checked; a malformed instruction anywhere rejects the whole sequence. The circuit compiler derives its own order. Actual physical placement, inverse placement and canonical port order determine every transport map. No successful predecessor order, support map or source equation is accepted as substitute evidence.

Every descendant record list is covered, including reordered, repeated, empty and full supports. The open-support theorem quantifies over independent boundary values, not merely the boundary valuations induced by whole-circuit inputs. Literal replacement construction changes both incoming and outgoing coordinates while retaining every offered replacement gate.

## Literal splicing and the acceptance boundary

Both raw splice graphs have matching exterior and replacement nodes, ordered source pairs, global outputs and directed dependencies. Acyclicity and actual compiler success or rejection correspond in both directions, even for incompatible or cyclic offered replacements. This is not an assertion that every compatible replacement is acyclic.

For an actually accepted descendant splice, the predecessor compiler computes its own accepted result. Physical positions follow the actual inverse placement, raw-node correspondence and new placement. Exterior gates occur once; replacement coordinates retain their identity. Open compatibility then supplies whole-circuit semantic preservation, exact local and whole signed savings, and strict-gain pullback. Both matched surcharge differences are zero without padding.

## Reviewed source and validation interfaces

| Source | Boundary |
| --- | --- |
| [Compiler wire fidelity](../lean/PNP/NANDTopologicalWireStructure.lean), [raw swaps](../lean/PNP/NANDGateRenaming.lean), [structural reindexing](../lean/PNP/NANDStructuralReindexing.lean) | Actual accepted compilation, literal inputs and ordered outputs |
| [Arbitrary records](../lean/PNP/NANDReindexedSupport.lean), [canonical ports](../lean/PNP/NANDReindexedPorts.lean), [independent semantics](../lean/PNP/NANDReindexedOpenSemantics.lean) | Derived incidence, ownership bijections and all independent open inputs |
| [Replacement](../lean/PNP/NANDReindexedReplacement.lean), [raw splice](../lean/PNP/NANDReindexedSplice.lean), [compiled splice](../lean/PNP/NANDReindexedCompiledSplice.lean) | Literal rewiring, matched surcharge, dependency and physical transport |

The [source contracts](../audits/lean-structural-reindexing0.test.mjs), [compiled publication contracts](../audits/lean-structural-reindexing-publication0.test.mjs) and [explicit-root axiom audit](../lean-audit/PNPStructuralReindexingAxiomAudit.lean) cover 108 reviewed theorem closures. Run npm run audit:m270 after generated status is reconciled. Ten generic and runtime regression families exercise nonidentity boundary, interface and exterior order; independent values; zero and nonzero replacements; duplicate records; incompatible constants; rejected cycles; malformed swaps and zero dimensions. Finite executions remain regression evidence, never theorem authority.

## Reviewed publication boundary

As of PNP-FORMAL-RECONSTRUCTION-STATUS-2026-09-17-270:

For arbitrary finite computational wire candidates and checked finite raw index-swap sequences, structural reindexing constructs the raw graph and its successful compilation from the original circuit. Actual compiler placement and inverse placement determine literal gate-source pairs and ordered output references. For every descendant primitive-record list, including empty, full and duplicate-bearing lists, the construction derives the predecessor records and canonical boundary, interface, selected-gate and exterior bijections. Open support functions agree for every independent boundary valuation. A replacement is pulled back by literal input and output rewiring without padding; support and replacement counts agree, both matched signed surcharges are zero, and exact signed saving and strict gain are preserved. Complete raw splices have corresponding source pairs, outputs and dependency edges, with equivalent acyclicity and compiler success or rejection in both directions. Actual compiled-position maps preserve exterior and replacement ownership and literal sources. An actually accepted compatible descendant splice computes an accepted predecessor splice preserving whole-circuit semantics and exact local and whole signed savings. No predecessor compilation, order, transport map or source-fidelity witness is supplied.

This closes the computational structural-reordering component of arbitrary-support replacement transport, not full manuscript profile semantics: transported profile labels are not a proof of profile semantics. It does not establish completeness of the raw-swap encoding for every abstract permutation, all R1-R9 or N1-N10 rules, or the remaining normalization and materializer transport. Open replacement compatibility is required for semantic preservation, and actual descendant compiler acceptance is required for compiled pullback; a compatible arbitrary replacement need not be acyclic. Full manuscript VerifyDW, ChargeSoundness and Package E, global certificate discovery, terminal-family derivation and global route coverage remain open. There is no encoded-input polynomial runtime, output-size or certificate-size theorem for the complete construction. Unconditional SaturatePositive, BCELReady and ZeroSlack, exact general PCCMin, deterministic CNFSAT in P and the eligible root remain open. Finite execution fixtures are regression evidence, not theorem authority. No fixed weighted checkpoint or global gate closes, and P = NP is not proved.

Formal artefact coverage: 246 of 248 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%.
Uncertainty range: 20% to 40%.
Global gates closed: 0 of 5.

Publication decision: defer. This closes the computational structural-reordering and literal arbitrary-support replacement-transport edge, but not full manuscript profiles, materializer transport or a global proof obligation. No fixed weighted checkpoint or global gate changes, and the published global bottom line remains unchanged. Preserve the coherent M264 website pin and batch pending core evidence at the next major publication.

The [fixed progress ledger](../status/PROOF_PROGRESS.json) is authoritative. Its risk-weighted estimate is neither confidence that the route is correct nor a delivery or time-remaining estimate.
