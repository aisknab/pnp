# Computed original-accounted frontier replacement

M253 is a computational frontier lift for one input-derived visible predecessor
cone. It is not arbitrary-support Pull/Expand across all manuscript traces.

Formal artefact coverage: 229 of 231 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%. Uncertainty range: 20% to 40%. Global gates closed: 0 of 5.

## Specification and the precise local premise

The route follows the manuscript pinned by
[the immutable archive manifest](../archive/legacy-v0/ARCHIVE.json): section 2
compatible extraction and replacement, section 4 unique physical ownership
and matched charges, section 5's full/quotient firewall, and section 6.1's
proper-support and full-discharge requirements.

For arbitrary finite input, ordinary-output and computational-field dimensions,
the inputs are the original wire carrier and a keep mask. Compute the masked
carrier without changing its program. Its actual ordinary and kept observations
seed a predecessor-closed physical cone. Extract that cone from the original
fully exposed computational word, not from the masked word. The interface
therefore includes selected wires observed by forgotten retained fields or
consumed by exterior gates.

Complete local open-function agreement is a premise: a replacement must agree
with every extracted interface value for every boundary valuation. Ordinary-output
or quotient-only agreement is insufficient. This stronger premise is stated
explicitly; it is not inferred from the M252 quotient agreement.

No support set, frontier, weight, topological order, successful compiler result
or full-result correctness certificate is supplied. The replacement and its
local agreement remain inputs; the construction does not search for them.

## Actual original-circuit construction and cost

The visible cone is predecessor closed, so no external gate-valued boundary
port enters it. This derives the primary-input boundary, well-foundedness and
success of the actual literal replacement compiler. Compiler success does not
depend on semantic agreement.

The compiler retains each original exterior gate once. Expansion recovers the
ordinary outputs and every ordered computational field from this complete
compiled word. Under complete local agreement, all those values match the
original for every valuation.

Write O for original gates, S for pulled support gates, R for replacement gates
and E for actual exterior gates.

| Object or comparison | Exact physical statement |
| --- | --- |
| Original partition | O = S + E |
| Actual expansion | R + E |
| Matched integer differences | O - S = (R + E) - R = E |
| Actual original-circuit saving | R < S |
| Proper support | S < O, equivalently E > 0 |
| Accepted proper gain | S < O and R < S |

Unlike the M252 reference lift, this accounting is against the actual original
circuit. It does not duplicate a materializer already represented by original
gates. A whole-support saving is not a proper-support certificate.

The proper regression has a two-gate tautology cone inside a three-gate
original. A zero-gate true replacement leaves the one-gate exterior once,
preserving a shared ordinary/forgotten true value and the exterior negation.
The whole-support fixture also admits a genuine saving, but its proper-gain
query rejects it because there is no exterior. Non-strict replacements are
rejected separately.

## Full-value discharge and the frontier firewall

Each lost-field discharge binds the original R5 source to the actual expanded
source at that coordinate. Its full-value witness follows from complete local
open-function agreement and literal splicing, not from a caller-supplied final
equality.

A negative regression gives the replacement the correct ordinary true output
but an incorrect forgotten retained predecessor. The complete frontier exports
both values. The kernel proves that this replacement does not satisfy the
required local agreement. Matching the visible output cannot bypass that test.

## Reviewed general interfaces

All eighteen interfaces build from the explicit PNP root, with exact reviewed
kernel-type fingerprints and dependencies propext and Quot.sound.

| Exact declaration | Checked interface |
| --- | --- |
| `PNP.DirectWire.WireFrontierLift.ordinary_output_selected` | Every gate-valued ordinary observation belongs to the computed cone. |
| `PNP.DirectWire.WireFrontierLift.kept_field_selected` | Every gate-valued kept field belongs to the computed cone. |
| `PNP.DirectWire.WireFrontierLift.records_predecessor_closed` | The selected gates are closed under actual physical predecessors. |
| `PNP.DirectWire.WireFrontierLift.selected_field_in_frontier` | Selected retained field producers occur on the complete extracted frontier. |
| `PNP.DirectWire.WireFrontierLift.primary_boundary` | Every external boundary port is a primary input, not an external gate. |
| `PNP.DirectWire.WireFrontierLift.original_charge` | The original gate count partitions into pulled support and actual exterior. |
| `PNP.DirectWire.WireFrontierLift.compile_isSome` | The actual compiler succeeds from the derived boundary for every well-typed replacement. |
| `PNP.DirectWire.WireFrontierLift.expanded_charge` | The expansion pays replacement gates plus each original exterior gate once. |
| `PNP.DirectWire.WireFrontierLift.matched_original_charge` | Original-minus-pulled and expanded-minus-replacement charges agree as integers. |
| `PNP.DirectWire.WireFrontierLift.original_gain_iff` | An actual original-circuit saving is equivalent to strict local saving. |
| `PNP.DirectWire.WireFrontierLift.expanded_output` | Complete local open-function agreement preserves every ordinary output. |
| `PNP.DirectWire.WireFrontierLift.expanded_field` | The same agreement preserves every ordered computational field. |
| `PNP.DirectWire.WireFrontierLift.expanded_equivalent` | The expanded and original ordinary-output words are equivalent. |
| `PNP.DirectWire.WireFrontierLift.discharge_source_exact` | A full discharge identifies the actual expanded source at the lost coordinate. |
| `PNP.DirectWire.WireFrontierLift.discharge_full_value` | That source has the original full value for every valuation. |
| `PNP.DirectWire.WireFrontierLift.proper_iff_exterior_positive` | Support properness is equivalent to a nonempty actual exterior. |
| `PNP.DirectWire.WireFrontierLift.checkedProperGain_isSome_iff` | The proper-gain query accepts exactly proper support and strict local saving. |
| `PNP.DirectWire.WireFrontierLift.ProperGain.checked` | An accepted result carries properness, actual strict gain, all fields and exact cost. |

The [Lean source](../lean/PNP/NANDWireFrontierLift.lean),
[root-importing regressions](../lean-regression/PNPWireFrontierLift.lean),
[exact axiom audit](../lean-audit/PNPWireFrontierLiftAxiomAudit.lean) and
[publication contracts](../audits/lean-wire-frontier-lift0.test.mjs) retain this
same boundary. The [recorded plan](./plans/2026-09-12-computed-original-accounted-frontier-lift.md)
states the dependency edge and the remaining obligations.

## Regression and hostile evidence

Fixtures cover proper and whole support, strict and non-strict replacements,
all-kept and all-forgotten masks, repeated and reordered computational sources,
mixed masks, constant and primary-input fields, no ordinary outputs and empty
dimensions. The negative frontier theorem and finite fixture equivalences are
kernel checked. Runtime execution is test evidence, not theorem authority.
The exported theorems quantify over arbitrary finite dimensions.

Contracts reject supplied cones or results, omitted or duplicated exterior cost,
reference-only accounting, weaker local agreement, non-strict or improper gains,
unbound discharge sources, finite-only types, extra axioms, stale fingerprints
and widened claims.

## Remaining boundary and publication decision

This is a computational frontier lift for one input-derived visible predecessor cone, not arbitrary-support Pull/Expand across all manuscript traces. The keep mask, replacement and complete local open-function agreement are inputs. Ordinary-output or quotient-only agreement does not imply the stronger completed-frontier premise. No replacement search or automatic local agreement is proved. A whole-support saving is not a proper-support certificate. The construction does not complete the full manuscript carrier, noncomputational profile fields, R6/R7 and general obligation-dependency DAG calculus, N1-N10, Package E, global routing, unconditional SaturatePositive, BCELReady or ZeroSlack, exact PCCMin or total encoded-size polynomial construction, runtime, output and certificate bounds. No fixed weighted checkpoint or global gate closes. Deterministic CNFSAT in P and the eligible root remain absent, and P = NP is not proved.

No fixed checkpoint or global gate closes. Publication decision: defer PNPLabs.
The coherent M231 public source pin remains unchanged. This computational cone
does not establish every arbitrary support across N1-N10, the full carrier or
obligation calculus. A physical count identity is not a total polynomial
execution, output-size or certificate-size theorem.
