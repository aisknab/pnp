# Source-derived closed histories and literal arbitrary-support replacement

As of PNP-FORMAL-RECONSTRUCTION-STATUS-2026-09-15-264.

Formal artefact coverage: 240 of 242 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%. Uncertainty range: 20% to 40%. Global gates closed: 0 of 5.

M264 closes the missing dependency between actual support extraction, closed computational histories and the existing literal graph compiler. The [milestone plan](plans/2026-09-15-source-derived-history-arbitrary-support.md) anchors the construction in sections 2/2.2, 4/5 and 6.1/6.2 of the [pinned manuscript](../archive/legacy-v0/ARCHIVE.json).

## Exact verified scope

For arbitrary finite candidate, support and raw-event dimensions, the constructor extracts actual computational fields with zero duplicate ordinary outputs, executes the existing closed R5/R6/R8 history, and derives literal-splice acyclicity from original gate labels and actual causal bounds. The current carrier and every pending snapshot preserve those bounds through actual normalization, source-identity cancellation and paid captured restoration. The existing literal graph compiler then succeeds for every accepted closed history without a caller-supplied replacement, rank, order or semantic certificate. All ordered original outputs are preserved; every exterior gate occurs once; final gates plus actual removals equal original gates plus all actual materializer charges. Strict gain follows when removals exceed charges. The splice stage introduces no rejection beyond the actual history compiler.

## Why literal replacement now compiles

The actual extractor labels primary inputs by zero and an original gate j by j+1. Its interface dependency levels stay below their original producer labels. The computational carrier exposes these interfaces as fields with zero duplicate ordinary outputs, so it does not accidentally protect gates that physical normalization may remove.

The invariant follows the current carrier and every pending captured snapshot. It is preserved by actual constant propagation, sharing, output-cone pruning, the existing three-pass normalizer, literal source-identity R6 and fully charged captured-materializer R8. Boolean equivalence alone is insufficient; the existing equivalent-but-cyclic counterexample still rejects.

For R replacement gates, assign exterior original gate j the rank (j+1)*(R+1), and replacement gate k the rank level(k)*(R+1)+(k+1). Every actual dependency edge decreases that rank. Thus the literal splice stage succeeds for every accepted closed history, without an additional caller-supplied acyclicity or correctness witness.

The exact physical accounting remains:

```text
final physical gates + actual history removals
  = original physical gates + actual materializer charges
```

Strict gain follows only when removals exceed charges. Restoration is not free, and finite termination plus gate accounting is not a polynomial-runtime theorem.

## Audited construction

- [Actual extraction bounds](../lean/PNP/ResidualTerminalSupportExtraction.lean).
- [Physical-normalization bounds](../lean/PNP/NANDNormalizationCausalBounds.lean).
- [Current-carrier and snapshot transport](../lean/PNP/NANDWireHistoryCausalBounds.lean).
- [Literal graph rank and compiler](../lean/PNP/NANDArbitrarySupportSplice.lean).
- [Raw history-to-splice constructor and final theorems](../lean/PNP/NANDWireHistoryArbitrarySupport.lean).

The [explicit-root axiom audit](../lean-audit/PNPWireHistoryArbitrarySupportAxiomAudit.lean) checks all 24 reviewed theorem closures. The [permanent regression](../lean-regression/PNPWireHistoryArbitrarySupport.lean) checks general theorem types and small executable cases covering interleaved exterior boundaries, paid restoration, genuine strict saving, empty dimensions, rejected unfinished histories and the cyclic-equivalence obstruction. These fixtures are regression evidence, never proof authority. The [source contracts](../audits/lean-wire-history-arbitrary-support0.test.mjs) and [compiled publication contracts](../audits/lean-wire-history-arbitrary-support-publication0.test.mjs) reject hidden assumptions, changed physical constructions, weakened theorem types, missing evidence and overstated scope.

## Remaining obligations

This covers the existing computational R5/R6/R8 language and three-pass physical normalizer, not all manuscript R1-R9 or N1-N10 rules, full R7, arbitrary observers, noncomputational carriers, full-profile compatibility, matched-kappa Pull/Expand or complete Package E. The support records and raw history remain input data. It does not derive terminal families, a globally successful rewrite strategy, global route coverage, unconditional SaturatePositive, BCELReady or ZeroSlack, exact general PCCMin or complete encoded-input polynomial runtime/output/certificate bounds. Boolean equivalence alone is not a causal certificate. Runtime fixtures are regression evidence, not proof authority. No fixed weighted checkpoint or global gate closes; deterministic CNFSAT in P and the eligible root remain absent, and P = NP is not proved.

## Publication and progress decision

Publication decision: publish after M264 is earned and merged. The general source-derived one-copy replacement capability materially changes the public bottom line; batch the deferred M263 result into the same PNPLabs release. The website must consume the exact verified core merge and byte-identical generated artefacts, not rebuild Lean. A full site-surface audit, deployment and independent production verification remain required.

No fixed weighted checkpoint changes state. Formal artefact coverage increases independently of the unchanged risk-weighted estimate in the [canonical progress ledger](../status/PROOF_PROGRESS.json). The estimate is not a probability of correctness or a delivery forecast.
