# M173 terminal Packet-budget no-lower composition

## Evidence-led selection

M167 closes the checked Packet branch over supplied finite grouped data. M172
closes the independently terminal-derived finite budget branch over every
canonical support. Both milestones explicitly leave their composition open,
while the declared ZeroSlack boundary still requires a complete no-lower
ledger. The next bounded step is therefore to put these two accepted branches
behind one fail-closed Boolean over the same direct-wire candidate.

## Target

For every finite direct-wire candidate and supplied terminal/Packet data:

1. bind the Packet realizer table's current implementation to that candidate;
2. recompute the complete canonical terminal budget no-lower checker;
3. recompute the five-row Packet no-lower checker;
4. accept only when both component checkers accept;
5. reflect the Boolean to the exact conjunction of the component propositions;
6. prove acceptance makes every budget-feasible canonical support a semantic
   minimum and excludes every feasible strict-equivalent-gain witness; and
7. prove the same acceptance excludes a positive Packet conclusion.

The named endpoint is
`PNP.DirectWire.terminal_packet_budget_no_lower_composition_excludes_gain_and_packet`.

## Claim boundary

The caps, Packet family, typed payloads, finite rank map, realizer claims,
activity environment, dependency rows, and before/after residual ranks remain
explicit inputs. Sharing the direct-wire candidate does not derive the Packet
data from terminal saturation. M173 is a finite two-branch composition, not
the manuscript's complete no-lower ledger. It does not implement HN/BUD
grammar, polynomial HResolve or BudgetResolve, normalization, remaining named
routes, saturation, replay, unconditional ZeroSlack, PCCMin exactness or
polynomial runtime, concrete SAT, the root theorem, or project-axiom removal.

## Required evidence

- root import and compiled theorem-inventory inclusion;
- an explicit axiom transcript for every public declaration;
- regression fixtures for joint acceptance, budget-gain rejection, and the
  polymorphic positive-Packet rejection interface;
- hostile mutations for omitted component checks, candidate decoupling,
  weakened semantic conclusions, fabricated acceptance, assumptions, fixed
  bounds, and claim widening;
- publication-map and formal-status rows with reviewed kernel fingerprints;
- current reconstruction, audit, pipeline, terminology, README, and canonical
  report documentation; and
- durable CI and aggregate-verifier coverage.

## Verification order

Run the focused source audit first, then build the new dependency chain on the
configured remote build host before the axiom transcript and regression.
Regenerate the compiled theorem inventory, publication payloads, and canonical
report only after the theorem surface stabilizes. Finish with the complete
core suite and a fresh exact-head reproduction. PNPLabs will consume those
verified artifacts byte-for-byte and will not compile Lean again.
