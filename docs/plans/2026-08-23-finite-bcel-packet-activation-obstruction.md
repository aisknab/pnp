# M185 finite BCEL/Packet activation-coherence obstruction

## Evidence-led selection

The formal status still names `Formal.ZeroSlack` as a blocker. M183 computes a
finite BCEL-ready nucleus and proves that every governed nonempty proper cut
has one exact projection defect. M184 maps that exact nucleus bijectively onto
the grouped Packet carrier used by the report-facing ZeroSlack boundary. The
same M184 certificate also proves that the supplied Packet family cannot have
constant activation. What remains missing is therefore not another carrier
identity: it is the numerical bridge from the terminal projection defect to
the Packet cut value and activation weights.

M185 makes that missing edge executable and fail-closed. It checks the declared
Packet cut value and every canonical nonempty proper Packet cut against the
computed terminal defect. Acceptance reconstructs the exact activation-
coherence proposition and the corresponding mapped-cut projection equation.
Because M184 excludes constant activation on the same family, the checker must
reject; a deterministic classifier returns either the cut-value mismatch or a
concrete proper cut whose activation weight disagrees with the terminal defect.

## Legacy anchor and unbounded abstraction

- Legacy anchor: the canonical manuscript's Sections 12 and 16, specifically
  the step that identifies the BCEL constant-cut quantity with the activation
  weights consumed by the BN6 Packet contradiction.
- Closed edge: computed M183 terminal defect + exact M184 carrier map ->
  executable activation-coherence check -> proof-bearing exact obstruction on
  the same M180 Packet family.
- Unbounded abstraction: arbitrary input, gate, output, profile, anchor,
  payload, rank, and carrier dimensions; every canonical nonempty proper cut
  of the supplied finite Packet carrier is checked.

## Exact theorem target

For every M184
`TerminalFiniteBCELPacketCarrierCoherenceCertificate`, define:

- its exact terminal defect from the selected M183 nucleus;
- a proposition requiring the Packet cut value and all nonempty proper Packet
  activation weights to equal that defect;
- a total Boolean checker equivalent to that proposition;
- a mapped-cut theorem showing that accepted coherence identifies each
  terminal proper cut's mapped Packet activation with its M183 projection
  excess; and
- a deterministic obstruction classifier returning either a cut-value
  mismatch or a proof-bearing proper-cut activation mismatch.

Prove
`PNP.terminal_finite_bcel_packet_activation_obstruction_checked_complete`:
the exact checker is false and one of those two explicit obstructions exists.
Expose the same result through the report-facing `ZeroSlackCertificate` without
adding a caller flag or another copy of the family.

## Claim boundary and downstream blockers

The terminal problem, initial positivity proof, grouped family, anchor
bijection, activation cells and masses, payloads, budget, typed realizer table,
dependency table, and rank maps remain supplied finite inputs. M185 diagnoses
the precise missing equality; it does not prove that activation coherence
holds. On the current accepted family it proves the opposite, as required by
the already checked Packet exclusion.

This milestone does not derive the Packet family or activation weights from
positive residual slack, construct BN3 through BN6 data from every valid input,
complete global routing or the no-lower ledger, prove unconditional
`SaturatePositive`, `BCELReady`, `ZeroSlack`, PCCMin exactness or polynomial
runtime, put SAT in P, remove a project assumption, or prove `P = NP`. It does
not change any fixed risk-weighted progress checkpoint, so the 30 percent score
and 20--40 percent uncertainty range remain unchanged. It adds one earned
formal-publication row only.

## Required evidence

- root import and compiled theorem-inventory inclusion;
- a complete axiom transcript for every new public declaration, excluding
  `Classical.choice` and project assumptions;
- generic regression of checker reflection, mapped-cut projection equality,
  mandatory rejection, both obstruction shapes, and report-facing access;
- hostile mutations rejecting caller success flags, detached defects or
  families, sampled or fixed cuts, assumptions, and widened claims;
- formal-status, fixed progress-ledger, and publication-map evidence with
  reviewed kernel fingerprints;
- reconstruction, bridge, pipeline, terminology, audit, README, and canonical
  report documentation; and
- durable CI and aggregate-verifier coverage.

## Verification and deduplication

Run lightweight source and contract checks before remote proof feedback. On
the capped remote builder, compile the changed dependency and root once, then
run the axiom transcript and regression. After source stabilization, regenerate
the theorem inventory and publication artifacts and run the complete core suite
once. The progress ledger must advance its current coordinate and coverage
while preserving the immutable M184 scoring baseline. Reuse identical-tree
evidence at later merge boundaries where the trust inputs are unchanged.
PNPLabs will consume exact merged core artifacts and validate publication,
browser, provenance, and deployment boundaries without rebuilding Lean.
