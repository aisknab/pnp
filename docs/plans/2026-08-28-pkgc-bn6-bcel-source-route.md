# M202: PkgC-derived BN6/BCEL source route

## Legacy anchor and dependency edge

- Legacy anchors: the pinned manuscript's Section 11.5
  `Separating-consumer theorem`, especially
  `separatingConsumersSingletonized`, and Section 11.6
  `BN6-HypergraphPacket`, especially `constantCutHypergraphHypotheses` and
  the passage from surviving PkgC keys to nonnegative BN6 footprint cells.
- Current formal boundary: M201 classifies an arbitrary finite active PkgC
  source ledger into an exact same-key cancellation or a constructed raw BN6
  positive-cell ledger with exact all-cut activation conservation. M200
  independently consumes a supplied raw BN6 positive-cell ledger and returns
  conditional ZeroSlack or one exact singleton/pair BCEL activation mismatch.
- Exact edge closed by M202: feed the BN6 cells constructed by M201 into the
  canonical checked M200 boundary. Return the exact PkgC cancellation if
  cellization fails, genuine ZeroSlack if the checked downstream branch closes,
  or reflect M200's raw-cell mismatch back to the original PkgC source
  activation ledger.

This is an arbitrary-finite theorem. It must not fix the source-cell count,
carrier, cut, candidate, circuit, rank count, payload type, restoration type,
or downstream selector count.

## Unbounded abstraction and theorem target

Define a cellized downstream bundle parameterized by the proof that every
listed PkgC source system is singletonized. The bundle may retain the currently
supplied checked Packet/HB fields:

- typed realizer table;
- accepted-claim equation;
- dependency table and checked HB closure; and
- route-clear equation.

Its raw BN6 positive cells, supports, footprint-size proofs, canonical grouped
family, carrier, and cut value must be derived from the PkgC source cells and
the checked BCEL-ready nucleus. No raw positive-cell list may be an independent
field.

Define a total source-level classifier that first runs
`classifyTerminalPkgCBN6Cellization`. In the cellized branch it must construct
the exact `PCCMinCheckedPacketBN6BCELCanonicalGroupingHBData` input and run the
M200 sparse activation classifier. Its three outcomes must retain:

1. `ZeroSlackResult candidate.toImplementation`;
2. the exact source cell, list membership, separating pair, and
   `TerminalPkgCSameKeyCancellationRealization`; or
3. one nonempty proper carrier sublist of length at most two whose original
   PkgC source activation weight differs from the checked BCEL defect.

The public endpoint will be
`PNP.DirectWire.pccmin_checked_packet_pkgc_bn6_bcel_source_route_or_zeroslack_checked_complete`
with the exact conclusion

```text
residualSlack candidate.toImplementation = 0
or an exact source-member PkgC same-key cancellation realization exists
or an exact singleton/pair proper cut witnesses
  terminalPkgCBN6SourceActivationWeight sourceCells cut != checkedBCELDefect.
```

The mismatch rewrite must use M201's all-cut conservation theorem. It must not
accept an erased route label, supplied raw-cell mismatch, sampled-cut premise,
or independently supplied BN6 ledger.

## Claim boundary and downstream blockers

The terminal problem, checked finite BCEL-ready certificate, active V54 source
consumer systems, active cuts, positive payload atoms, typed restorer, realizer
table, claims, rank assignment, dependency table, checked HB closure, selector
silence, and route-clear result remain supplied. The source systems and their
payloads are not yet constructed from every valid terminal candidate.

A returned PkgC cancellation or BCEL activation mismatch is exact evidence,
but is not yet a verified gain or globally rank-decreasing transition. M202
does not construct upstream BN3 through BN5 data, complete PkgC/BN3--BN6 under
one terminal-derived family, derive blocker semantics, prove semantic
dependency completeness, prove manuscript-wide `SaturatePositive` or
`BCELReady`, establish unconditional `ZeroSlack`, or construct complete
encoded-size polynomial `PCCMin`.

No fixed progress checkpoint changes state. The risk-weighted estimate remains
35 percent, the uncertainty range remains 20 to 40 percent, and zero of five
global gates remain closed. Formal artefact coverage may change independently
when the publication row is earned.

## Required evidence

- compilation of the arbitrary-finite cellized downstream bundle, total
  three-way classifier, source-ledger mismatch reflection, public endpoint,
  and explicit `PNP` root import;
- regression coverage for the PkgC-cancellation branch, the source-ledger
  activation-mismatch branch, and the conditional-ZeroSlack branch;
- exact checks that constructed positive cells preserve source length, payload
  order, supports, and all-cut activation before entering canonical grouping;
- hostile checks rejecting an independently supplied raw BN6 cell list,
  supplied footprint-size certificate, fixed carrier, erased cancellation,
  powerset enumeration, and claims of complete route integration,
  unconditional ZeroSlack, or polynomial PCCMin;
- axiom transcripts for every reviewed declaration, rejecting project axioms,
  `sorryAx`, and `Classical.choice`; and
- synchronized theorem inventory, publication map, status, progress history,
  report, durable workflow expectations, audit questions, and current
  documentation.

## Stop condition

If the M201 constructed BN6 ledger cannot inhabit the exact M200 canonical
grouping boundary, or if a raw-cell activation mismatch cannot be rewritten to
the original source activation ledger for the same cut, stop at that theorem
edge. Do not replace it with supplied raw cells, a supplied equality
certificate, a fixed finite instance, a new axiom, `sorry`, a weakened theorem,
or a broader completion claim.
