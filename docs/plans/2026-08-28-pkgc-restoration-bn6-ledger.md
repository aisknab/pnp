# M205: PkgC restoration-coverage BN6 ledger

## Legacy anchor and dependency edge

- Legacy anchor: the pinned manuscript's Section 11.5 separating-consumer
  theorem and its transition into Section 11.6 BN6 cellization. Every surviving
  quotient source cell must either expose a concrete PkgC route or satisfy the
  singletonization premise used to construct its BN6 footprint.
- Current formal boundary: M201 scans an arbitrary finite active PkgC source
  ledger and constructs the complete BN6 positive-cell ledger, but it reaches
  the singletonized branch through an always-total supplied typed restorer.
  M204 removes that restorer for one consumer system by preserving the exact
  Hall branch and computing the coverage-derived ambient BN4 consequence, but
  it does not lift that classifier over the complete PkgC source ledger or
  reconnect the all-singletonized branch to BN6 cellization.
- Exact edge closed by M205: attach one finite restoration universe and ambient
  BN4 ledger to each active source cell, scan the complete source ledger in
  list order with M204's total classifier, preserve the first exact Hall,
  ambient-reduction, or ambient-mismatch result, and construct the BN6 positive
  cells with exact payload order and all-cut activation conservation only when
  every source system is proved singletonized.

This is one arbitrary-finite family theorem. It must not fix the carrier,
source-ledger length, consumer count, separating-pair size, restoration size,
ambient-ledger size or order, activation key, payload, candidate, rank, or cut.

## Unbounded abstraction and theorem target

Define one enriched source cell containing:

1. an existing proof-bearing active PkgC/BN6 source cell;
2. its exact finite restoration-coordinate universe; and
3. its ambient BN4 activation-cell ledger.

Project an arbitrary finite enriched ledger to the existing source ledger.
When every enriched cell is singletonized, construct the existing BN6 positive
cells and prove:

1. exact source/BN6 cell-count equality;
2. exact source-order payload preservation; and
3. exact source/BN6 activation-weight equality on every cut.

Define a total first-obstruction classifier whose outcomes are:

1. all source systems singletonized;
2. the first exact M204 Hall deficit and forced `qRestorationHall` route;
3. the first coverage realization, computed ambient remainder, embedding, and
   exact residual reduction; or
4. the first coverage realization, generated missing cell, and proof that no
   exact ambient embedding exists.

The public endpoint will be
`PNP.DirectWire.terminalPkgC_restorationCoverage_bn6_cellization_checked_complete`.

## Claim boundary and downstream blockers

The active source cells, cuts, payloads, restoration universes, coordinate maps,
full-restoration coordinate lists, and ambient BN4 ledgers remain explicit
inputs. M205 removes the typed-restorer seam from the complete family boundary,
but it does not derive those objects from every valid terminal candidate or
prove semantic full-restoration adequacy.

A Hall deficit remains an exact local Q route rather than a verified gain or
globally rank-decreasing transition. An ambient reduction does not prove its
remainder empty, and an ambient mismatch remains compatibility evidence rather
than a complete global route. M205 therefore does not finish PkgC/BN3-BN6,
derive HN/BUD/HB semantics, prove global route coverage, SaturatePositive,
BCELReady, unconditional ZeroSlack, executable polynomial PCCMin, deterministic
CNFSAT in P, a global gate, the eligible root theorem, or P = NP.

No fixed progress checkpoint changes state. The risk-weighted estimate remains
35 percent, the uncertainty range remains 20 to 40 percent, and zero of five
global gates remain closed. Formal artefact coverage may change independently
when the new publication row is earned.

## Required evidence

- compilation of the arbitrary-finite enriched source cell, source projection,
  all-singletonized BN6 construction, total first-obstruction classifier,
  public endpoint, and explicit `PNP` root import;
- regression coverage for an all-singletonized multi-cell ledger and for Hall,
  computed-reduction, and ambient-mismatch outcomes reached after an earlier
  singletonized source cell;
- exact checks that the classifier invokes M204 for every source cell and that
  the successful branch reuses M201's computed BN6 construction rather than
  accepting singletonization, supports, payloads, or conservation equations;
- hostile checks rejecting a typed restorer, supplied BN6 ledger, supplied
  classifier result, erased route labels, fixed family sizes, powerset scans,
  or widened global claims;
- axiom transcripts for every reviewed declaration, rejecting project axioms,
  `sorryAx`, and `Classical.choice`; and
- synchronized theorem inventory, publication map, status, progress history,
  report, workflows, audit questions, and current documentation.

## Stop condition

If M204's one-cell result cannot be lifted over an arbitrary finite source
ledger while preserving the exact first obstruction and constructing the BN6
ledger only from proved singletonization, stop at that theorem edge. Do not
replace it with a typed restorer, supplied singletonization proof, supplied BN6
cells, fixed instance, new axiom, `sorry`, weakened theorem, erased failure
label, or broader completion claim.
