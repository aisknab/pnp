# M206: Candidate-bound PkgC restoration-coverage BN6/BCEL route

## Legacy anchor and dependency edge

- Legacy anchors: the pinned manuscript's Section 11.5 separating-consumer
  theorem and Section 11.6 BN6 hypergraph-packet transition. A surviving
  nonsingleton separating pair must retain its exact restoration route; only a
  ledger in which every source system is singletonized may enter BN6
  cellization and the checked Packet/HB boundary.
- Current formal boundary: M205 scans an arbitrary finite enriched PkgC source
  ledger without a typed restorer and either preserves the first exact Hall,
  ambient-reduction, or ambient-mismatch result, or constructs the complete BN6
  positive-cell ledger after proving every source system singletonized. M202
  independently enters the checked BN6/BCEL sparse activation route, but it
  still reaches cellization through an always-total typed restorer.
- Exact edge closed by M206: specialize the M205 ledger to one computed BCEL
  nucleus, require every ambient BN4 ledger to use that nucleus's canonical
  atoms, preserve all three M205 obstruction branches literally, and feed only
  the all-singletonized M205 branch into the existing checked BN6/BCEL sparse
  classifier. Reflect any downstream raw-cell mismatch back to the original
  enriched source activation ledger on the same cut.

This is one arbitrary-finite theorem. It must not fix the candidate dimensions,
anchor carrier, source-ledger length, consumer count, restoration-universe or
ambient-ledger size or order, rank count, selector count, activation cut, or
payload values.

## Unbounded abstraction and theorem target

Define candidate-bound source data containing:

1. one finite saturate-positive problem and checked BCEL-ready certificate;
2. an arbitrary finite M205 restoration-coverage source ledger whose source and
   BN4 activation atoms are the computed primitive-record type;
3. a proof that every cell's ambient BN4 ledger uses the same computed BCEL
   nucleus's canonical atoms; and
4. checked downstream Packet/HB data parameterized by the proof that every
   source consumer system is singletonized.

The total classifier must return exactly one of:

1. genuine conditional `ZeroSlackResult` from the checked downstream branch;
2. the first exact restoration Hall deficit and its `qRestorationHall` route;
3. the first computed ambient remainder, exact embedding, and residual
   reduction, tied to the same BCEL nucleus;
4. the first generated missing ambient cell and proof that no exact embedding
   exists, tied to the same BCEL nucleus; or
5. one nonempty proper singleton/pair cut whose original enriched source
   activation weight differs from the checked BCEL defect.

The public endpoint will be
`PNP.DirectWire.pccmin_checked_packet_pkgc_restoration_coverage_bn6_bcel_route_or_zeroslack_checked_complete`.

## Claim boundary and downstream blockers

The terminal problem, checked BCEL-ready certificate, source cells and active
cuts, restoration universes and coordinate maps, ambient ledgers, ranks,
claims, dependency table, checked HB closure, route-clear result, and selector
silence remain explicit inputs. M206 removes the typed-restorer seam from the
complete checked BN6/BCEL route and binds its ambient ledgers to the same
candidate nucleus, but it does not derive those objects from every valid input
or materialize semantically adequate full candidates.

A Hall deficit remains a local `Q` route rather than a verified gain or global
rank decrease. An exact ambient reduction does not prove its remainder empty,
and an incompatibility or activation mismatch is not yet embedded in the
complete decreasing route system. M206 therefore does not finish
PkgC/BN3--BN6, HN/BUD/HB semantics, global route coverage,
`SaturatePositive`, `BCELReady`, unconditional `ZeroSlack`, executable
polynomial `PCCMin`, deterministic CNFSAT in P, a global gate, the eligible root
theorem, or P = NP.

No fixed progress checkpoint changes state. The risk-weighted estimate remains
35 percent, the uncertainty range remains 20 to 40 percent, and zero of five
global gates remain closed. Formal artefact coverage may change independently
when the new publication row is earned.

## Required evidence

- compilation of the candidate-bound arbitrary-finite source data, total
  five-way classifier, public endpoint, and explicit `PNP` root import;
- regression coverage preserving Hall, ambient-reduction, ambient-mismatch,
  conditional-ZeroSlack, and source-activation-mismatch outcomes;
- exact checks that the successful branch uses M205's projected source cells,
  constructed BN6 ledger, and all-cut activation conservation rather than an
  independently supplied BN6 list or mismatch;
- hostile checks rejecting a typed restorer, supplied BN6 ledger, erased
  obstruction, ambient ledger detached from the checked nucleus, fixed family
  sizes, powerset scans, or widened global claims;
- axiom transcripts for every reviewed declaration, rejecting project axioms,
  `sorryAx`, and `Classical.choice`; and
- synchronized theorem inventory, publication map, status, progress history,
  report, durable workflow expectations, audit questions, and current
  documentation.

## Stop condition

If M205's dependent all-singletonized branch cannot inhabit the existing
checked BN6/BCEL boundary, if any Hall or ambient obstruction must be erased,
or if the downstream raw mismatch cannot be rewritten to the original enriched
source ledger for the same cut, stop at that theorem edge. Do not replace it
with a typed restorer, supplied BN6 cells, supplied route labels, fixed finite
instances, a new axiom, `sorry`, a weakened theorem, or a broader completion
claim.
