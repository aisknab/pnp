# PkgC restoration-coverage charge-descent milestone

## Legacy anchor

The pinned manuscript's Section 11.5 PkgC full-restoration branch removes a
same-key balanced contribution before the Section 11.6 BN6 transition. The
fixed `RankWF` order later names charge size as the eighth coordinate of the
ten-coordinate residual rank. M206 already constructs the exact balanced BN4
subledger for every fully covered nonsingleton separating pair, embeds that
subledger into the ambient ledger with multiplicity, computes the remainder,
and proves that the complete canonical residual ledger is unchanged. It does
not yet prove that this transition decreases the formal rank.

## Unbounded abstraction

The result ranges over arbitrary activation, semantic, transport, frontier,
charge-owner, obligation, origin, kernel, and mode-projection types, every
finite nonempty consumer antichain, every nonsingleton disjoint separating
pair, every restoration universe, every arbitrary-order ambient BN4 ledger,
and every exact multiset remainder. No fixed circuit, consumer count, ledger
length, rank value, source coordinate, or schedule slot occurs in the theorem
interface.

## Exact theorem boundary

Define unsigned BN4 charge as the sum of every cell's natural mass. Prove:

1. unsigned charge is additive under list concatenation and invariant under
   exact permutation;
2. the canonical coverage-derived cancellation ledger has unsigned charge
   `2 * (pair.left.length + pair.right.length)`;
3. that charge is strictly positive because both listed consumers are
   nonempty;
4. an exact M206 ambient embedding decomposes ambient charge into removed
   charge plus remainder charge;
5. the remainder therefore has strictly smaller unsigned charge; and
6. for every fixed assignment of the other nine `TerminalResidualRank`
   coordinates, the computed remainder rank is `LexLT` the ambient rank by the
   `chargeSize` coordinate.

Construct one proof-bearing charge-descent value from the exact embedding. Do
not accept a rank, inequality, success flag, or descent certificate from the
caller. Compose it with M206 so the successful ambient-reduction constructor
is upgraded mechanically while ZeroSlack, Hall, ambient incompatibility, and
activation mismatch remain distinct.

The public theorem is
`pccmin_checked_packet_pkgc_restoration_coverage_charge_route_or_zeroslack_checked_complete`.
Its ambient branch exposes exact residual-ledger preservation, strict unsigned
charge decrease, and exact context-parametric ten-coordinate rank descent.

## Downstream blockers

This milestone closes only the ranked-transition seam for M206's successful
ambient extraction branch. It does not prove that the remainder is empty and
does not turn residual-ledger preservation into unconditional ZeroSlack.
Hall deficits, missing ambient cells, and source activation mismatches remain
diagnostic routes rather than verified gains or global rank descents. The
terminal problem, source systems, restoration universes, ambient ledgers,
semantic full candidates, Packet ranks and claims, HB dependency table,
checked closure, route-clear result, and selector silence remain supplied
rather than constructed from every valid terminal input.

The remaining load-bearing work is to derive the source/restoration/ambient
objects from terminal input, compile every other returned route into a genuine
gain or formal rank decrease, complete PkgC/BN3--BN6 under one terminal-derived
family, establish HN/BUD/HB semantics and semantic dependency completeness,
prove manuscript-wide SaturatePositive and BCELReady, close unconditional
ZeroSlack, and construct exact polynomial PCCMin.

## Conservative progress decision

M207 adds one earned formal publication row and one newly scoped row. It does
not close a fixed risk-weighted checkpoint or a global proof gate. The
risk-weighted proof-completion estimate therefore remains 35 percent with the
20 to 40 percent uncertainty range. Formal artefact coverage advances from
182 of 184 to 183 of 185 current scoped rows.

## Regression and hostile evidence

- Compile the unsigned-charge algebra, positive-removal theorem, exact
  decomposition, strict scalar descent, context-parametric rank descent, and
  M206 composition.
- Exercise every classifier branch and prove that only the ambient-reduction
  branch is upgraded to the constructed charge descent.
- Derive the axiom transcript from every declaration in the module.
- Reject a length-only or membership-only measure, omitted mass summation,
  caller-supplied inequality or rank proof, lost permutation multiplicity,
  non-strict rank comparison, reordered rank coordinates, dropped M206
  branches, fixed finite fixtures in the public theorem, and overclaiming
  documentation.

## Release gates

Run the focused Lean regression and axiom audit first, then the focused hostile
audit. Reconcile the compiled inventory, publication map, formal status,
canonical report, proof-progress ledger, workflow, and current documentation.
Run one complete capped remote verification, publish a focused draft PR,
require every normal check, merge manually, and reproduce the exact merge from
a fresh clean remote checkout. Synchronize that exact core merge into PNPLabs,
perform the required full publication-surface audit without recompiling Lean,
merge and reproduce the exact site commit, deploy it through the narrow
deployer, and independently verify production provenance and bytes.
