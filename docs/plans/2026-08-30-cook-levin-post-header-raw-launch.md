# Cook-Levin post-header raw-launch milestone

## Legacy anchor

The pinned legacy report's final step explicitly uses SAT NP-completeness after
placing SAT in P. The active reconstruction must therefore complete one
uniform polynomial-time concrete Cook-Levin builder and reduction rather than
accumulate more fixed token slots. M209 supplied an all-coordinate raw header
router, M210 decoded every post-header semantic route, and M211 supplied a
fixed all-input raw quotient/remainder divider. The next dependency is to join
those exact interfaces without supplying a route or arithmetic witness.

## Unbounded abstraction

M212 ranges over every concrete polynomial-time verifier, every source input,
and every natural token coordinate. It reads the exact remainder from M209's
checked terminal configuration and conditionally constructs M211's canonical
divider input. For every coordinate inside the complete formula-token
schedule it derives the M210 body, finish, or out-of-range classification; no
coordinate class, quotient, remainder, route certificate, or execution trace
is supplied as proof authority.

## Exact theorem boundary

Add `PNP.Concrete.CookLevin.BuilderPostHeaderRawLaunch` with:

1. an executable all-coordinate handoff from M209's decoded raw endpoint to
   M211's exact divider dispatcher;
2. exact M209 and M211 work traces at the selected natural inputs;
3. exact quotient/remainder recovery for all M210 body, finish, and
   out-of-range routes;
4. an in-range theorem excluding the out-of-range post-header branch for every
   finite complete-schedule coordinate;
5. one source-size polynomial bounding the sum of the two compiled raw traces;
   and
6. a public checked endpoint with no supplied route or trace premise.

## Downstream blockers

M212 is executable Lean orchestration of two exact fixed machines. It is not a
single literal tape-rewrite bridge between their configurations, does not
preserve the complete builder workspace through such a rewrite, and does not
emit the selected body or finish token. The complete formula builder,
`FunctionProgram.RawRefinement`, encoded-size output proof, concrete
`PolynomialReduction`, CNFSAT NP-hardness and NP-completeness transport, and a
deterministic CNFSAT decider remain open.

The residual-band minimiser, unconditional ZeroSlack, polynomial PCCMin, the
eligible root theorem, and the publication gate also remain open.

## Conservative progress decision

This closes the exact post-header orchestration boundary but not the fixed
complete-builder checkpoint or any global proof gate. The risk-weighted
proof-completion estimate therefore remains 35 percent with the 20 to 40
percent uncertainty range. Formal artefact coverage is regenerated from the
authoritative publication ledger after its new row is reconciled.

## Regression and hostile evidence

- Compile the all-coordinate remainder, launch, route, and combined-bound
  theorem interfaces with warnings treated as errors.
- Derive the axiom transcript from every public declaration in the module.
- Require the exact M209 terminal reader, M211 divider trace, all three M210
  route classes, the finite in-range theorem, and the source-size polynomial.
- Reject supplied route data, fixed coordinate fixtures, semantic-only
  arithmetic, hidden enumeration, a missing bound, or wording that calls the
  orchestration a literal bridge or complete formula builder.

## Release gates

Run the focused Lean regression and axiom audit first, followed by the focused
hostile audit. Reconcile the root import, reviewed theorem-name interface,
compiled inventory, publication map, formal status, canonical report,
proof-progress ledger, durable workflow, and current documentation. Run one
complete capped remote verification, publish a focused draft PR, require every
normal check, merge manually, and reproduce the exact merge from a fresh clean
remote checkout. Synchronize that exact core merge into PNPLabs, perform its
full publication-surface audit without rebuilding Lean, merge and reproduce
the site commit, deploy it through the narrow deployer, and independently
verify production provenance and bytes.
