# Cook-Levin post-header raw-tape bridge milestone

## Legacy anchor

The pinned legacy report's final complexity transport requires one uniform
polynomial-time Cook-Levin formula builder. M209 supplied the fixed
all-coordinate header router, M210 supplied the exact post-header semantic
decoder, M211 supplied the fixed unary quotient/remainder machine, and M212
proved an all-coordinate executable orchestration of those components. M212
explicitly left the physical machine boundary open: Lean constructed M211's
input tape outside the raw transition system and did not preserve the builder
workspace through a literal tape rewrite.

## Unbounded abstraction

M213 ranges over every concrete polynomial-time verifier, source input,
post-header coordinate, positive problem-derived clause width, and finite
builder workspace. M209's equality and greater-than paths reach the same
rejecting halt through two different final raw transitions. M213 replaces that
shared terminal target with two branch-preserving entry states in one fixed
finite bridge table while retaining the exact M209 terminal tape bytes. The
bridge derives zero or the strict positive shifted unary remainder from that
raw control/tape state, copies it and the exact width into a fresh divider
region, and places the head at an M211-ready entry protected by a left boundary.
The arbitrary workspace behind the router boundary must remain byte-for-byte
unchanged. No remainder, quotient, route witness, output tape, or successful
execution trace may be supplied as proof authority.

## Exact theorem boundary

Add `PNP.Concrete.CookLevin.BuilderPostHeaderRawTapeBridge` with:

1. one collision-free fixed finite bridge table over literal `WorkTape` cells;
2. branch-preserving entry states for the two M209 post-header terminal
   transitions, with an active tape exactly equal to the corresponding M209
   equality or greater-than endpoint and extended only by the problem-derived
   unary width, a fresh shield, and arbitrary exterior builder workspace;
3. exact equality and strict-post-header raw traces that derive zero or the
   exact positive shifted remainder from those states, copy the width, and
   reach a shielded M211 input tape;
4. an exact theorem that the builder workspace is unchanged and inaccessible
   behind the final divider shield;
5. an exact M211 trace on the shielded tape, preserving the complete exterior;
6. exact compilation of the bridge and shielded divider traces to the concrete
   three-symbol machine, a one-step-short timeout boundary, and one external
   source-size polynomial bound for every in-range coordinate; and
7. a public checked endpoint covering every M210 body and `Finish` route without
   supplied branch, arithmetic, workspace, or trace certificates.

## Downstream blockers

M213 ends with the exact quotient/remainder available on a literal preserved
workspace. It does not select or append the corresponding body or `Finish`
token, iterate this subroutine through the complete schedule, construct the
complete formula builder, establish builder `FunctionProgram.RawRefinement`,
prove the complete encoded output and construction-time bounds, or package the
concrete `PolynomialReduction`. CNFSAT NP-hardness and NP-completeness,
deterministic `CNFSAT in P`, the locked-NAND-to-residual-band route, the
residual-band minimiser, unconditional ZeroSlack, polynomial PCCMin, the
eligible root theorem, and the publication gate remain open.

## Conservative progress decision

This closes one physical handoff dependency inside the open complete-builder
checkpoint. It does not close that fixed checkpoint or any global gate. The
risk-weighted proof-completion estimate therefore remains 35 percent with the
20 to 40 percent uncertainty range. Formal artefact coverage is regenerated
from the authoritative publication ledger after the new row is reconciled.

## Regression and hostile evidence

- Compile equality, zero-width rejection, strict positive remainder, arbitrary
  remainder, arbitrary positive width, and arbitrary-workspace exact traces.
- Require the raw table to consume the canonical M209 terminal layout through
  its two branch-preserving terminal entry states rather than a host-constructed
  remainder or prebuilt M211 input.
- Require the final M211-ready tape, complete exterior preservation, exact
  shielded divider run, compiled simulation, one-step-short timeout, and the
  source-size polynomial bound.
- Derive the axiom transcript from every public declaration in the module.
- Reject supplied route or arithmetic witnesses, precomputed quotient/remainder
  fields, finite coordinate fixtures, hidden enumeration, semantic-only tape
  construction, workspace loss, missing bounds, or wording that calls M213 a
  body-token emitter or complete formula builder.

## Release gates

Run the focused Lean regression and axiom audit first, followed by the focused
hostile audit. Reconcile the root import, reviewed theorem-name interface,
compiled inventory, publication map, formal status, canonical report,
proof-progress ledger, durable workflow, and current documentation. Run one
complete capped remote verification, publish a focused draft PR, require every
normal check, merge manually, and reproduce the exact merge from a fresh clean
remote checkout. Synchronize that exact core merge into PNPLabs, perform its
full publication-surface audit without rebuilding Lean, merge and reproduce the
site commit, deploy it through the narrow deployer, and independently verify
production provenance and bytes.
