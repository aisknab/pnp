# Cook-Levin post-divider raw route-classifier milestone

## Legacy anchor

The pinned manuscript's final complexity transport depends on the standard
Cook-Levin reduction being realised by one uniform polynomial-time raw formula
builder.  The active formal reconstruction names that dependency in the open
`reductions-complete-cook-levin-builder` checkpoint.  M210 supplied the exact
semantic clause/token/`Finish` decoder, M211 supplied raw unary division, and
M213 reached the exact quotient/remainder on a literal tape while preserving
the builder workspace.  The next unresolved edge is to classify that physical
post-divider endpoint before any arbitrary token can be selected or emitted.

## Unbounded abstraction

M214 ranges over every concrete polynomial-time verifier, source input,
in-range formula coordinate, positive problem-derived token width, arbitrary
clause count, arbitrary collision-free exterior prefix, and arbitrary builder
workspace.  One fixed finite tape-rewrite table must consume the canonical M213
shielded-divider endpoint, copy the exact problem-derived clause count from its
workspace sidecar, and construct a protected M209 comparison input from the
divider's literal quotient marks.  The existing fixed comparator must then
classify quotient below the clause count as a body row and quotient equal to the
clause count as the unique `Finish` row.  No route result, quotient, remainder,
clause coordinate, token coordinate, comparison tape, or successful trace may
be supplied as proof authority.

## Exact theorem boundary

Add `PNP.Concrete.CookLevin.BuilderPostDividerRawRouteClassifier` with:

1. one collision-free fixed finite bridge table over literal `WorkTape` cells;
2. exact traces from both M213 equality and greater-than shielded-divider
   endpoints with the exact formula-clause-count sidecar and arbitrary trailing
   workspace;
3. a physical rewrite that preserves the complete M213 exterior, restores the
   sidecar, retains the exact divider remainder ledger behind a fresh boundary,
   and exposes the divider quotient as M209 coordinate marks followed by the
   copied clause count;
4. an exact shielded M209 comparison trace that cannot cross the new boundary;
5. exact agreement, for every in-range post-header coordinate, between the raw
   comparison result and M210's typed body or `Finish` route, including exact
   clause/token quotient and remainder coordinates in the body case;
6. compiled six-raw-steps-per-work-step traces, one-step-short timeout evidence,
   and one verifier-derived source-size polynomial bound; and
7. the public endpoint
   `cook_levin_builder_post_divider_raw_route_classifier_checked_complete`.

## Downstream blockers

M214 will classify the physical post-divider route but will not inspect the
selected clause payload, choose its `Option CNFToken`, append body or `Finish`
bits, iterate the complete schedule, construct the complete raw formula,
establish builder `FunctionProgram.RawRefinement`, prove the final encoded
construction bound, or package the concrete `PolynomialReduction`.  Concrete
CNFSAT NP-hardness and NP-completeness, deterministic `CNFSAT in P`, the
residual-band minimiser, unconditional ZeroSlack, polynomial PCCMin, the
eligible root theorem, and the publication gate remain open.

## Conservative progress decision

This retires one general physical routing dependency inside the still-open
complete-builder checkpoint.  It does not close a fixed checkpoint or global
gate.  The risk-weighted proof-completion estimate therefore remains 35
percent with the 20 to 40 percent uncertainty range.  Formal artefact coverage
will be regenerated from the authoritative publication ledger.

## Regression and hostile evidence

- Compile zero and positive quotient, body and unique `Finish`, arbitrary
  remainder, arbitrary positive width, arbitrary clause count, both canonical
  M213 branches, arbitrary safe exterior prefixes, and arbitrary workspaces.
- Require the bridge to consume the literal M213 terminal tape and the exact
  problem-derived clause-count sidecar rather than a host-constructed comparison
  tape or supplied route result.
- Require sidecar restoration, exterior preservation, shielded comparison,
  exact typed route agreement, compiled execution, one-step-short timeout, and
  the external polynomial bound.
- Derive the axiom transcript from every public declaration in the module.
- Reject finite coordinate fixtures, fixed-slot continuation, supplied
  quotient/remainder or route witnesses, semantic-only tape construction,
  hidden enumeration, workspace loss, missing bounds, or wording that calls
  M214 a token emitter or complete formula builder.

## Release gates

Run the focused Lean regression and axiom audit first, then the focused hostile
audit.  Reconcile the root import, reviewed theorem-name interface, compiled
inventory, publication map, formal status, canonical report, proof-progress
ledger, durable workflow, and current documentation.  Run one complete capped
Atlast verification, use a focused draft PR with all normal checks, merge
manually, and reproduce the exact merge from a fresh clean Atlast checkout.
Then synchronize that exact core merge into PNPLabs, perform its full
publication-surface audit without recompiling Lean, merge and reproduce the
site commit, deploy it through the narrow launcher, and independently verify
production provenance and bytes.
