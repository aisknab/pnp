# Cook-Levin post-divider selected-token launch milestone

## Legacy anchor

The pinned manuscript's final complexity transport requires one uniformly
polynomial Cook-Levin formula builder. The active dependency is the open
`reductions-complete-cook-levin-builder` checkpoint. M214 now classifies the
exact literal post-divider quotient as a body row or the unique `Finish` row,
while preserving the exterior and arbitrary builder workspace. The next
unresolved edge is to derive the corresponding padding or token action and
connect it to the already-audited fixed token appender without supplying the
route or token as proof authority.

## Unbounded abstraction

M215 ranges over every concrete polynomial-time verifier, source input, and
post-header coordinate in the complete problem-derived token schedule. It
also ranges over arbitrary classifier workspace and arbitrary appender
exterior. The selected entry must come from the canonical direct schedule at
that coordinate. A padding entry must preserve the emitted-token prefix; a
populated body or `Finish` entry must run the fixed 59-rule appender and append
exactly that selected token.

## Exact theorem boundary

Add `PNP.Concrete.CookLevin.BuilderPostDividerSelectedTokenLaunch` with:

1. a proof-carrying embedding of every post-header coordinate into the full
   token schedule;
2. exact agreement between M210's decoded route interpreter, direct token
   lookup, and the canonical schedule entry;
3. a canonical emitted-token prefix obtained by removing padding slots before
   the selected coordinate;
4. exact prefix evolution: padding leaves the prefix unchanged and a populated
   entry appends exactly its selected token;
5. M214 physical classifier evidence at every embedded coordinate with
   arbitrary classifier workspace;
6. exact fixed-appender work and compiled traces for every populated entry,
   plus a one-step-short nonhalting result;
7. one verifier-derived source-size polynomial dominating M214 and the
   selected appender stage; and
8. the public endpoint
   `cook_levin_builder_post_divider_selected_token_launch_checked_complete`.

No route witness, token request, schedule entry, trace, or successful branch
certificate may be supplied to the public endpoint.

## Downstream blockers

M215 is executable Lean orchestration between the exact M214 classifier and
the literal appender. It does not yet implement the literal tape-to-tape
selector that chooses an appender state from the physical route and clause
payload, iterate the complete schedule, construct the complete raw formula,
establish builder `FunctionProgram.RawRefinement`, prove the final encoded
construction bound, or package the concrete `PolynomialReduction`. Concrete
SAT hardness/NP-completeness transport, deterministic `CNFSAT in P`, the
residual-band minimiser, unconditional ZeroSlack, polynomial PCCMin, the
eligible root theorem, and the publication gate remain open.

## Conservative progress decision

This retires the general executable selected-token handoff inside the still
open complete-builder checkpoint. It does not close a fixed checkpoint or
global gate. The risk-weighted proof-completion estimate remains 35 percent
with the 20 to 40 percent uncertainty range. Formal artefact coverage is
regenerated from the authoritative publication ledger.

## Regression and hostile evidence

- Cover padding, arbitrary populated body tokens, and the unique `Finish`.
- Require selection from the canonical schedule via M210's decoded route.
- Require the existing fixed raw appender, exact next-prefix equality,
  compiled execution, one-step-short nonhalting, and the external bound.
- Retain M214's all-coordinate physical classifier proposition.
- Derive the axiom transcript from every public declaration in the module.
- Reject finite coordinate fixtures, supplied route/token inputs, schedule
  enumeration, precomputed output, weakened prefix equality, hidden
  assumptions, missing bounds, or wording that calls this a literal composite
  builder or complete reduction.

## Release gates

Run the focused Lean regression and axiom audit, then the focused hostile
audit. Reconcile the root import, reviewed theorem-name interface, compiled
inventory, publication map, formal status, canonical report, proof-progress
ledger, durable workflow, and current documentation. Run one complete capped
remote-builder verification, use a focused draft PR with all normal checks,
merge manually, and reproduce the exact merge from a fresh clean checkout.
Then synchronize the exact core merge into PNPLabs, perform its full
publication-surface audit without recompiling Lean, merge and reproduce the
site commit, deploy it through the narrow launcher, and independently verify
production provenance and bytes.
