# M224: Cook-Levin first-body separator mirrored dispatch

## Selection rationale and legacy anchor

The pinned legacy manuscript's `TraceEquivalence` lemma and final SAT-decision
section require one uniform concrete SAT construction. The active
reconstruction tracks the unfinished literal builder under
`Formal.ConcreteSAT` and the open
`reductions-complete-cook-levin-builder` checkpoint.

M223 executes the full classifier's unique `Finish` path through the reflected
physical dispatcher, but every populated body route still stops at the
classifier's distinct accepting geometry: the head remains on the first
unconsumed clause-count unit and the right end marker has not yet become a
request cell. The first post-header coordinate is uniformly populated by the
separator beginning the first canonical clause. It therefore supplies the
smallest non-instance-specific body route on which to close that different
physical handoff without introducing a caller-selected coordinate or token.

## Bounded theorem target

Derive the zero-valued first-body index from the controller and prove that its
canonical schedule entry is `CNFToken.sep`. Run M220's complete 711-rule
classifier at that index with the canonical emitted header workspace. Prove
that its terminal is the body/accept branch and that the remaining positive
clause-count word ends at the protected right boundary.

Add one fixed two-rule right scanner which crosses exactly that remaining unary
word, changes only its end marker into M217's physical separator request, and
halts. Chain the scanner to a fixed blank-sentinel orientation pass and M223's
reflected dispatcher. The resulting single collision-free literal machine
must reach the exact next canonical emitted prefix, with exact work and
compiled traces, one-step-short nonhalting, a fixed rule count and one
source-input-size polynomial bound.

## Exact theorem boundary

Add
`PNP.Concrete.CookLevin.BuilderPhysicalClassifierFirstBodySeparatorMirroredDispatch`
and the endpoint:

```text
PNP.Concrete.CookLevin.BuilderPhysicalClassifierFirstBodySeparatorMirroredDispatch.cook_levin_builder_physical_classifier_first_body_separator_mirrored_dispatch_checked_complete
```

The endpoint takes only a `VerifierTableauProblem`. It accepts no supplied
coordinate, request token, classifier result, scan trace, emitted prefix,
machine, polynomial bound or success certificate.

## Downstream blockers preserved

M224 closes only the first populated body coordinate. It establishes the
right-end-marker geometry and physical separator handoff needed by later body
routes, but it does not select arbitrary body tokens, derive padding requests,
connect every classifier outcome, or iterate successive configurations in one
literal machine.

It does not prove complete builder `FunctionProgram.RawRefinement`, package the
Cook-Levin `PolynomialReduction`, establish concrete NP-hardness or
NP-completeness, put `CNFSAT` in `P`, close a fixed checkpoint or global gate,
create the eligible root theorem, or prove `P = NP`.

The fixed risk-weighted score therefore remains 35 percent with the 20 to 40
percent uncertainty range. Formal artefact coverage is regenerated separately.

## Evidence and release gates

- Build the new module under the pinned Lean toolchain on the configured remote
  builder.
- Add exact first-coordinate, separator selection, body-terminal geometry,
  request writing, orientation, reflected dispatch, complete work and compiled
  trace, one-step-short, fixed-rule and polynomial-bound regressions plus a
  declaration-complete axiom audit.
- Add hostile checks that reject a caller-supplied coordinate or request,
  omitted classifier/scanner/orienter/dispatcher stages, incomplete output,
  missing traces or bounds, hidden assumptions, and widened all-route, loop,
  reduction or complexity claims.
- Import the module through the root and theorem inventory, then regenerate the
  canonical inventory, publication map, status, progress ledger and report.
