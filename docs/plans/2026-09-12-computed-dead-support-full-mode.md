# M246: computed dead-support full-mode acceptance

## Legacy anchor and dependency edge

The pinned manuscript's section 5 Package E boundary requires a proper compatible
support, an explicit same-carrier replacement and a closed final obligation
ledger. Its mode firewall forbids treating projected equality as full equality.
M245 now computes the actual dead-support frame and proper physical replacement,
but deliberately does not accept arbitrary full profiles or open obligations.

Close the next checked-acceptance edge for this actual non-exhaustive physical
construction: computed proper support -> complete finite profile comparison ->
explicit obligation-role scan -> proof-bearing full-carrier result with closed
observed obligations, or a precise rejecting outcome.

This is not derivation of the manuscript carrier, its semantic dependency graph,
or its R1-R9 discharge ledger. The existing finite profile observation system
remains input data. No invariance or correctness assertion is supplied by the
caller, and no unconditional checkpoint receives credit.

## Unbounded construction and exact target

For arbitrary `current : Implementation inputs outputs` and arbitrary finite
`system : TerminalProfileSystem inputs outputs profileWidth`:

1. Run M245's actual proper physical gain constructor.
2. If none is returned, report no proper dead-support gain; do not claim that
   there is no whole-circuit gain or that the implementation is minimum.
3. Reuse the existing complete profile mismatch scan on the actual replacement.
   Return its first mismatching coordinate, role and canonical agreeing prefix.
   Do not use a quotient projection or recompute Boolean equivalence.
4. If all coordinates agree, scan obligation-role coordinates in canonical order.
   Return the first still-open obligation with its complete preceding closed
   obligation prefix.
5. Accept only if both scans pass. Construct the existing
   `TerminalFullCarrierRealization` using M245's universal semantic theorem,
   and prove observed obligations discharged on both current and replacement.

The central acceptance theorem is:

```lean
(classifyDeadSupportFullMode system current).tag = .accepted ↔
  (deadSupportProperGain current).isSome = true ∧
  (∀ coordinate, system.observe (deadSupportReplacementImplementation current) coordinate =
    system.observe current coordinate) ∧
  system.ObligationsDischarged (deadSupportReplacementImplementation current)
```

Prove complete branch soundness, exact properness, complete Boolean semantics,
full-profile minimum invariance as a specification result, physical size/slack
accounting and strict descent for accepted results. The recognizer must contain
no exhaustive subset, implementation, valuation or reference-minimum search.
Do not route a mismatch or open obligation to a fictitious completed global route.

The exact model boundary matters: checking every supplied finite coordinate and
its obligation bits is not a proof that all manuscript carrier dependencies,
R5 creation or R6-R8 discharges were derived. Observer computation is not assumed
polynomial, so this gives no encoded-size polynomial runtime claim.

## Source and expectation changes together

Add arbitrary-dimension type regressions and exact axiom/type inventory coverage
with the implementation. Test genuine acceptance, a profile mismatch, a retained
open obligation despite profile equality, a non-obligation true coordinate,
zero profile width, empty/no-dead and all-dead implementations, and a mismatch
hidden by a quotient projection. Runtime fixtures are bounded tests, not proof
authority.

Update the root import, audit, reviewed-name producer/consumer, public interface,
publication fingerprints, status fields and permanent workflow together.
Hostile tests must reject skipped coordinates, wrong roles, accepting open
obligations, supplied correctness, finite-only or weakened types, axiom injection
and claims of complete manuscript admissibility or global completion.

## Verification and publication

Use one independent remote checkout with an exact predecessor-tree/toolchain
cache preflight. Check the leaf and targeted regressions first, then explicit
root, exact workflow block and kernel fingerprints. Freeze Lean source before
inventory sealing and generated publication, progress and documentation work.
Run changed-boundary hostile checks, one report reproduction and one deduplicated
standard suite. Keep normal PR/post-merge and exact-object checks; reuse unchanged
heavy evidence after reanchoring to the actual M245 merge.

Risk-weighted proof completion remains 40%, with 20% to 40% uncertainty unless
a fixed checkpoint genuinely changes. This scoped finite-profile acceptance
component is not expected to close one. Formal artefact coverage is separate.
Global proof gates remain open, the eligible root absent and publication false.

Publication decision: defer PNPLabs. This internal checked-acceptance boundary
does not change the coherent published M231 bottom line. Notify meaningful
verified substeps independently of website cadence.

Remove the exact temporary checkout, fixtures, logs and transport parent after
final release verification, or a completed diagnosis of an abandoned approach.
