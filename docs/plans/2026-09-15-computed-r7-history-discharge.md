# M265: computed R7 discharge through raw computational histories

## Anchor and finite dependency path

Use section 6.1 of the manuscript pinned by
[the archive manifest](../../archive/legacy-v0/ARCHIVE.json): R5 creates
obligations, and R6, R7 and R8 discharge them in full mode. Unary cut realization
is R7; projected agreement is insufficient. Sections 4 and 6.2 require actual
physical charges and safe, traceable replacement.

The development parent is the verified M264 merge
`029153fc5d8bfc84c61d33858d0472bcbf3d3a73`, tree
`d725f0e14d1a4625c0ce991364fa4481ef3c032a`.

M257 already constructs constant/unary replacements on arbitrary finite
computational supports. M263-M264 execute R5/R6/R8 histories and derive safe
one-copy ambient replacement. Close the missing R7 edge between these existing
general constructions. Do not substitute another finite circuit, fixed schedule
slot, supplied full-value certificate, or paid-copy fallback for literal
history-to-splice safety.

## Construction and complete intended boundary

All source input, gate, output, field, support and history dimensions remain
arbitrary. A raw R7 action identifies an existing open creation and raw support
coordinates in its immutable captured carrier. Decode gate, boundary and
interface coordinates against that actual carrier; reject invalid coordinates.
The computational profile dimension remains zero.

Recognize the completed support's zero/unary boundary using the existing
source-derived constructor. Compute the replacement from the actual open
function, never a supplied truth table or implementation. Restore the requested
field from its actual captured source through that realization and its actual
materializer. Charge every appended physical gate; preserve every ordinary
output, every available full field and each other pending immutable snapshot.
The original creation identity, not merely a field index, binds the discharge.

Extend the raw action, dependency ordering, executable transition, lifecycle
record and closed-history proof together. R7 refers to its creation intrinsically.
Reject missing or reused creation identities, unrecognized boundaries and
unfinished final ledgers. Do not add a correctness certificate to the public
raw-input compiler.

## Exact proof obligations

First prove, for every unary carrier, arbitrary input labels and complete
observation, that the actual minimum constructor cannot increase its syntactic
dependency level:

```lean
CausalBound.outputLevel
    (WireUnaryRealization.implementation carrier).candidate labels observation
  ≤ CausalBound.outputLevel carrier.exposed.candidate labels observation
```

Reuse `CausalBound.source_sound`: if an observation genuinely changes between
the two unary inputs, its actual source graph must include that input's label.
This is a statement about the literal zero/one-gate constructor, not a principle
that arbitrary Boolean-equivalent programs have the same dependencies.

Derive the constant/unary local-word bounds, then prove for arbitrary recognized
support records and every primary-input labelling:

```lean
(WireUnaryArbitrarySupport.expanded carrier records small).CausalBounds labels
  (CausalBound.outputLevel carrier.implementation.candidate labels)
  (carrier.fieldLevel labels)
```

The existing `CompiledRawNandGraph.sound` field concerns Boolean evaluation,
not syntactic dependencies. Establish the bound for the actual topological
compiler construction and its computed result. Do not assume that an arbitrary
record carrying Boolean soundness has the required wiring bound.

Use that result to prove the new state transition preserves
`State.CausalInvariant`, including the original bounds of every pending
snapshot. Re-prove the extended executable history's full output/field
preservation, source-bound creation lifecycle, exact gate balance and derived
literal-splice success. The final balance remains:

```text
final gates + actual removals = original gates + actual charges
```

The existing raw history-to-splice constructor must acquire no additional
caller-supplied rank, order, agreement, causal bound or successful compilation
premise. A failed general dependency proof stops this integration; it must not
be replaced with an assumption or a finite prefix.

## Source and expectation matrix

| Changed boundary | Reconcile before expensive checks | Focused evidence |
| --- | --- | --- |
| Unary literal construction and actual compiler bounds | Exact general theorem types, constructive axiom closures, old public interfaces | Targeted Lean targets and explicit axiom probes |
| Raw support decoder and R7 materializer | Complete coordinate validation, full-field realization, actual source and charge | General type regressions and small positive/hostile computations |
| Action, dependency ordering and execution | Every existing closed declaration list, exhaustive action case and lifecycle fixture | Consumer scan across all audits and Lean regressions |
| Pending and current causal bounds | No Boolean-only causal premise, immutable snapshots, exact captured identity | General R7 transition and extended-history bounds |
| Literal history splice | Preserve the existing no-extra-rejection theorem and exact physical accounting | General theorem regression plus existing hostile cyclic-equivalence case |
| Root and inventory | Root import closure, reviewed Lean and JavaScript name sets, publication row and fingerprint keys | Source/name-set checks, then actual compiled inventory |
| Status, progress and report | Actual emitted counts and hashes, unchanged weighted checkpoints, conservative limitations | Generated check mode and publication/progress contracts |
| Workflow and package | Existing durable trigger families and exact new commands, read-only permissions | Shell syntax, size budget and closed script fixture |

Prepare theorem/type and axiom expectations with their source. Scan every
consumer of an edited module, not only the new milestone's audits. Preserve
old theorem statements, immutable coordinates and established fingerprints.
Exploratory files are temporary proof work, not earned publication evidence.

## Verification and release interaction

Keep this development checkout separate from the already-merged M263-M264
website release. Do not alter that release's exact core pin or mirrors.
Complete its deployment and requested repository backups as soon as its existing
release checks permit. Resume this proof work from its recorded checkpoint.

Use the matching verified source/toolchain cache for targeted development.
Rebuild changed dependencies and the explicit root before imported audits.
Reconcile source contracts before the deduplicated full core suite, then require
normal PR/post-merge checks and exact-object reproduction. Do not run Lean as
part of the website release.

## Progress and publication decision

Baseline at M264: formal artefact coverage is 240 of 242 current scoped rows
earned. Risk-weighted proof completion estimate is 40%, with uncertainty 20% to
40%. Global proof gates closed: 0 of 5. This plan awards no new credit.

No fixed checkpoint is expected to close. Support selection and the raw history
remain data, not a derived global strategy. Full manuscript R1-R9/N1-N10,
noncomputational carriers and profiles, complete Package E, terminal-derived
families, global routing, unconditional SaturatePositive, BCELReady and ZeroSlack,
exact general PCCMin and complete encoded polynomial runtime, output and
certificate bounds remain open. The eligible root and deterministic CNFSAT in P
remain absent.

Publication decision: defer a separate PNPLabs cycle for this integration.
Retain the coherent M264 public pin and batch the earned result into the next
major publication. Notify genuine verified substeps and the earned core milestone
without conflating artefact coverage with weighted proof completion.

## Compiler and unary-bound integration checkpoint

The first checked prerequisites are now maintained in
NANDTopologicalCausalBounds.lean and NANDWireUnaryCausalBounds.lean.
The only existing compiler API addition is RawNandCompilationState.finish_position,
which exposes the actual completed position without altering compilation.
Its closed declaration list and exact signature are maintained together in
audits/lean-arbitrary-support-splice0.test.mjs; the positive and hostile
source contracts are the focused consumer checks. The explicit root imports
both new modules, and the dynamic root-closure contract covers their reachability.

The general compiler theorem assumes a graph cap inequality as an intermediate
proof predicate, not as a new executable input. The next splice proof must derive
those caps from the actual original program and computed replacement using
extractTerminalSupport_causal_levels. No weighted checkpoint, milestone row,
publication gate or complete M265 claim is earned by this prerequisite alone.

The source-derived whole-carrier bound is now proved for the actual recognized
R7 constructor, including ordinary outputs and all computational fields.
The maintained regression is
[PNPComputedR7CausalBounds.lean](../../lean-regression/PNPComputedR7CausalBounds.lean).
It checks the general no-extra-certificate interfaces and exhibits a Boolean-sound
compiler record with an artificial dependency, protecting the actual-result
premise. Raw-history actions, source-bound discharge and lifecycle integration
are still pending; this checkpoint earns no milestone row or weighted credit.

## Final raw-input contract review

Before sealing the inventory, include general list-decoder round-trip and exact
coordinate-preservation theorems, plus an if-and-only-if recognition theorem:
raw R7 succeeds precisely when every coordinate decodes against the captured
carrier and the actual completed boundary has at most one port. These quantify
over arbitrary finite lists and carrier dimensions; finite runtime probes remain
regressions, not the authority for recognition or completeness.

The final audit must bind those decoder theorems to the computed realization,
source-bound discharge, full lifecycle and exact charged literal splice. No
fixed weighted checkpoint changes merely because these interface obligations
are made explicit.

## Explicit-root verification checkpoint

The complete explicit root and fresh compiled inventory now include the computed
R7 integration. All existing reviewed type fingerprints remain unchanged. Both
changed literal CI audit blocks, the causal and R7 regressions, and the existing
root-imported raw-history regression pass. Earlier prerequisite checkpoint notes
above describe their state at that time. Current status/report sealing, the full
core publication validation and normal PR/merge checks remain release gates.

Formal artefact coverage: 241 of 243 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%.
Uncertainty range: 20% to 40%.
Global gates closed: 0 of 5.

No weighted checkpoint changes. Publication decision: defer the website batch.
