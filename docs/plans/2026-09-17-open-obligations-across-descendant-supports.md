# M269: open obligations across descendant supports

## Parent and release boundary

Develop against the exactly verified M268 feature
`c08e6d278547c1a1957d777f770a19184e2eedf5`, tree
`e7e3fe6f32d0bcca60fb1bd69bbeba09cb2267b4`, in an isolated checkout.
M268's PR checks are still running at plan creation. Keep its release branch
frozen. Before publishing this successor, bind a fresh branch to the exact M268
merge, preserve the reviewed source changes, and finish the parent's release
gates. No child milestone is earned merely because the parent feature is green.

The parent release is now complete at merged commit
`39f90f85d373cecb6aa2ca6ca54ef205e8b51dfb`, with the same tree recorded
above. Integration uses that exact merge as its base. The initial feature
coordinate remains only as plan-creation history; all parent PR, post-merge and
independent exact-merge checks have passed.

## Manuscript anchor and dependency edge

The pinned manuscript's section 6.1 Package E permits R5 to create obligations,
permits only full-mode R6/R7/R8 to discharge them, and requires the final ledger
to be closed. Section 6.3 composed splice also requires full-mode obligation
compatibility. Section 4 requires actual materializer charges and unique physical
ownership. The specification is the manuscript pinned by
[the archive manifest](../../archive/legacy-v0/ARCHIVE.json).

M268 verifies complete offered programs on proper computational supports, but
each local history closes its obligations before another support is selected.
The missing edge is a complete source-derived program that preserves an actual
open ambient obligation ledger through arbitrary intervening support splices,
then discharges the same creations and checks closure at the program boundary.

The finite path is state and snapshot transport through actual support splices,
a raw mixed-program executor with computed dependency order, full final closure,
and the proper-support verifier interface. Full-profile and remaining rule-family
integration are still needed for complete manuscript Package E.

## Unbounded input and exact target

Input, output, field and gate dimensions, support sizes, event counts, dependency
graphs, stage lists and program lengths are arbitrary finite data.

The public compiler must start from the source carrier and raw actions. Inputs
may identify support records, raw local programs, creations and predecessors;
they may not supply intermediate carriers, schedules, snapshots, successful
execution, full-value witnesses, physical owners, charges or wiring ranks.

The internal splice bridge has this shape:

```lean
def execute
    (before : WireObligationHistory.State source)
    (raw : WireDescendantCertificate.RawCertificate) :
    Option (Receipt before raw)
```

Its returned state must preserve the exact pending snapshot function, preserve
ordinary outputs and currently available full fields, and add only the actual
nested run's cumulative charges/removals. The internal state parameter is not an
extra premise at the final raw-source entry point.

The complete mixed program must execute every primitive or support operation in
its computed order, reject invalid identities/references/lifecycles and rejected
local programs, and require an empty final ambient ledger. Final closure must
yield all full field values, actual execution accounting and source-derived
causal bounds. A failed tail must never return a successful prefix.

For the proper-support certificate wrapper, complete execution and final closure
must derive the outer literal splice. Proper physical support and strict final
saving remain separate computed checks; temporary expansion is allowed.

## Required invariants

- Preserve each pending creation's exact identity, captured carrier and full-value
  connection, not only the number of pending entries.
- A support splice cannot create, erase or discharge an ambient obligation.
- Reuse the actual R5/R6/R7/R8 operations. A projected value is not a full-mode
  cancellation witness, and a read of an open ambient field must reject.
- Keep local and ambient event identities distinct. Derive any compound namespace
  from the actual raw program positions; do not accept caller-supplied owner maps.
- Preserve literal input-label dependency bounds for current values and immutable
  snapshots, so the final splice does not need supplied acyclicity.
- Retain actual run/compiler receipts and physical position transport. Conserve
  original gates and every historical allocation, including allocations later
  removed; prove unique ownership and exact charge/removal balance.
- Decode every selected support against the actual preceding descendant.
- Return no full certificate with a live ambient obligation, failed local stage,
  malformed coordinate, whole outer support or final nondecrease.

## Stop rule and remaining blockers

Prove the general mixed-program result. Do not replace it with a fixed example,
a prefix-only compiler, supplied correctness, an added coverage premise, or a
rule that forces every intermediate ambient ledger to close.

The existing computational wire-carrier model is not the full manuscript
profile universe. Inner programs retain their existing language; exposing a
quotient wire must not be described as establishing its original full value.
No global certificate discovery, terminal-family derivation, complete Package E,
matched-kappa Pull/Expand, global routing, unconditional ZeroSlack, exact general
PCCMin or encoded-input polynomial execution/certificate-size bound is claimed
without its own complete proof.

## Verification and publication

Use module builds and exact axiom closures first. Test general theorem types and
executable regressions: an obligation surviving multiple different support
splices before actual restoration, reads before/after closure, unavailable or
duplicate creations, colliding local/global identifiers, failed tails, zero
dimensions, temporary expansion and actual historical charges.

Before broad verification, reconcile explicit root imports, reviewed theorem
names/type pins, status producer/validator fields, closed package fixtures, exact
durable workflow blocks, progress history and current documentation. Then
regenerate the inventory/status/report once source is stable and run one
deduplicated complete suite. Keep PR, exact-object release and post-merge checks
as distinct boundaries; reuse unchanged evidence within each boundary.

No fixed weighted checkpoint is assumed closed by this computational extension.
The current estimate remains 40% with uncertainty 20% to 40%, and zero global
gates closed. Compute evidence-row coverage only after the complete milestone is
earned. Decide publish/defer from the proved end-to-end capability and actual
public claim boundary, never from an added row or milestone count.

## Integration contract and check matrix

The mathematical source and eight regression groups are targeted-verified.
Before changing the release producers, maintain these dependent contracts as
one integration boundary. Frozen source and declaration contracts come from
reviewed code and successful compiled evidence, not from self-updating tests.

| Producer or change | Consumers reconciled in the same phase | First rejecting check |
| --- | --- | --- |
| Nine open-program Lean modules | Explicit root imports, closed source/declaration contracts, regression imports | Source-contract mutations and root-closure preflight |
| Reviewed theorem-name set | Lean inventory producer, JavaScript required names, publication row, fingerprint key set, explicit-root axiom audit | Exact name-set comparison before root and inventory |
| Complete execution, pending-ledger lifecycle and physical ownership | Compiled type pins, status producer and validator, scope/non-claim text, current documentation | Hostile type, premise, axiom and status mutations |
| Milestone audit script | Closed package-script fixture and aggregate verifier | Independent package-field and script mutation checks |
| Durable workflow commands | Exact checked shell blocks and axiom transcript contracts | Extracted-block shell syntax, then exact execution after prerequisites |
| Generated inventory, publication, progress and report | Canonical mirrors, current documentation, source seal and links | Generated check modes and focused publication/progress contracts |

Build the explicit root and compiled inventory only after the cheap producer/
consumer checks pass. Derive actual type fingerprints and counts from that
compiled environment; then reconcile the generated publication, progress and
report chain before the deduplicated full suite. Reuse unchanged targeted
module and regression evidence without treating it as full release evidence.

## Website publication decision

Publication decision: defer. This closes the computational mixed-program
obligation-lifecycle and physical-accounting edge for offered certificates,
but does not establish full manuscript VerifyDW, global certificate discovery
or an unconditional checkpoint. No global proof gate or fixed weighted
checkpoint changes, and the published global bottom line remains unchanged.
Preserve the coherent M264 website pin and batch pending core evidence at the
next major publication.
