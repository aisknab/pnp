# Wire-backed computational profiles and exposure balance

Status: the general construction, seven arbitrary-dimension regression contracts,
six hostile/edge-case regressions, and normalization/checked-splice compatibility
have passed the explicit-root build and regression. The exact root axiom
transcript checks all 35 reviewed declarations against the compiled inventory,
using only standard propext and Quot.sound where needed. All earlier theorem
pins are preserved. Canonical publication/status generation, focused release
contracts and the deterministic report have passed. Normal release validation
found an orchestration-size issue; shortening display labels without changing
commands repaired it, and the affected workflow guards now pass. The remaining
release gates are exact-revision reproduction, normal PR checks and merge. No
fixed checkpoint or progress score changes.

## Intended dependency

The pinned manuscript at
`final-pnp-proof-report-docs-hardened-7072f8d-sealed` distinguishes full
realizations from quotient comparisons and requires positivity preservation or
a real named route in RW-SaturatePositive. Reconstruct the computational-field
part of that boundary from actual wire sources, not a caller-supplied observer
or an assertion that physical cost equals the increase in semantic minimum.

The ordinary Boolean outputs are mandatory in both modes. An internal
computational field is a literal input, constant or gate wire evaluated under
every input valuation. Masking removes only a profile comparison constraint by
substituting a literal constant slot. It does not remove an ordinary output,
allocate gates, discharge a proof-only obligation or make a quotient witness
usable as full evidence.

## General theorem and reconstruction boundary

For arbitrary finite input, output and field widths, construct full/quotient
matching, exhaustive reference minima and attained wire-backed witnesses.
Derive the exact full-lift condition and lower bounds against every realization.

For exposure masks before <= after which both retain a fixed comparison mask,
prove:

- The full minimum is monotone.
- The quotient minimum is unchanged.
- The exact decrease in full slack equals the increase in projection defect.
- Full slack plus projection defect is conserved.
- If full slack falls to zero from a positive starting value, an attained
  quotient witness has a strict deficit and cannot be used as a full replacement
  without restoring every omitted field.

This is a proposed computational-profile reconstruction. It does not assert
the manuscript's stronger forced-cost transparency statement. Ordinary outgoing
interface growth still requires the existing zero-cost branch or a genuinely
proved route. The old observer-based terminal family is not silently identified
with this new carrier type.

The all-gate tuple constructor reads the source program, but is not declared to
be the canonical manuscript profile or terminal family. Keeping every internal
gate value may impose unnecessary constraints on replacements.

## Checked integration

The proof reuses `WireCarrier.normalize_output`, `normalize_field` and
`splice_checked` to show that existing checked transformations preserve this
full mode and both semantic minima. A checked splice still requires an equivalent
open replacement and successful compilation; no such witness is assumed to
exist globally or produced from a quotient-only comparison.

## Expectations prepared with the source

| Producer | Consumers / expected boundary | First check |
| --- | --- | --- |
| Actual wire fields and literal mask | General full/quotient/lift theorem types; no ordinary-output erasure | Leaf Lean build and source-only hostile tests |
| Reference minima and attained witnesses | Exact gate-count and universal lower-bound theorems; explicit exhaustive/non-polynomial limitation | Exact axiom probe and bounded regression |
| Exposure states | Exact two-measure conservation and monotonicity; no forced-cost premise | General Lean theorem types and hostile mutations |
| Checked normalization/splicing | Complete output and field equality before either minimum is reused | Targeted integration proof and axiom check |
| Root and theorem inventory | Explicit import, Lean name producer, JavaScript required names, publication pins and fingerprint keys | Name-set and root-source preflight before extraction |
| Public status and report | Current progress, claim flags, documentation, package fixture and workflow assertions | Generate canonical values before targeted current-surface checks |

Preserve every earlier theorem pin and milestone row. Update the complete
producer/expectation chain before broad validation. Do not invent compiled
fingerprints or counts. Keep required historical tags available in fresh clones.

## Verification and release order

1. Preserve the checked research source and its limitations.
2. Prepare this follow-on change in a separate checkout while M273 finishes.
   Do not alter or push the M273 release branch.
3. After M273 merges, create the public feature branch from that actual merge
   coordinate; carry over only the reviewed new source.
4. Compile the changed leaf, then the explicit root in that checkout before any
   importing axiom audit. Reconcile inventory producers and consumers before
   extracting and sealing compiled evidence.
5. Run focused positive/hostile checks, normal core validation and exact-head
   reproduction. Reuse unchanged verification only at the same trust boundary.
6. Use the draft PR, all-checks, manual-ready/manual-merge and exact-merge process.

## Remaining obligations and publication decision

The complete ten-role profile grammar, governed terminal-derived families,
ordinary-interface routing, positive-slack activation, rank-decreasing global
routes, unconditional SaturatePositive, BCELReady and ZeroSlack remain open.
Reference minimization is exhaustive; it proves no polynomial-time PCCMin,
encoded-size bounds, deterministic SAT theorem or eligible root.

Publication decision: defer. No fixed weighted checkpoint, global proof gate or
public global bottom line changes. Preserve the coherent website source pin.
Formal artefact coverage may change after an earned merge; risk-weighted proof
completion does not change merely for these local/general components.

See the canonical [progress ledger](../../status/PROOF_PROGRESS.json) and
[pinned archive](../../archive/legacy-v0/ARCHIVE.json). Do not count this plan or
its private probe as an earned publication row.
