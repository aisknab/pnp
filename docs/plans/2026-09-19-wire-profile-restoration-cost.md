# Wire-profile restoration cost and semantic-minimum compatibility

Status: implemented with compiled publication evidence sealed. The explicit-root
build, exact standard-axiom closures, arbitrary-dimension contracts, guarded
executable examples and kernel-checked limitation/recovery example have passed.
Source, hostile, package and final-workflow preflights also pass. Complete
release validation, normal review checks and merge remain required. This plan
awards no fixed-checkpoint credit.

## Legacy anchor and dependency

The pinned manuscript at
`final-pnp-proof-report-docs-hardened-7072f8d-sealed`
distinguishes quotient comparison from constructive full use in its mode
firewall and Package E. RW-SaturatePositive requires a real zero-cost retract
or named route for interface exposure; physical accounting alone is not a
semantic minimum theorem.

Connect the existing actual shared hidden-wire materializer to M274's
wire-backed full and quotient minima. Reuse the existing restoration,
normalization and complete unary constructor. Do not replace them with a
supplied observer, cost, minimum, completeness premise or full-lift certificate.

For arbitrary finite input, ordinary-output and field widths, write N for
physical source size, F for the full minimum, Q for the quotient minimum,
C for the actual shared materializer charge, and S for the gates actually
removed by full-field physical normalization of the paid quotient-minimum
witness. Prove the following exact interface:

- The older concrete quotient-agreement premise is equivalent to the new
  wire-profile quotient comparison.
- The constructed paid and normalized witnesses preserve every ordinary
  output and every actual computational field.
- F <= Q + C, hence projection defect D = F - Q satisfies D <= C.
- The paid witness costs F + (C - D).
- S <= C - D, and the normalized witness costs F + (C - D - S).
- The normalized witness is a strict gain against the original exactly when
  C - D - S < N - F. Returning no gain does not prove zero semantic slack.

These are general statements over actual input-derived programs and computed
reference witnesses. Reference minimization remains exhaustive, not a
polynomial-time PCCMin implementation.

## Explicit limitation and existing recovery

Three successive negations provide a checked case with positive full slack
where paid restoration followed by the fixed physical normalizer finds no gain.
This is a limitation of restoration alone, not a counterexample to every route,
the manuscript's full route family, or the final target.

The existing general unary constructor already handles that case. Prove that
its gate count equals the new full minimum for every one-input carrier with
arbitrary output and field widths. This is a compatibility theorem using its
existing completeness proof, not a new finite classifier or a reason to award
duplicate progress. Do not extend this one-input theorem to arbitrary inputs.

## Source and expectation changes

| Producer | Consumers and invariant | First verification |
| --- | --- | --- |
| Concrete restoration and exact overhead | General theorem types, full output/field preservation, strict original-size comparison | Changed leaf and hostile source contracts |
| Physical normalization | Actual saved-gate trace; no assumption of semantic optimality | Exact accounting and closure audit |
| Existing unary realizer | Exact one-input minimum identity, general width scope | Arbitrary-width theorem and limitation/recovery regression |
| Root and reviewed names | Root import, axiom probe, inventory producer, required-name consumer and publication fingerprint keys | Name-set preflight before inventory extraction |
| Canonical status and report | Claim flags, progress ledger, marked summaries, FAQ, package fixture and workflow assertions | Regenerate and reconcile before focused release checks |
| Durable workflow | Existing triggers and exact commands preserved; final size and shell syntax guards | Root/workflow preflight after the final workflow edit |

Keep all earlier theorem pins, declarations, publication rows and historical
coordinates intact. Derive emitted counts and fingerprints only after the
successful compiled inventory. Do not make a test accept an unexplained change.

## Verification and release

1. Keep M274's verified PR branch frozen while independent preparation runs.
2. Establish the explicit-root build before root-importing regressions and the
   exact axiom probe. Use only standard propext and Quot.sound in the reviewed
   closure; the statements do not require Classical.choice.
3. Run focused source/hostile checks, then prepare the compiled publication and
   current documentation expectations before any broad suite.
4. Start the public feature branch from the actual verified M274 merge, not its
   feature coordinate. Reuse exact unchanged leaf and executable-case evidence;
   the root, sealed release, PR, exact merge and production boundaries remain
   distinct.
5. Complete the normal draft PR, all-checks, manual-ready/manual-merge and exact
   merge reproduction process. Do not repeat the core proof build for PNPLabs.

## Remaining obligations and publication decision

This component does not derive the complete manuscript profile grammar or
terminal family, prove forced-cost equality, cover every exposure failure,
construct global route coverage, or prove unconditional SaturatePositive,
BCELReady or ZeroSlack. Complete polynomial PCCMin, encoded runtime/output/
certificate bounds, deterministic SAT and the eligible root remain open.

No fixed weighted checkpoint changes state. No general completeness is inferred
from the unary subcase or from the reference-minimum witness.

Publication decision: defer. This cost/compatibility interface and its explicit
limitation do not change the public global-proof bottom line, close a global
gate or earn a fixed checkpoint. Preserve the coherent current website source
pin and batch pending evidence at the next qualifying major publication.

See the [canonical progress ledger](../../status/PROOF_PROGRESS.json) and
[pinned archive](../../archive/legacy-v0/ARCHIVE.json).
