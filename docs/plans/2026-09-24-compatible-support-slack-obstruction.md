# Unrestricted compatible-support slack: core correction

## Impact-first selection

The next integration records a counterexample to a named, load-bearing lemma,
not another easy local proof or publication row. The pinned manuscript's section
2.2, Compatible replacement and Global slack law, treats boundary/interface
completeness as sufficient for substitution. The actual computed cut below
refutes the unrestricted slack inequality under that reading.

This is more consequential than a further causal-transport wrapper: the existing
checked framed and production-closed substitution results already establish
their restricted claims. Later initial-completion and routing arguments must
not rely on the unrestricted statement. The general source-derived full-profile,
admissibility and first-loss routing problem remains the mathematical priority.

## Legacy anchor and exact failure

Use `archive/legacy-v0/ARCHIVE.json` and document tag
`final-pnp-proof-report-docs-hardened-7072f8d-sealed`, section 2.2.
Preserve that historical manuscript unchanged.

The fixed eleven-gate source has ten ordinary outputs and is semantically
minimum among **all** equivalent implementations, not just a searched finite
list. Its proper nine-gate support is obtained with the existing extractor,
which computes every incoming and outgoing port. Its independent open function
has exact minimum eight. Hence local slack is one while whole-circuit slack
is zero. The smaller same-function literal substitution is cyclic; the identity
substitution succeeds.

The principal statement is:

```lean
theorem global_slack_law_violation :
    ¬ residualSlack support.extractedCandidate.toImplementation ≤
      residualSlack original.toImplementation
```

This is a refutation of the unrestricted raw-port-completeness interpretation,
not of the checked acyclic framed law, a correctly restricted admissible law,
the complete saturated manuscript route, or either answer to P versus NP.

## Integration boundary

Start from the latest fetched core main. Bring in only the three verified
counterexample modules, their explicit-root axiom audit and regression, and the
already reviewed impact-first agent policy. Do not bring in unrelated unmerged
research changes. The required supporting APIs already exist on core main.

Keep the result as a correction/research finding, not a new earned positive
roadmap row. Existing coverage and weighted checkpoints do not change merely
because these files or declarations are added. Review each earned checkpoint
for a dependency on the refuted statement; reduce credit only if an actual
earned obligation is invalidated. The existing restricted results are not
revoked by this example.

## Source and expectation chain

- Import the three modules from the explicit PNP root. Update the root-closure
  expectation and run its source-only positive and negative checks.
- Freeze the reviewed public declarations and proof-source contract in a
  focused audit. Reject weaker minimum/slack statements, supplied authority,
  missing ports, native proof authority and widened claims.
- Audit all eighteen public theorem closures (including both auxiliary semantic
  bridges) and retain the existing independent runtime
  controls. Run through the explicit root at integration.
- Extend the durable read-only proof workflow and current verifier coverage;
  do not add temporary workflows or duplicate a full proof suite in PNPLabs.
- Regenerate the compiled inventory and source-bound current publication
  artifacts after source stabilization. Derive changed counts and hashes; do
  not preselect them or alter immutable archive files.
- Annotate the current reviewer worksheet/crosswalk and current reconstruction
  explanation with the verified limitation, retaining historical quotations.
  Reconcile affected document contracts before broad validation.

## Verification and publication

The research-checkout leaf builds, exact axiom audit and runtime controls are
already green. Do not repeat them in that unchanged checkout. The isolated
main-based integration tests a genuinely different dependency boundary. Use
targeted source tests first, then the required root/inventory/publication and
normal PR/reproduction gates. No real GitHub write precedes repository-specific
identity, fetch and dry-run verification.

Publication decision: prepare a correction after the core integration and all
release gates pass, because a counterexample to a named central manuscript
lemma materially clarifies the public bottom line. Batch pending verified core
results without calling the correction proof-completion progress. Preserve the
existing coherent site pin until the replacement release is fully verified.

The remaining research target is to derive the complete admissible support
family and legitimate first-loss/rank-decreasing outcomes from actual terminal
inputs. A supplied profile, compilability proof, optimizer, coverage certificate,
rank map, or another finite example is not a solution to that obligation.

## Integration review

The isolated current-main root build, compiled inventory export, all eighteen
explicit public theorem axiom closures, and independent runtime controls pass.
All previously reviewed theorem types and axiom closures are unchanged.
The generated status, report and progress contracts remain fail closed.

Retain the M280 status/progress coordinate as the last earned-row baseline;
date this subsequent correction separately and bind its evidence by the exact
source seal and eventual merge commit. No pinned historical object, earned
milestone row, fixed checkpoint or progress value is rewritten or invented.

The source-only and focused publication checks have passed. Remaining release
work is the required full core validation, exact-head reproduction, draft PR,
durable CI and manual merge, followed by the selected batched correction
publication. Reuse the unchanged proof-source build evidence; do not repeat it
as a website check.
