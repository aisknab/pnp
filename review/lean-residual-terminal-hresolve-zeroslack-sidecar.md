# Review: proof-bearing HResolve ZeroSlack sidecar

## Theorem boundary

- [ ] Confirm the candidate family, implementation map, route predicates, and
  blocker semantics remain explicit inputs.
- [ ] Confirm `noHereditarySidecar` is the equation produced by
  `checkNoHereditarySidecar`, not a caller-supplied Boolean or proposition.
- [ ] Confirm exact routes imply `IsSemanticallyMinimum` and gain routes
  produce `StrictEquivalentGain` witnesses.
- [ ] Confirm the endpoint states only checked finite-family coverage and does
  not claim full HResolve, ZeroSlack, PCCMin, SAT in P, or `P = NP`.

## Executable and hostile evidence

- [ ] Re-run the axiom transcript and require all eight declaration lines.
- [ ] Re-run the regression and inspect the two blocked members, nonvacuous
  semantic minimum, nonvacuous strict gain, and four rejection cases.
- [ ] Re-run the hostile audit and confirm retained strings, caller success,
  missing checker binding, erased exact/gain semantics, assumptions,
  classical choice, fixed bounds, and claim widening fail closed.
- [ ] Confirm `ZeroSlack.lean` imports this module and no longer declares the
  old three-string `HResolveSidecarCertificate`.

## Publication boundary

- [ ] Match every required theorem name and kernel fingerprint to the compiled
  inventory.
- [ ] Confirm generated status keeps complete ZeroSlack, polynomial PCCMin,
  the publication root, and theorem emission false.
- [ ] Confirm public text distinguishes this proof-bearing interface from
  terminal-derived blocker semantics and full HResolve.
